# mult_agent_code — Claude Code 指南

项目类型：Python，本地优先的长任务 Agent harness（CLI + Studio）。

⚠️ **改任何代码/文档前，必读 `docs/zh/研发总计划.md`（执行计划唯一入口）**，并遵守 AGENTS.md 的 Code Triage Lock。

完整工程上下文统一维护在 **AGENTS.md**（跨工具共用，单一真源）。
下面这行把它导入到 Claude Code 的上下文，避免重复维护：

@AGENTS.md
