# Studio 前端产品化路线（对标主流竞品 · 多迭代）

> 单一真源：本文件是 Studio 前端"像成熟产品"的执行路线。每个迭代 brief→实现→验证(tsc/build/smoke/preview)→提交→回写本表状态。
> 触发：用户「前端仔仔细细对标主流竞品产品设计，更新详细开发计划，设计多个迭代，长任务一次做完，别一点点跟我说」。
> 关联：[[S76-studio-main-content-liveness]] brief、研发总计划 ACTIVE_SLICE S76。

## 0. 方法

12 代理 workflow：6 份竞品前端画像（Claude Code / Cursor / Cline·Roo / Zed·Windsurf / Codex·Aider / 跨界产品工艺 ChatGPT·Linear·Warp·Vercel）+ 5 维现状审计（信息架构 / 视觉系统 / 交互可供性 / 长任务健壮性·持久化 / 状态与反馈诚实）→ 综合成 I2–I14。竞品与审计原始产物见 workflow `wf_6fbda591-50a`。

## 1. 四条硬约束（同时成立）

1. **主区内容要丰富**（不止流式）——像 Cursor/Cline/Claude Code 那样把 plan/工具/diff/进度都端到主区。
2. **紧凑·高级**——主对话已刻意去框化，读起来像成熟产品；**不得重新堆卡片/加边框背景**。
3. **绝对诚实**——不渲染任何"未发生"的状态/标签/token/费用；`<think>` 清洗；压缩/截断要显式告知且原文一键可达。
4. **长任务不崩**——运行中打开高频事件流不卡死；session 可备份/还原。

## 2. 设计原则

1. **派生而非杜撰**：所有新主区（plan/todo 清单、phase 条、context 表、live diff）都从运行时**已流出的事件**派生（phase、transcript_kind plan/todo_update、cost_report、file_changed）；无数据则渲染空而非占位。richness+诚实+零后端改动一次满足。
2. **每个 token 过诚实闸**：状态/标签/表只在有真事件字段时渲染；绝不显示 $（cost_report 无定价）、绝不在终止事件前显示绿色"done"、估算 token 必标 `est.`。
3. **去框化神圣**：richness 靠排版/字重/缩进/语义色/默认折叠，**不**靠新卡片背景。新元素（phase 条、清单、context 表）是复用 tokens 的安静内联条。响亮色只留给需人决策的（diff/权限/错误）。
4. **稳定是结构性的**：完成的 turn 变不可变、廉价渲染（memo/虚拟化）节点；只有流式尾部活跃重渲染。切会话缓存不清空。重连带 sequence 游标做无缝幂等回填；磁盘 JSONL 永远是真源。
5. **一条 transcript，统一块可供性**：收敛到既有 turn/step 模型为原子单位，往上加折叠/跳转/锚点，而非再开并列面板。

## 3. 竞品差距 → 迭代（优先级）

| 差距 | 谁做得好 | 我们现状 | 优先 | 迭代 |
|---|---|---|---|---|
| 无常驻 plan/todo 清单（长跑缺"脊柱"） | Cursor/Cline/Windsurf/Claude Code | 只当泛化折叠步，无固定原地更新清单 | must | I3 |
| 无 phase/进度条 | Claude Code/Warp | phase 字段仅内部分组用 | must | I3 |
| transcript 无虚拟化；切会话清空重读 | Warp BlockList / ChatGPT 窗口化 | Thread 全量挂 DOM；切换 reset []；readSessionEvents 无 offset | must | I6+I7 |
| SSE→轮询静默、无重连指示、无 seq 游标 | Vercel/CI 重连横幅 + 单调 seq | 静默退化，连接态不达 UI，seq 被忽略 | must | I2+I8 |
| 发送/目标失败被静默吞 | ChatGPT/Claude "Failed—retry?" | sendGoal 无 catch | must | I2 |
| 运行中不能排队/steer；不能编辑重发 | Cursor 队列 / ChatGPT edit-resend | Send→Stop 阻塞；user_message 不可编辑 | should | I9 |
| 主流无 context/token 压力表 | Cline 持久遥测头 | cost_report 仅深藏 inspector | should | I4 |
| 无 session 备份/分支/还原；delete 即 fs.rm | Cursor/Cline checkpoint / ChatGPT 分支 | 硬删无软删/快照 | should | I10 |
| 无命令面板/键盘导航 | VS Code/Linear/Warp Cmd+K | 仅 4-5 个面板切换键 | nice | I13 |
| 设计系统漂移（字重/间距/色/按钮高/无浅色） | Linear/Vercel 规范 | tokens 有但采用不全，硬编码色 | should | I14 |
| 原始 `<think>` 可能泄漏、推理未一致次级化 | claude.ai 灰斜体 + "Thought for N" | iter1 有芯片但清洗未全路径保证 | should | I5 |
| 错误含糊无码无下一步 | Vercel 错误摘要+跳转 | friendlyError 仅 6 模式，其余空 | should | I12 |
| 主流无主动改动文件 accept/reject | Cursor/Zed/Windsurf 内联 diff | 藏在 diff-focus 模式后 | should | I11 |

## 4. 迭代清单

> 状态：☑ done · ▶ in progress · ☐ todo。DoD 见各行 acceptance；全部**不触 DO_NOT_TOUCH**（后端只动 `studio/server.mjs`）。

### I1 ☑ 主内容区活起来（已提交 0c3bb94）
优先自有富事件（修"没一个字"根因+中文乱码）；思考常驻"Thought for Xs"芯片；工具/权限卡完成后常驻；工作区切换解卡。

### I2 ☑ 诚实的发送/动作错误 + 重连指示（S）（已提交）
- **目标**：发送/决策/权限失败绝不静默；流断了用户看得见。
- **scope**：`useSessionEvents` 的 sendGoal/sideAsk/permit/resolveDecision 包 try/catch→toast.error 且不丢输入框文本；`api.ts subscribeToEvents` 暴露 `live|reconnecting|offline`（onStatus）；`MissionPaneHeader` 非 live 时显安静状态 pill；轮询指数退避(1→2→4→8 cap 30s)+3 次失败后"立即重连"；pending turn 上"发送失败—重试"。
- **files**：useSessionEvents.ts / api.ts / MissionPaneHeader.tsx / App.tsx / Thread.tsx
- **验收**：杀服务器 ~2s 内出"Reconnecting…"并自动恢复；send reject 出 toast 且文本留存；退避有上限（时序单测）；build 绿。
- deps: —

### I3 ☑ 常驻 phase 条 + plan/todo 清单（M）（已提交）
- **目标**：给每次运行可见脊柱：紧凑 phase 指示 + 原地更新的 plan/todo 清单，纯派生。
- **scope**：新 `planModel.ts derivePlan()` 折叠 transcript_kind plan/todo_update→有序项(pending/in-progress/done/blocked)，最新覆盖，无则渲染空；`PlanChecklist.tsx` 去框化钉在当前 turn 顶(○◐✓⚠)，非活跃 turn 折成"N of M"；`PhaseStrip.tsx` understand→plan→execute→review→result 分段仅点亮到达段；接入 Thread 活跃 turn 上方。
- **files**：planModel.ts / PlanChecklist.tsx / PhaseStrip.tsx / Thread.tsx / narrative.ts / thread-turn.css
- **验收**：有 plan 事件→清单原地翻状态；无→不显空壳；phase 条只点亮已发生段；focus 模式仍紧凑；derivePlan 单测。
- deps: —

### I4 ☑ 可一眼看的 context/token 表（诚实标 est.）（已提交）
> 注：readContextUsage 共享 helper 已落 inspectorUtils，EvidenceExplorer 去重复用留待 I14。
> **R1 缺陷修复**：server.mjs `redact()` 的 key 正则 `token` 误伤 `*_tokens` 计数字段（把 token 数脱敏成 [REDACTED]）→ 改 `isSecretKey` 白名单排除计数遥测、仍红所有真凭证形态；现表显 "4% context · 8.6k/200k · 9.3k in · 6.8k out (est.)"。

## R 轮次（/goal：5 轮 · 前后端搭配 · 人性化 · 可视化零缺陷 · 修缺陷 · 更新文档）

- **R1 缺陷修复+后端同步+诚实错误恢复**：☑ redact 过度脱敏修复（token 计数可见）+ ☑ 工作区切换 guard 修复已重启生效（切 test_project 成功）+ I12 诚实错误恢复卡。
- **R2 长任务健壮性**（I7 虚拟化/memo/跳最新/大输出截断）。
- **R3 无缝重连+后端同步**（I8 seq 游标）。
- **R4 人性化交互**（I9 排队/steer/编辑重发 + 键盘）。
- **R5 可视化零缺陷+文档**（I14 设计系统收敛+浅色 + I11 内联 diff + 文档归一）。
- **scope**：`ContextMeter.tsx` 细内联填充条(used vs context_window_tokens)+"input/output tokens (est.)"读 runDetail.cost_report；字段缺则**不渲染**（不显 0）；绝不显 $；>0.75 转 --warn（对齐 hard_stop）；抽 `readContextUsage(runDetail)` 与 EvidenceExplorer 共用。
- **files**：ContextMeter.tsx / EvidenceExplorer.tsx / ContextPanel.tsx / MissionPaneHeader.tsx / inspectorUtils.ts
- **验收**：有字段按比例填充+过阈变色；无字段不显表不显"0 tokens"；无 $；helper 共用。
- deps: —

### I5 ☑ 推理清洗 + "Thought for Ns"一致 + 流式打磨（已提交）
> 注：cleanReasoning 已集中到 narrative.ts 并在 ThinkingBlock/ChatStreamPreview/LiveStream/TurnFinal 全路径生效；流式尾部加闪烁 caret。增量 markdown 按完整块解析（防流末重排）留待 I14/I7（MarkdownBody memo）一并做，避免此刻改解析器引入风险。
- **scope**：`narrative.ts` 集中 `cleanReasoning()` 剥 `<think>`/工具脚手架，全路径(尾+完成)生效；芯片在所有 turn 都出、展开灰斜体默认折叠；增量 markdown 按完整块解析防流末重排；流式尾部 model_end 前显闪烁光标。
- **files**：narrative.ts / ConversationTurn.tsx / LiveStream.tsx / TurnFinal.tsx / thread-turn.css
- **验收**：含字面 `<think>` 两态都被剥（cleanReasoning 单测）；有推理必显折叠芯片；光标仅流式在、model_end 消失。
- deps: —

### I6 ◑ 切会话缓存（已提交）；完成 turn memo 并入 I7
> 缓存部分已落地并验证（A→B→A 内容即时恢复无重取闪空）。ConversationTurn memo 需 callback 稳定化 + 大 fixture 验证，与 I7 虚拟化的渲染性能一起做（同批更安全），故 I6 记 ◑ 部分完成、剩余并入 I7。
- **scope**：`useSessionEvents` 用 per-session Map 缓存，切回不清空全读、后台刷新活跃会话；`ConversationTurn` React.memo，完成 turn 视为不可变仅活跃 turn 重渲染；拆完成 turn 与流式尾部的 narrative 派生；磁盘 JSONL 仍真源。
- **files**：useSessionEvents.ts / Thread.tsx / ConversationTurn.tsx / threadUtils.ts / turnDiff.ts
- **验收**：A→B→A 不闪空不全量重取；2k 事件下仅活跃 turn 重渲染（dev 计数）；既有 thread 测试过。
- deps: —（是 I7 前置）

### I7 ◑ transcript 窗口化 + 跳到最新 + 尊重上滚（R2 · 已提交）
> 已落：按数量窗口化（仅渲染最近 60 turn，超出显"Load N earlier turns"）→ DOM 有上限、长 run 不冻结；跳到最新 pill（上滚离活边即现）；自动跟随只在贴底时触发（读 ref 防 scroll↔state 环，尊重手动上滚不再硬拽）。大工具输出仍由既有 ClampedOutput 截断。**剩余并入后续**：完成 turn React.memo + 增量 narrative（优化 JS 重算，窗口化已防崩，属性能非正确性）；错误 gutter 标记跳转（依赖更细锚点）。
- **scope**：仅挂视口内/近视口 turn（离屏定高占位）；自研或本地 vendored 窗口化（无 CDN，遵 CSP）；上滚离活边显"跳到最新↓"并暂停自动跟随（扩 Thread near-bottom）；失败 turn 在滚动槽/minimap 标记 + 跳到下一个错误；超大单条工具输出 head+tail 截断 + 显式"show N more"。
- **files**：Thread.tsx / ConversationTurn.tsx / ToolCallCard.tsx / useThreadWindow.ts / thread-shell.css
- **验收**：合成 2 万事件流畅、挂载 turn 数有上限；上滚出 pill 暂停跟随、点回活边；错误 turn 槽标记可跳；大输出截断带通知可展开。
- deps: I6

### I8 ☐ 带 seq 游标的无缝 SSE 重连 + 幂等回填（M）
- **scope**：`server.mjs` readSessionEvents/notifySSE 附单调 per-session seq，events 端点收 `?since=<seq>` 游标回填；SSE 连上带 last seq，服务器只补 seq>since；`api.ts` 跟 lastSeq、(重)订阅带 since、seen 游标存 sessionStorage 使刷新只补增量；event_id 加 per-process 计数器防同毫秒碰撞。
- **files**：server.mjs / api.ts / useSessionEvents.ts / types.ts
- **验收**：运行中重启服务器重连精确补齐无缺无重（脚本化崩溃/重连对比磁盘）；刷新只补游标后；同毫秒事件 id 不同；mergeEventLists 仍去重。
- deps: I2

### I9 ☐ 运行中排队 + steer + 编辑重发（M）
- **scope**：运行中 Composer 不阻塞，Enter 入队(显"queued"芯片可拖排序)，turn 边界(model_end/决策解)自动发队首；steer 走既有 side/next-action 通道到下一步、不可达则明确等下一 turn（诚实不谎称中途注入）；最新 user_message 加 Edit 重填 composer、重发起新 turn 不改历史；Ctrl+Enter 发、Esc 停。
- **files**：Composer.tsx / useSessionEvents.ts / ConversationTurn.tsx / useStudioKeyboard.ts / composer.css
- **验收**：运行中 Enter 出队芯片、到边界自动发、可重排；编辑最新消息重填并起新 turn 不改历史；Esc 停 Ctrl+Enter 发；切会话清队不串。
- deps: I2

### I10 ☐ session 备份 + 从 turn 分支 + 还原（先软删）（L）
- **scope**：`server.mjs` deleteSession 改软删(移 `.trash/<id>`+deleted_at)+还原/清除端点；快照/分支端点：复制 session.json+events.jsonl 到某 turn 末为新 session(parent_session_id/branched_from_seq)；`types.ts StudioSession` 加可选 version/parent_session_id/branched_from_seq/deleted_at(向后兼容)；UI 历史 turn"从此分支"+删除 Undo toast；诚实文案：分支只继承对话不回滚工作区文件。
- **files**：server.mjs / api.ts / types.ts / ConversationTurn.tsx / SessionList.tsx / App.tsx
- **验收**：删→trash+Undo 完整还原；从 turn K 分支=恰好含到 K 的事件+parent 设；旧无新字段 session 仍加载；文案说明不回滚文件。
- deps: I8

### I11 ☐ 主线内联 summary-first diff + 每文件 accept/reject（M）
- **scope**：产生 file_changed 的 turn 末加 `ChangesSummary`（"3 files, +47 −12"）可展开为每文件 diff(复用 DiffReviewPane)；每文件 Accept(git stage)/Reject(git discard) 接既有 api；有改动自动露出但默认折叠保密度；诚实：反映真 git 态，成功前不显 accepted。
- **files**：ConversationTurn.tsx / ChangesSummary.tsx / DiffPreviewSection.tsx / DiffReviewPane.tsx / thread-turn.css
- **验收**：有改动显摘要可展开无需 diff-focus；accept/reject 走 git 且反映结果态；默认折叠保密度；计数与 git 一致。
- deps: I6

### I12 ☑ 诚实错误恢复 + 更丰富错误分类（R1 · 已提交）
> friendlyErrorText 未知错误不再返回空串→显真错误首行(脱敏)+下一步；friendlyErrorCategory 派生 auth/rate_limit/timeout/network/model 类别写入失败事件 data.error_category；TurnFinal 失败态显类别徽章。Retry/Open logs 内联动作留待与 I7 错误标记跳转一并接线。
- **scope**：error 事件显紧凑错误块=(a)已知友好文案(b)"Inspect"露脱敏非空首行(c)Retry/Open logs；`server.mjs friendlyErrorText` 加分类标(auth/rate_limit/timeout/network/model/unknown)供 UI 徽章，unknown 透传脱敏首行非空串；接 I7 错误标记跳转；不杜撰码。
- **files**：TurnFinal.tsx / EventCard.tsx / server.mjs / thread-shell.css
- **验收**：已知错显分类徽章+友好+Retry；未知显脱敏首行(非空)+Inspect；Inspect 不泄密；错误块默认次级化保密度。
- deps: I2

### I13 ☐ 命令面板 + 键盘导航（M）
- **scope**：`CommandPalette.tsx`(Ctrl+K)：切会话(模糊)/跳 turn N/跳下个错误/跳待决策·权限/切面板/停运行；方向键在 turn/step 移选、Enter 聚焦 inspector；`useStudioKeyboard` 注册(Ctrl+K、g-e 下个错误、g-d 下个决策)不撞既有；面板动作复用既有 handler。
- **files**：CommandPalette.tsx / useStudioKeyboard.ts / App.tsx / SessionList.tsx / Thread.tsx
- **验收**：Ctrl+K 可切会话/跳 turn/跳错误决策；方向键移选更新 inspector；既有键无回归；面板动作=鼠标等价。
- deps: I7

### I14 ☐ 设计系统收敛（字重/间距/动效/色 token/按钮高/浅色）（L）
- **scope**：字重收敛到 2-3 token 值替换内联 500-780；间距贴 4/8；按钮高统一 control token、动效并入共享 --dur/--ease baseline、去内联 #b8860b 归一 --warn；加 prefers-color-scheme 浅色 token 集+设置开关(暗默认)；inspector 列表行高一致、聚焦环对比增强。
- **files**：tokens.css / thread-turn.css / thread-shell.css / components.css / composer.css / sidebar.css / inspector-diff.css / inspector-evidence.css / shell.css
- **验收**：触及 CSS 无内联 #b8860b(grep 零)；字重降到 token 集；切浅色连贯无不可读对比；间距全 4/8。
- deps: I3,I4,I9,I11,I12（放最后一次性归一所有新组件样式）

## 5. 排序与依赖

关键路径 **I2 → I6 → I7 → I13**；**I3/I4/I5 可立即并行落地**（派生型、零后端、最像 Cursor/Cline 的可见升级、最低风险）；持久化链 **I8 → I10**；I9 依 I2、I11 依 I6、I12 扩 I2、I14 收尾归一 I2–I13 样式。每迭代独立可发、可回退，均不触后端 Python（后端只动 `studio/server.mjs`）。

## 6. 风险（择要）

- I7 窗口化易破坏 scroll-to-bottom/选择/自动跟随 → 建在 I6 不可变 turn 拆分上、20k fixture 验证前保留非窗口化回退。
- I3 清单依赖运行时真发 plan/todo_update；无则诚实显空（脊柱质量=发射质量，不杜撰）。
- I8 改 server.mjs 与 event_id 方案；坏游标契约会丢/重事件 → 崩溃/重连对比磁盘测试守门后再默认开。
- I9 别暗示运行时做不到的中途注入 → 诚实闸：队列/steer 未确认真通道就明确等下一 turn 边界。
- I4 别把估算 token 当精确 → 强制 est. + 无 $。
- I14 浅色面大；若时间紧先发暗色打磨、浅色作后续，不阻塞收敛。

## 7. 执行与验收规范

每迭代：（可选微 recon）→ 精确 Edit 实现 → `tsc --noEmit` + `vite build` + 相关 studio smoke + 关键路径 preview 冒烟 → 提交（一迭代一 commit，中文信息，Co-Authored-By）→ 回写本表状态☑。不推送直到用户明示。遵守 [[keep-docs-aligned-no-drift]]、[[convergence-direction]]、[[studio-product-convergence-refactor]]。
