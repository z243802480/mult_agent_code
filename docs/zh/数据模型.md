# 多智能体自主开发系统 - 数据模型

## 1. 文档目的

本文档定义系统核心数据对象、状态枚举、文件落盘位置和 schema 约定。

目标：

- 让运行时、命令、智能体、工具和评估系统共享同一套结构化语言。
- 减少靠自然语言传递状态造成的歧义。
- 支持长任务续航、上下文压缩、任务恢复和审计。
- 为后续代码实现提供直接的数据结构依据。

## 2. 通用约定

### 2.1 Schema 版本

每个持久化对象必须包含：

```json
{
  "schema_version": "0.1.0"
}
```

版本规则：

- patch：字段说明、非破坏性默认值调整。
- minor：新增可选字段。
- major：字段删除、字段含义改变或结构破坏性变更。

### 2.2 ID 约定

推荐 ID 格式：

```text
run-20260427-0001
goal-0001
task-0001
decision-0001
exp-0001
artifact-0001
event-0001
agent-0001
toolcall-0001
memory-0001
```

ID 应在单个项目内唯一。

### 2.3 时间格式

所有时间使用 ISO 8601：

```text
2026-04-27T14:30:00+08:00
```

### 2.4 状态字段

状态字段必须使用枚举，不使用自由文本。

自由文本应放到：

- `summary`
- `reason`
- `notes`
- `description`

### 2.5 文件路径

路径使用项目根目录相对路径：

```json
{
  "path": "src/main.py"
}
```

禁止在持久化对象中默认写入机器相关绝对路径，除非对象是本机运行日志。

### 2.6 全局状态与 Workspace 状态

Asteria 必须区分全局产品状态和单个 workspace 状态。

全局状态位于用户目录，例如 Windows 上的：

```text
C:\Users\<User>\.asteria\
```

全局状态只保存跨项目配置和索引：

- 最近 workspace 列表。
- 当前选中的 workspace 指针。
- 全局用户设置。
- 全局模型 provider / route 本机配置引用。
- 账号、token 额度、UI 偏好、产品级缓存。
- 不应保存单工程的 run、session、产物、证据和 task graph。

Workspace 状态位于项目根目录：

```text
<workspace_root>\.asteria\
```

Workspace 状态保存该工程的全部运行状态：

- project / policy。
- sessions / runs。
- task graph。
- worker invocation / result。
- evidence / validation / cost / report。
- Studio conversation / session thread。
- candidate workspace / promotion queue 引用。

单工程输出文件、报告、运行证据和 `.asteria/runs/` 必须落在 workspace 目录下。只有系统级全局配置才允许写入用户目录。

## 3. 目录映射

Workspace 内推荐落盘结构：

```text
AGENTS.md
.asteria/
  project.json
  policies.json
  current_session.json
  context/
    root_snapshot.json
    snapshots/
    handoffs/
  tasks/
    backlog.json
  runs/
    run-20260427-0001/
      run.json
      goal_spec.json
      task_plan.json
      task_plan_eval.json
      events.jsonl
      runtime_hooks.jsonl
      tool_calls.jsonl
      tool_observations.jsonl
      model_calls.jsonl
      workers.jsonl
      worker_results.jsonl
      validation_results.jsonl
      experiments.jsonl
      decisions.jsonl
      artifacts.jsonl
      cost_report.json
      eval_report.json
      final_report.md
  memory/
    project_memory.jsonl
    user_memory.jsonl
    research_memory.jsonl
    experiment_memory.jsonl
  acceptance/
    latest_summary.json
    acceptance_report.json
    history.jsonl
    failures/
  plugins/
    example.plugin.json
```

全局产品状态推荐结构：

```text
%USERPROFILE%/.asteria/
  user_settings.json
  recent_workspaces.json
  current_workspace.json
  model.routes.local.ps1
  accounts/
  cache/
  logs/
```

全局目录不得包含 workspace 的源码副本、运行产物全文、session transcript 全文或未脱敏证据包。

## 4. ProjectConfig

文件：

```text
.asteria/project.json
```

用途：

记录项目级元数据和 agent 入口信息。

```json
{
  "schema_version": "0.1.0",
  "project_id": "project-0001",
  "workspace_id": "workspace-0001",
  "name": "mult-agent-code",
  "workspace_type": "planning_workspace",
  "created_at": "2026-04-27T14:30:00+08:00",
  "updated_at": "2026-04-27T14:30:00+08:00",
  "languages": ["markdown"],
  "frameworks": [],
  "package_managers": [],
  "commands": {
    "install": null,
    "run": null,
    "test": null,
    "lint": null,
    "typecheck": null,
    "build": null,
    "format": null
  },
  "important_paths": ["docs/", "docs/zh/"],
  "protected_paths": [".env", "secrets/", ".git/"],
  "root_guidance_path": "AGENTS.md",
  "default_policy_path": ".asteria/policies.json"
}
```

枚举：

```text
workspace_type:
  empty_workspace
  planning_workspace
  codebase
  multi_project
  mixed
  unknown
```

`ProjectConfig` 是 workspace-local 对象。`workspace_id` 用于把全局 recent workspace 索引、Studio project switcher 和本地 `.asteria/` 状态关联起来；它不要求 workspace 路径写入项目内配置。

## 4.1 GlobalWorkspaceIndex

文件：

```text
%USERPROFILE%/.asteria/workspaces.json
```

用途：

记录产品级 workspace 列表和当前选中项，支撑类似 Codex / Claude Code 的 project switcher。

```json
{
  "schema_version": "0.1.0",
  "current_workspace_id": "workspace-8d3a...",
  "current_workspace_root": "H:/mult_agent_code",
  "recent_workspaces": [
    {
      "workspace_id": "workspace-8d3a...",
      "name": "mult_agent_code",
      "workspace_root": "H:/mult_agent_code",
      "output_root": "H:/mult_agent_code",
      "artifact_root": "H:/mult_agent_code/.asteria/artifacts",
      "last_opened_at": "2026-05-27T14:30:00+08:00"
    }
  ],
  "updated_at": "2026-05-27T14:30:00+08:00"
}
```

约束：

- `workspace_root` 是本机 UI/CLI 用的绝对路径，只能保存在全局用户目录，不能写入可提交的 workspace 配置。
- 全局索引只保存 recent/current workspace、默认 output_root 和 artifact_root；不保存 run evidence、prompt、API key、产物正文或跨 workspace session。
- 打开 workspace 后，所有 run/session/evidence/artifact 仍写入该 workspace 的 `.asteria/`。
- 删除最近项不删除 workspace 文件。
- 切换 workspace 必须同时切换 current session 解析范围，不能跨 workspace 读取 session。

## 4.2 WorkspaceEnvelope

文件：

```text
.asteria/runs/<run_id>/workspace_envelope.json
```

用途：

把一次 run 绑定到明确 workspace、输入范围、输出范围、候选工作区策略、权限模式和产物根目录。它是 workspace-local 运行契约，不是用户级 recent index。

```json
{
  "schema_version": "0.1.0",
  "workspace_id": "workspace-8d3a...",
  "workspace_root": "H:/mult_agent_code",
  "input_roots": ["H:/mult_agent_code"],
  "output_root": "H:/mult_agent_code",
  "artifact_root": "H:/mult_agent_code/.asteria/artifacts",
  "candidate_workspace_policy": "controlled_patch",
  "worktree_policy": "controlled_patch",
  "read_scope": ["H:/mult_agent_code"],
  "write_scope": ["H:/mult_agent_code"],
  "scope_summary": {
    "input_root_count": 1,
    "read_scope_count": 1,
    "write_scope_count": 1,
    "output_inside_workspace": true,
    "artifact_root_inside_workspace": true
  },
  "artifact_policy": "workspace_artifacts",
  "git_policy": "detect",
  "permission_mode": "reviewed_auto",
  "created_at": "2026-05-27T14:30:00+08:00"
}
```

约束：

- `run/session/evidence/artifact` 默认仍落在 workspace-local `.asteria/`。
- 代码改动类产物可以写入 workspace 受控 write scope；报告、研究、导出和生成型产物必须能追溯到明确 `output_root` / `artifact_root`。
- `worktree_policy` 是产品侧候选工作区语义；`candidate_workspace_policy` 保留为当前 runtime 执行策略字段，二者在第一阶段保持一致。
- Chat/Status/Sessions/Studio 消费该 envelope 时，应向用户展示 root、output、artifact 和 permission，而不是只展示 backend run id。

## 5. PolicyConfig

文件：

```text
.asteria/policies.json
```

用途：

记录预算、安全、决策颗粒度和工具策略。

> 下例为 `autonomous_long_task` **活跃 profile 的有效预算**（其 `max_replans_per_task=3`）。
> base 默认是 `2`，由 `autonomous_long_task` profile 覆盖为 `3`——分层有意为之，真源在
> `policies.default.json`（base vs profile），不是漂移。详见 [`成本安全与风险.md`](./成本安全与风险.md)。

```json
{
  "schema_version": "0.1.0",
  "decision_granularity": "balanced",
  "active_budget_profile": "autonomous_long_task",
  "budgets": {
    "max_model_calls_per_goal": 200,
    "max_tool_calls_per_goal": 1000,
    "max_total_minutes_per_goal": 240,
    "max_iterations_per_goal": 32,
    "max_repair_attempts_total": 20,
    "max_repair_attempts_per_task": 4,
    "max_replans_per_task": 3,
    "max_research_calls": 20,
    "max_user_decisions": 12
  },
  "budget_profiles": {
    "validation": {
      "max_model_calls_per_goal": 60,
      "max_tool_calls_per_goal": 120
    },
    "balanced": {
      "max_model_calls_per_goal": 120,
      "max_tool_calls_per_goal": 500
    },
    "autonomous_long_task": {
      "max_model_calls_per_goal": 200,
      "max_tool_calls_per_goal": 1000
    }
  },
  "context": {
    "compaction_threshold": 0.75,
    "hard_stop_threshold": 0.9
  },
  "permissions": {
    "allow_network": false,
    "allow_shell": true,
    "allow_destructive_shell": false,
    "allow_global_package_install": false,
    "allow_secret_file_read": false,
    "allow_remote_push": false,
    "allow_deploy": false
  },
  "model_routing": {
    "planning": "strong",
    "architecture": "strong",
    "coding": "medium",
    "review": "strong",
    "summarization": "cheap",
    "classification": "cheap"
  }
}
```

枚举：

```text
decision_granularity:
  autopilot
  balanced
  collaborative
  manual
```

## 6. Session 与 Run

`Session` 是面向用户的上下文单位：一个目标、一组任务、一段可恢复的工作会话。

`Run` 是内部执行记录：当前 MVP 中一个 session 映射到一个 `.asteria/runs/<run_id>/`
目录。为了兼容已有日志和 schema，内部字段仍使用 `run_id`；用户命令和文档使用
`session_id` 表达要恢复或切换的上下文。

当前 session 指针文件：

```text
.asteria/current_session.json
```

旧的 `.asteria/current_run.json` 只作为兼容读取入口；新的运行时写入
`.asteria/current_session.json`。

```json
{
  "schema_version": "0.1.0",
  "session_id": "run-20260427-0001",
  "set_at": "2026-04-27T14:30:00+08:00",
  "reason": "plan_created"
}
```

## 7. Run

文件：

```text
.asteria/runs/<run_id>/run.json
```

用途：

记录一次运行的总状态。

```json
{
  "schema_version": "0.1.0",
  "run_id": "run-20260427-0001",
  "goal_id": "goal-0001",
  "status": "running",
  "started_at": "2026-04-27T14:30:00+08:00",
  "ended_at": null,
  "entry_command": "asteria run \"做一个密码测试工具\"",
  "current_phase": "PLAN",
  "workspace": {
    "mode": "single_workspace",
    "path": "."
  },
  "summary": ""
}
```

枚举：

```text
run.status:
  queued
  running
  paused
  blocked
  completed
  failed
  cancelled

current_phase:
  INIT
  SPEC
  PLAN
  BRAINSTORM
  DECIDE
  RESEARCH
  DESIGN
  IMPLEMENT
  VERIFY
  REVIEW
  REPAIR
  KEEP_OR_DISCARD
  MEMORY_UPDATE
  REPORT
  DONE
  BLOCKED
```

## 8. GoalSpec

文件：

```text
.asteria/runs/<run_id>/goal_spec.json
```

用途：

结构化用户目标。

```json
{
  "schema_version": "0.1.0",
  "goal_id": "goal-0001",
  "original_goal": "做一个密码测试工具",
  "normalized_goal": "构建一个本地优先的密码测试与辅助工具",
  "goal_type": "software_tool",
  "assumptions": [
    "用户希望工具本地运行",
    "默认不把密码发送到外部服务"
  ],
  "constraints": [
    "local_first",
    "privacy_safe"
  ],
  "non_goals": [
    "不证明密码绝对安全",
    "默认不查询在线泄露 API"
  ],
  "expanded_requirements": [
    {
      "id": "req-0001",
      "priority": "must",
      "description": "提供密码强度评分",
      "source": "inferred",
      "acceptance": ["输入密码后显示强度等级和主要原因"]
    }
  ],
  "target_outputs": ["local_web_app", "readme", "tests"],
  "definition_of_done": [
    "可以本地运行",
    "可以输入密码并获得强度反馈",
    "有隐私说明",
    "有基础测试"
  ],
  "verification_strategy": ["unit_tests", "smoke_test"],
  "budget": {
    "max_iterations": 8,
    "max_model_calls": 60
  }
}
```

枚举：

```text
goal_type:
  software_tool
  codebase_improvement
  research
  report
  knowledge_base
  automation
  unknown

expanded_requirements.priority:
  must
  should
  could
  wont

expanded_requirements.source:
  user
  inferred
  research
  memory
  decision
```

## 9. Task

文件：

```text
.asteria/tasks/backlog.json
.asteria/runs/<run_id>/task_plan.json
```

用途：

记录可调度任务。

```json
{
  "schema_version": "0.1.0",
  "task_id": "task-0001",
  "title": "实现密码强度评分",
  "description": "根据长度、字符多样性和常见弱密码规则给出评分",
  "status": "ready",
  "priority": "high",
  "role": "CoderAgent",
  "depends_on": [],
  "acceptance": [
    "输入密码后返回分数",
    "返回可解释原因",
    "包含单元测试"
  ],
  "allowed_tools": ["read_file", "search_text", "apply_patch", "run_tests"],
  "expected_artifacts": ["src/scoring.ts", "tests/scoring.test.ts"],
  "assigned_agent_id": null,
  "created_at": "2026-04-27T14:30:00+08:00",
  "updated_at": "2026-04-27T14:30:00+08:00",
  "notes": ""
}
```

枚举：

```text
task.status:
  backlog
  ready
  in_progress
  testing
  reviewing
  blocked
  done
  discarded

task.priority:
  critical
  high
  medium
  low
```

## 10. TaskPlanEval

文件：

```text
.asteria/runs/<run_id>/task_plan_eval.json
```

用途：

记录整张任务计划的确定性质量评估，避免 PlannerAgent 生成“看起来有任务、实际不可执行或不可验收”的计划。

```json
{
  "schema_version": "0.1.0",
  "run_id": "run-20260427-0001",
  "created_at": "2026-04-27T14:30:00+08:00",
  "status": "pass",
  "overall_score": 0.92,
  "scores": {
    "granularity_score": 0.9,
    "dependency_score": 1.0,
    "acceptance_score": 0.9,
    "artifact_score": 0.9,
    "tooling_score": 0.9
  },
  "summary": "Task plan quality pass with score 0.92; 0 error(s), 1 warning(s).",
  "issues": [],
  "recommendations": [],
  "task_count": 4
}
```

评估维度：

- 任务颗粒度是否适中。
- 是否有明确 ready 入口。
- `depends_on` 是否引用有效任务。
- `acceptance` 是否可观察、可验证。
- `expected_artifacts` 是否足够具体。
- `allowed_tools` 是否支持任务真实执行和验证。

枚举：

```text
task_plan_eval.status:
  pass
  warn
  fail

task_plan_eval.issues[].severity:
  warning
  error
```

`fail` 代表当前计划不应直接进入 `/execute`，应优先重规划或创建决策点。

## 11. AgentSpec

用途：

定义智能体角色和权限。

```json
{
  "schema_version": "0.1.0",
  "agent_id": "agent-0001",
  "role": "CoderAgent",
  "model_tier": "medium",
  "allowed_tools": ["read_file", "search_text", "apply_patch", "run_tests"],
  "write_scope": "assigned_workspace",
  "budget": {
    "max_model_calls": 10,
    "max_tool_calls": 40
  },
  "instructions_ref": "AGENTS.md#coderagent"
}
```

枚举：

```text
agent.role:
  GoalSpecAgent
  PlannerAgent
  ArchitectAgent
  ProductAgent
  ResearchAgent
  CoderAgent
  UIExperienceAgent
  TesterAgent
  ReviewerAgent
  AutoCorrectionAgent
  MemoryAgent
  ReleaseAgent

model_tier:
  cheap
  medium
  strong
```

AgentSpec 是静态角色模板，不应保存某次运行的动态上下文、工具调用历史、模型调用历史或文件副作用。运行时应通过 Session、WorkerInvocation 和 ContextMount 组合出一次具体执行。

## 11.1 RuntimeProfile

用途：

描述一次 Worker 启动时挂载的运行环境。RuntimeProfile 可以由多个子 profile 组成，让同一个 AgentSpec 在不同任务中使用不同模型、权限、账号和上下文。

```json
{
  "schema_version": "0.1.0",
  "runtime_profile_id": "runtime-profile-0001",
  "agent_id": "agent-0001",
  "model_profile_id": "model-profile-0001",
  "tool_permission_profile_id": "tool-profile-0001",
  "account_profile_id": "account-profile-0001",
  "sandbox_profile_id": "sandbox-profile-0001",
  "context_mount_id": "context-mount-0001",
  "budget": {
    "max_model_calls": 3,
    "max_tool_calls": 20,
    "max_runtime_minutes": 10
  }
}
```

原则：

- profile 保存配置和引用，不保存 secret 明文。
- 模型选择、账号权限、工具权限和沙盒策略应可独立替换。
- Coordinator 根据任务类型、风险、成本和模型能力画像选择 RuntimeProfile。

## 11.2 ModelProfile

用途：

定义 Worker 本次执行使用的模型供应商、模型名、模型层级和用途。

```json
{
  "schema_version": "0.1.0",
  "model_profile_id": "model-profile-0001",
  "purpose": "coding",
  "provider": "openai_compatible",
  "model_name": "example-coder-model",
  "model_tier": "medium",
  "fallback_profile_ids": ["model-profile-cheap-0001"],
  "limits": {
    "max_input_tokens": 24000,
    "max_output_tokens": 4000
  }
}
```

## 11.3 ToolPermissionProfile

用途：

定义 Worker 本次执行可以使用哪些工具，以及读写范围。

```json
{
  "schema_version": "0.1.0",
  "tool_permission_profile_id": "tool-profile-0001",
  "allowed_tools": ["read_file", "search_text", "apply_patch", "run_tests"],
  "read_scope": ["src/", "tests/", "docs/"],
  "write_scope": ["src/asteria_runtime/", "tests/"],
  "allow_network": false,
  "allow_shell": true,
  "allow_destructive_shell": false
}
```

设计说明：

- `ToolPermissionProfile` 是运行时硬外壳的一部分，用来约束风险、所有权和可并发调度边界。
- 它不应被滥用成业务流程脚本。profile 应回答“Worker 可以在哪里读写、能用哪些工具、哪些风险被禁止”，而不是回答“模型必须按哪些固定步骤完成任务”。
- 如果模型判断当前权限或 scope 不足，应通过结构化请求让 Coordinator 扩展 scope、补充 context 或创建 DecisionPoint，不能静默越界。
- 随着模型能力提升，profile 的目标应保持稳定：保护系统和用户，而不是替代模型推理。

## 11.4 AccountProfile

用途：

定义 Worker 可使用的账号或凭据引用。该对象只能保存引用，不能保存 secret 明文。

```json
{
  "schema_version": "0.1.0",
  "account_profile_id": "account-profile-0001",
  "provider": "openai_compatible",
  "credential_ref": "AGENT_MODEL_API_KEY",
  "permission_tier": "standard",
  "rate_limit_hint": {
    "requests_per_minute": 60
  }
}
```

## 11.5 SandboxProfile

用途：

定义 Worker 的执行隔离策略。

```json
{
  "schema_version": "0.1.0",
  "sandbox_profile_id": "sandbox-profile-0001",
  "backend": "single_workspace",
  "workspace_policy": "controlled_patch",
  "cleanup_policy": "keep_evidence",
  "export_required": ["artifact_index", "diff", "failure_evidence"]
}
```

枚举：

```text
sandbox.backend:
  single_workspace
  temp_workspace
  git_worktree
  container
  remote_worker
```

## 11.6 PromptEnvelope 与 CapabilityManifest

文件：

```text
.asteria/runs/<run_id>/prompt_envelope.json
.asteria/runs/<run_id>/prompt_envelope_execute.json
.asteria/runs/<run_id>/prompt_envelope_debug.json
.asteria/runs/<run_id>/prompt_envelope_review.json
```

用途：

把模型工作循环看到的系统提示词和能力边界固化为可审计对象。`plan`、`execute`、`debug`、`review` 都必须生成同一结构的 PromptEnvelope；`plan` 保留兼容文件名 `prompt_envelope.json`，其他 mode 使用带后缀的文件名。Prompt evidence 默认只保存 section 摘要、hash、token 估算和证据引用，不保存完整 prompt body，避免 protected path 或 secret 内容被复制进运行证据。

```json
{
  "schema_version": "0.1.0",
  "run_id": "run-20260427-0001",
  "mode": "plan",
  "section_order": [
    "identity",
    "operating_contract",
    "project_guidance",
    "capability_manifest",
    "tool_policy",
    "safety_envelope",
    "failure_repair",
    "delegation_contract",
    "context_compaction",
    "user_communication"
  ],
  "sections": [
    {
      "name": "project_guidance",
      "source": "AGENTS.md",
      "priority": "project",
      "cache_scope": "workspace",
      "token_estimate": 1200,
      "content_hash": "sha256:...",
      "summary": "Project rules and boundaries...",
      "evidence_refs": ["AGENTS.md"],
      "cache_break_reasons": ["workspace_guidance_changed"]
    }
  ],
  "capability_manifest": {
    "modes": ["plan", "build", "review", "repair", "release"],
    "direct_tools": [],
    "deferred_tools": [],
    "mcp_tools": [],
    "skills": [],
    "subagents": [],
    "verification": [],
    "boundaries": {
      "protected_paths": [".env", "secrets/"],
      "shell": "deny",
      "destructive_shell": "deny",
      "budget": "runtime_enforced"
    }
  },
  "content_hash": "sha256:..."
}
```

CapabilityManifest 面向模型展示能力和边界，面向 Runtime 保留强制执行字段。每个能力项至少包含：

```text
name
kind
permission_state
sandbox_profile
read_scope
write_scope
cost_tier
observation_schema
```

分层：

```text
direct_tools       当前模型循环可直接计划使用的读、搜、改、执行类工具
deferred_tools     需要先发现或装载的延迟能力
mcp_tools          经过 runtime policy、预算、schema、日志门禁的 MCP adapter
skills             领域工作流、模板和参考资料
subagents          有 delegation brief、scope、权限和验证预期的子 worker
verification       run_tests、review_agent、merge_gate 等独立验证能力
```

## 11.7 ContextMount

用途：

定义 Worker 启动时应该加载哪些上下文切片，而不是默认加载完整历史。

```json
{
  "schema_version": "0.1.0",
  "context_mount_id": "context-mount-0001",
  "run_id": "run-20260427-0001",
  "task_id": "task-0001",
  "mount_type": "debug_context",
  "includes": {
    "root_guidance": true,
    "goal_brief": true,
    "task_brief": true,
    "artifact_refs": ["artifact-0001"],
    "failure_evidence_refs": ["failure-0001"],
    "decision_refs": ["decision-0001"],
    "validation_refs": ["validation-0001"],
    "recent_event_count": 20
  },
  "summary": "Debug context for task-0001 with failure evidence and relevant artifact references."
}
```

枚举：

```text
context_mount.mount_type:
  planning_context
  coding_context
  review_context
  debug_context
  summary_context
```

## 11.8 WorkerInvocation

文件：

```text
.asteria/runs/<run_id>/workers.jsonl
```

用途：

记录 Coordinator 启动一次 Worker 的完整挂载信息。

```json
{
  "schema_version": "0.1.0",
  "worker_invocation_id": "worker-0001",
  "run_id": "run-20260427-0001",
  "task_id": "task-0001",
  "agent_id": "agent-0001",
  "runtime_profile_id": "runtime-profile-0001",
  "status": "running",
  "started_at": "2026-04-27T14:30:00+08:00",
  "ended_at": null,
  "summary": "Run CoderAgent for task-0001 with controlled patch workspace.",
  "delegation_brief": {
    "goal": "实现 task-0001 的可验证改动",
    "why": "该任务来自当前 TaskGraph 的 ready 节点",
    "known_context": {
      "task_id": "task-0001",
      "acceptance": ["目标行为可验证"],
      "read_scope": ["src/", "tests/"]
    },
    "constraints": {
      "allowed_tools": ["read_file", "apply_patch", "run_tests"],
      "parallel_safety": "serial",
      "merge_strategy": "candidate_workspace"
    },
    "allowed_writes": ["src/", "tests/"],
    "expected_output": {
      "artifact_refs": ["src/example.py"],
      "validation_commands": ["pytest"]
    },
    "verification_expectation": {
      "verification_policy": "independent",
      "completion_contract": {"requires_tests": true}
    }
  },
  "brief_quality": {
    "status": "pass",
    "missing_fields": []
  }
}
```

`delegation_brief` 是 Coordinator 给子 worker 的最小任务合同。`brief_quality.status` 为 `warn` 时表示 brief 缺少目标、背景、已知上下文、约束、写入范围、期望输出或验证预期中的关键字段。执行前的 DelegationContract gate 会按风险处理该状态：低风险只记录，高风险或写入型任务会直接拒绝 worker 并记录 `denied`；如果 Planner 已明确要求模型创建 runtime scope request，则允许 warn-only 通过，让受控申请流程继续发生。Release gate 必须检查高风险低质量委派被阻断、scope request 例外有明确证据，并确认 `workers.jsonl`、`worker_results.jsonl`、`task_execution_evidence.jsonl` 对同一 worker/task 的状态口径一致。

枚举：

```text
worker_invocation.status:
  queued
  running
  succeeded
  failed
  denied
  timeout
  cancelled
```

## 11.9 WorkerResult

文件：

```text
.asteria/runs/<run_id>/worker_results.jsonl
```

用途：

记录 Worker 完成后的产物、验证、成本和失败证据。

```json
{
  "schema_version": "0.1.0",
  "worker_result_id": "worker-result-0001",
  "worker_invocation_id": "worker-0001",
  "run_id": "run-20260427-0001",
  "task_id": "task-0001",
  "status": "succeeded",
  "artifact_refs": ["artifact-0001"],
  "validation_refs": ["validation-0001"],
  "failure_evidence_refs": [],
  "cost": {
    "model_calls": 1,
    "tool_calls": 8
  },
  "summary": "Implemented task-0001 and passed targeted validation."
}
```

枚举：

```text
worker_result.status:
  succeeded
  partial
  failed
  denied
  timeout
```

## 11.9.1 AgentRunGraph

文件：

```text
.asteria/runs/<run_id>/agent_run_graph.json
```

用途：

记录一次 run 中的多 worker / 子智能体协作图。它不是聊天记录，而是控制面可消费的运行拓扑：哪些 worker 被调度、各自拿到什么上下文和预算、采用什么协作角色、产生哪些 artifact / validation / failure evidence，以及整个协作批次是否成功。

```json
{
  "schema_version": "0.1.0",
  "agent_run_graph_id": "agent-run-graph-run-20260427-0001",
  "run_id": "run-20260427-0001",
  "status": "succeeded",
  "coordination_modes": ["parallel_safe_batch_selection"],
  "max_concurrency_observed": 2,
  "child_worker_plans": [
    {
      "worker_invocation_id": "worker-0001",
      "worker_result_id": "worker-result-0001",
      "task_id": "task-0001",
      "agent_id": "agent-coder",
      "runtime_profile_id": "runtime-profile-0001",
      "context_mount_id": "context-mount-0001",
      "model_tier": "medium",
      "tool_permission_profile_id": "tool-permission-profile-0001",
      "strategy_mode": "disjoint_write_workers",
      "strategy_max_child_workers": 2,
      "coordination_mode": "parallel_safe_batch_selection",
      "collaboration_role": "implementation_child",
      "read_scope": ["src/module_a.py"],
      "write_scope": ["src/module_a.py"],
      "depends_on": [],
      "budget": {
        "max_model_calls": 1,
        "max_tool_calls": 12
      },
      "artifact_refs": ["artifact-0001"],
      "validation_refs": ["validation-0001"],
      "failure_evidence_refs": [],
      "cost": {
        "model_calls": 1,
        "tool_calls": 8
      },
      "status": "succeeded",
      "summary": "Implemented isolated change for task-0001."
    }
  ],
  "collaboration_summary": {
    "total_workers": 1,
    "successful_workers": 1,
    "failed_workers": 0,
    "blocked_workers": 0,
    "total_model_calls": 1,
    "total_tool_calls": 8,
    "artifact_refs": ["artifact-0001"],
    "validation_refs": ["validation-0001"],
    "failure_evidence_refs": [],
    "strategy_modes": ["disjoint_write_workers"],
    "merge_strategy": "merge_gate_promotion",
    "next_actions": []
  }
}
```

枚举：

```text
agent_run_graph.status:
  pending
  running
  succeeded
  partial
  failed
  blocked

child_worker_plan.strategy_mode:
  serial_worker
  readonly_fanout
  disjoint_write_workers
  plan_then_serial

child_worker_plan.collaboration_role:
  serial_worker
  research_child
  implementation_child
  planning_child
```

控制面约定：

- `agent_run_graph.json` 必须通过 schema 校验，schema 位于 `schemas/agent_run_graph.schema.json` 和包内 `src/asteria_runtime/schemas/agent_run_graph.schema.json`。
- `status`、`sessions --context`、final report、capability report 和 weekly report 应读取该对象，而不是分别推导多 worker 结论。
- 如果并行 worker 在底层 artifact recorder 中产生重复 artifact ref，汇总层可以去重；worker 数量、validation refs 和 failure evidence refs 仍必须完整保留。

## 11.10 ValidationResult

文件：

```text
.asteria/runs/<run_id>/validation_results.jsonl
```

用途：

记录每条验证命令或验证工具调用的结构化结果，并通过 `WorkerResult.validation_refs`
和 `ContextMount.includes.validation_refs` 关联回 worker 执行与上下文恢复。

```json
{
  "schema_version": "0.1.0",
  "validation_result_id": "validation-0001",
  "run_id": "run-20260427-0001",
  "task_id": "task-0001",
  "tool_name": "run_command",
  "command": "pytest tests/test_scoring.py",
  "status": "passed",
  "summary": "12 tests passed",
  "error": null,
  "data": {
    "exit_code": 0
  },
  "created_at": "2026-04-27T14:30:00+08:00"
}
```

设计说明：

- `ValidationResult` 是验证门禁的事实记录，不是自然语言备注。
- `WorkerResult.validation_refs` 应只引用本次 worker 产生或确认的验证证据。
- 后续 debug/review/summary worker 不应默认读取完整历史，而应通过 `ContextMount` 选择相关验证切片。
- 验证失败时也必须记录，便于失败归因、修复任务生成和能力画像统计。

枚举：

```text
validation_result.status:
  passed
  failed
```

## 12. DecisionPoint

文件：

```text
.asteria/runs/<run_id>/decisions.jsonl
```

用途：

记录需要用户操盘的重大分支。

```json
{
  "schema_version": "0.1.0",
  "decision_id": "decision-0001",
  "status": "pending",
  "question": "密码测试工具是否应该包含在线泄露查询？",
  "recommended_option_id": "local_optional_import",
  "options": [
    {
      "option_id": "no_breach_check",
      "label": "不做泄露检查",
      "tradeoff": "完全本地且简单，但现实风险提示较弱",
      "action": "cancel_scope"
    },
    {
      "option_id": "local_optional_import",
      "label": "本地可选导入",
      "tradeoff": "隐私安全，但需要用户准备本地列表",
      "action": "create_task"
    },
    {
      "option_id": "online_api",
      "label": "在线 API",
      "tradeoff": "方便，但有隐私和网络依赖问题",
      "action": "create_task"
    }
  ],
  "default_option_id": "local_optional_import",
  "impact": {
    "scope": "medium",
    "budget": "medium",
    "risk": "high",
    "quality": "medium"
  },
  "selected_option_id": null,
  "created_at": "2026-04-27T14:30:00+08:00",
  "resolved_at": null
}
```

枚举：

```text
decision.status:
  pending
  resolved
  defaulted
  cancelled

impact value:
  low
  medium
  high

decision.options[].action:
  create_task
  record_constraint
  cancel_scope
  require_replan
```

## 13. ContextSnapshot

文件：

```text
.asteria/context/root_snapshot.json
.asteria/context/snapshots/<timestamp>.json
```

用途：

压缩上下文，支持长任务续航。

```json
{
  "schema_version": "0.1.0",
  "snapshot_id": "snapshot-0001",
  "run_id": "run-20260427-0001",
  "created_at": "2026-04-27T14:30:00+08:00",
  "focus": "handoff for ReviewerAgent",
  "goal_summary": "构建本地优先密码测试工具",
  "definition_of_done": ["本地运行", "强度评分", "隐私说明", "测试通过"],
  "accepted_decisions": ["默认不使用在线泄露 API"],
  "active_tasks": ["task-0001", "task-0003"],
  "modified_files": [
    {
      "path": "src/scoring.ts",
      "reason": "实现强度评分"
    }
  ],
  "verification": [
    {
      "command": "npm test",
      "status": "passed",
      "summary": "12 tests passed"
    }
  ],
  "failures": [],
  "research_claims": [
    "密码强度评分不能等同于是否已经泄露"
  ],
  "open_risks": ["缺少 UI screenshot 检查"],
  "next_actions": ["运行 smoke test", "补 README"]
}
```

## 14. CommandRun

用途：

记录一次命令执行。

```json
{
  "schema_version": "0.1.0",
  "command_run_id": "cmd-0001",
  "run_id": "run-20260427-0001",
  "command": "plan",
  "args": {
    "goal": "做一个密码测试工具"
  },
  "status": "completed",
  "started_at": "2026-04-27T14:30:00+08:00",
  "ended_at": "2026-04-27T14:31:00+08:00",
  "artifacts": ["goal_spec.json", "task_plan.json"],
  "cost": {
    "model_calls": 3,
    "tool_calls": 8
  },
  "summary": "生成目标规格和 7 个任务"
}
```

枚举：

```text
command:
  init
  plan
  brainstorm
  research
  compact
  decide
  review
  debug
  handoff
  run

command.status:
  queued
  running
  completed
  failed
  blocked
  cancelled
```

## 15. ToolCall

文件：

```text
.asteria/runs/<run_id>/tool_calls.jsonl
```

用途：

记录工具调用，支持审计和轨迹评估。

```json
{
  "schema_version": "0.1.0",
  "tool_call_id": "toolcall-0001",
  "run_id": "run-20260427-0001",
  "task_id": "task-0001",
  "agent_id": "agent-0001",
  "tool_name": "run_tests",
  "input_summary": "npm test",
  "output_summary": "12 tests passed",
  "status": "success",
  "started_at": "2026-04-27T14:30:00+08:00",
  "ended_at": "2026-04-27T14:30:10+08:00",
  "error": null
}
```

枚举：

```text
tool_call.status:
  success
  failure
  denied
  timeout
```

## 15.1 ToolObservation

文件：

```text
.asteria/runs/<run_id>/tool_observations.jsonl
```

用途：

记录工具结果进入模型工作循环前的 raw evidence。它与 `user_progress.jsonl` 中的 `tool_observation` 事件、以及 `AgentHarness` 使用的模型 observation 保持同一来源，避免报告、人类进度和模型下一轮判断各自拼一套事实。

```json
{
  "schema_version": "0.1.0",
  "observation_id": "tool-observation-toolcall-0001",
  "run_id": "run-20260427-0001",
  "task_id": "task-0001",
  "tool_call_id": "toolcall-0001",
  "tool_name": "run_tests",
  "ok": true,
  "status": "completed",
  "summary": "12 tests passed",
  "error_class": null,
  "artifact_refs": [],
  "evidence_refs": [".asteria/runs/run-20260427-0001/tool_calls.jsonl"],
  "user_progress_event_id": "progress-0004",
  "next_hint": "continue",
  "observation": {
    "tool_name": "run_tests",
    "ok": true,
    "summary": "12 tests passed"
  },
  "created_at": "2026-04-27T14:30:10+08:00"
}
```

约束：

- `observation` 是模型可消费摘要，不能复制大段 stdout、diff 或 secret。
- `evidence_refs` 指向原始证据文件，便于 debug/review/report 复查。
- `user_progress_event_id` 关联人类可见进度，保证 raw evidence、model observation、user progress 三线一致。
- 失败 observation 的 `next_hint` 默认进入 diagnose / repair / replan / ask / stop 决策循环；进入 DebugAgent / ReviewAgent 前，Runtime 会把它转换成显式 `tool_observation_actions`，包括每个动作的适用条件和模型指令，避免真实模型只看到失败摘要却不知道可选下一步。

## 16. ModelCall

文件：

```text
.asteria/runs/<run_id>/model_calls.jsonl
```

用途：

记录模型调用和成本。

```json
{
  "schema_version": "0.1.0",
  "model_call_id": "modelcall-0001",
  "run_id": "run-20260427-0001",
  "agent_id": "agent-0001",
  "runtime_profile_id": "runtime-profile-0001",
  "model_profile_id": "model-profile-0001",
  "purpose": "planning",
  "model_provider": "zhipu",
  "model_name": "glm-example",
  "model_tier": "strong",
  "prompt_envelope_hash": "sha256:...",
  "prompt_envelope_path": ".asteria/runs/run-20260427-0001/prompt_envelope_execute.json",
  "context_envelope_hash": "sha256:...",
  "context_envelope_path": ".asteria/runs/run-20260427-0001/context_envelopes/context_envelope_task-0001.json",
  "capability_manifest_hash": "sha256:...",
  "input_tokens": 5000,
  "output_tokens": 1200,
  "status": "success",
  "created_at": "2026-04-27T14:30:00+08:00",
  "summary": "生成任务计划"
}
```

枚举：

```text
model_call.purpose:
  goal_spec
  planning
  brainstorming
  research
  coding
  review
  debugging
  summarization
  evaluation
```

`runtime_profile_id` 和 `model_profile_id` 用于把一次模型调用关联回 Worker 启动时挂载的运行环境。`prompt_envelope_hash`、`prompt_envelope_path` 和 `capability_manifest_hash` 用于把一次模型 action 追溯回当时的 PromptEnvelope 与 CapabilityManifest；`context_envelope_hash` 和 `context_envelope_path` 用于追溯当次模型调用实际消费的 ContextEnvelope。当 manifest 变化时，PromptEnvelope 的 cache break reason 和模型调用 metadata 应共同提供可审计链路。`capability_report`、`weekly_report` 和 `acceptance_gate` 会汇总 `capability_manifest_audit`，显示 prompt envelope / context envelope 数量、关联到 manifest hash / context envelope hash 的模型调用数、manifest 是否变化以及 cache break reasons。并发、多模型和多账号调度开启后，成本分析、能力画像和失败归因必须优先按 profile/purpose/envelope 维度聚合，而不是只按模型名聚合。

## 17. Artifact

文件：

```text
.asteria/runs/<run_id>/artifacts.jsonl
```

用途：

记录运行产物。

```json
{
  "schema_version": "0.1.0",
  "artifact_id": "artifact-0001",
  "run_id": "run-20260427-0001",
  "task_id": "task-0001",
  "type": "source_file",
  "path": "src/scoring.ts",
  "absolute_path": "H:/mult_agent_code/src/scoring.ts",
  "workspace_id": "workspace-8d3a...",
  "output_root": "H:/mult_agent_code",
  "artifact_root": "H:/mult_agent_code/.asteria/artifacts",
  "created_by": "agent-0001",
  "summary": "实现密码评分逻辑",
  "created_at": "2026-04-27T14:30:00+08:00"
}
```

枚举：

```text
artifact.type:
  source_file
  test_file
  report
  screenshot
  patch
  research_note
  eval_result
  context_snapshot
  memory_entry
```

## 18. Experiment

文件：

```text
.asteria/runs/<run_id>/experiments.jsonl
```

用途：

记录 keep/discard 尝试。

```json
{
  "schema_version": "0.1.0",
  "experiment_id": "exp-0001",
  "run_id": "run-20260427-0001",
  "task_id": "task-0001",
  "idea": "增加常见弱密码检测",
  "baseline": {
    "tests_passed": 10,
    "requirement_coverage": 0.55
  },
  "candidate": {
    "changed_files": ["src/scoring.ts", "tests/scoring.test.ts"]
  },
  "evaluator": {
    "commands": ["npm test"],
    "metrics": ["tests_passed", "requirement_coverage"]
  },
  "metrics_after": {
    "tests_passed": 12,
    "requirement_coverage": 0.7
  },
  "decision": "keep",
  "reason": "覆盖率提升且测试通过"
}
```

枚举：

```text
experiment.decision:
  keep
  discard
  retry
  blocked
```

## 19. EvalReport

文件：

```text
.asteria/runs/<run_id>/eval_report.json
```

用途：

记录验收评估。

```json
{
  "schema_version": "0.1.0",
  "run_id": "run-20260427-0001",
  "goal_eval": {
    "goal_clarity_score": 0.85,
    "requirement_expansion_score": 0.8,
    "definition_of_done_score": 0.82
  },
  "artifact_eval": {
    "required_artifacts_present": true
  },
  "outcome_eval": {
    "requirement_coverage": 0.75,
    "verification_pass_rate": 0.9,
    "documentation_score": 0.7
  },
  "trajectory_eval": {
    "scope_drift_score": 0.1,
    "cost_efficiency_score": 0.85
  },
  "cost_eval": {
    "within_budget": true
  },
  "overall": {
    "status": "pass",
    "score": 0.82,
    "reason": "核心需求覆盖，成本在预算内"
  }
}
```

枚举：

```text
overall.status:
  pass
  partial
  fail
```

## 20. AcceptanceReport

文件：

```text
.asteria/acceptance/acceptance_report.json
```

用途：

记录 runtime 级验收结果、场景明细、聚合指标、趋势 delta、趋势告警，以及失败场景转任务后的闭环结果。

关键字段：

```json
{
  "schema_version": "0.1.0",
  "suite": "core",
  "requested_scenarios": ["password_cli"],
  "root": ".",
  "ok": true,
  "returncode": 0,
  "summary_json": ".asteria/acceptance/latest_summary.json",
  "aggregate": {
    "total": 1,
    "passed": 1,
    "failed": 0,
    "model_calls": 3,
    "tool_calls": 2,
    "duration_seconds": 118.4
  },
  "trend": {
    "previous": null,
    "deltas": {}
  },
  "trend_warnings": [],
  "scenarios": [
    {
      "scenario": "password_cli",
      "ok": true,
      "workspace": ".asteria/acceptance/workspaces/password_cli",
      "failure_summary": ""
    }
  ]
}
```

补充文件：

- `.asteria/acceptance/latest_summary.json`：底层验收脚本的机器摘要。
- `.asteria/acceptance/history.jsonl`：每次验收追加一行，用于趋势比较。
- `.asteria/acceptance/failures/*.json`：失败场景被 promote 成修复任务时写入的证据。

当 `/acceptance` 使用 `--fail-on-trend-warning` 时，`trend_warnings` 非空会让命令以非零状态退出，但原始 `ok` 字段仍表示场景本身是否通过。

## 21. CostReport

文件：

```text
.asteria/runs/<run_id>/cost_report.json
```

用途：

记录成本和预算状态。

```json
{
  "schema_version": "0.1.0",
  "run_id": "run-20260427-0001",
  "model_calls": 34,
  "tool_calls": 82,
  "estimated_input_tokens": 120000,
  "estimated_output_tokens": 18000,
  "latest_context_estimated_tokens": 64000,
  "max_context_estimated_tokens": 88000,
  "context_window_tokens": 200000,
  "context_window_ratio": 0.32,
  "context_pressure_status": "within_budget",
  "strong_model_calls": 8,
  "cheap_model_calls": 12,
  "repair_attempts": 2,
  "context_compactions": 1,
  "user_decisions": 2,
  "status": "within_budget",
  "warnings": []
}
```

枚举：

```text
cost.status:
  within_budget
  near_limit
  hard_stop
  exceeded
  stopped
```

## 22. MemoryEntry

文件：

```text
.asteria/memory/*.jsonl
```

用途：

记录可复用记忆。

```json
{
  "schema_version": "0.1.0",
  "memory_id": "memory-0001",
  "type": "project_decision",
  "content": "用户偏好本地优先，不默认调用在线泄露 API",
  "source": {
    "run_id": "run-20260427-0001",
    "decision_id": "decision-0001"
  },
  "tags": ["privacy", "local_first"],
  "confidence": 0.9,
  "created_at": "2026-04-27T14:30:00+08:00"
}
```

枚举：

```text
memory.type:
  user_preference
  project_decision
  architecture_note
  research_claim
  experiment_lesson
  tool_knowledge
  failure_lesson
```

## 23. Event

文件：

```text
.asteria/runs/<run_id>/events.jsonl
```

用途：

记录系统事件。

```json
{
  "schema_version": "0.1.0",
  "event_id": "event-0001",
  "run_id": "run-20260427-0001",
  "timestamp": "2026-04-27T14:30:00+08:00",
  "type": "phase_changed",
  "actor": "orchestrator",
  "summary": "PLAN -> IMPLEMENT",
  "data": {
    "from": "PLAN",
    "to": "IMPLEMENT"
  }
}
```

枚举：

```text
event.type:
  run_started
  run_completed
  phase_changed
  task_created
  task_updated
  tool_called
  model_called
  decision_created
  decision_resolved
  artifact_created
  verification_run
  repair_attempted
  context_compacted
  budget_warning
  policy_denied
  error
```

## 24. RuntimeHookEvent

文件：

```text
.asteria/runs/<run_id>/runtime_hooks.jsonl
```

用途：

记录运行时生命周期 hook。它是 worker、tool、后续 agent/plugin 协作的统一观测和扩展边界，不替代 `events.jsonl`、`tool_calls.jsonl` 或 `model_calls.jsonl`；它回答“哪个运行时边界在什么时候被触发、由谁触发、对应哪个 task/worker/tool”。

```json
{
  "schema_version": "0.1.0",
  "hook_event_id": "hook-0001",
  "run_id": "run-20260427-0001",
  "hook_name": "before_tool_call",
  "phase": "tool",
  "actor": "ExecuteCommand",
  "task_id": "task-0001",
  "worker_invocation_id": null,
  "tool_name": "read_file",
  "summary": "Starting tool read_file",
  "data": {
    "arg_keys": ["path"]
  },
  "created_at": "2026-04-27T14:30:00+08:00"
}
```

当前内置 hook：

```text
before_worker
after_worker
worker_error
before_tool_call
after_tool_call
tool_call_error
```

约束：

- hook 必须通过 `RuntimeHookManager` 写入，不能由命令层临时拼 JSONL。
- hook handler 默认为观测型，失败只写入 `runtime_hook_handler_failed`，不应破坏主执行。
- hook 数据只记录摘要、id、状态和参数 key，不能写入 secret 或大段原始上下文。
- hook 写入前必须应用 policy 中的 `redacted_data_keys`，敏感 key 的值统一替换为 `<redacted>`。
- 插件体系后续只能在该生命周期层上扩展，并继续受 runtime policy、预算、schema 和 protected paths 约束。

## 25. PluginManifest

文件：

```text
.asteria/plugins/<plugin-id>.plugin.json
```

用途：

声明插件元数据、订阅的 hook、权限需求和入口点。当前阶段只校验和加载声明，不执行外部插件代码。这样插件体系可以先形成可审计控制面，再逐步开放 handler 执行。

```json
{
  "schema_version": "0.1.0",
  "plugin_id": "example.audit",
  "name": "Example Audit Plugin",
  "version": "0.1.0",
  "enabled": false,
  "entrypoint": "plugins/example_audit.py",
  "hook_subscriptions": ["after_tool_call", "worker_error"],
  "permissions": {
    "network": false,
    "shell": false,
    "write_workspace": false,
    "read_secrets": false
  },
  "capabilities": ["audit-log"],
  "description": "Records additional audit metadata for selected hooks."
}
```

约束：

- `plugin_id` 必须稳定，后续日志和报告以它作为审计主键。
- `hook_subscriptions` 只能包含 HookPolicy 允许的 hook name。
- `enabled=false` 或 `hooks.plugins_enabled=false` 时，manifest 可以被读取，但不得注册 executable handler。
- `entrypoint` 是声明字段，不代表当前阶段会 import 或执行该文件。
- 插件权限必须显式声明；默认没有 network、shell、workspace write、secret read。


## 25.5 ErrorTaxonomy

用途：

统一 runtime 阻塞分类。`doctor`、`package-check`、`gate-status` 和 `capability-report` 的每个 check 都会映射到 `error_type`，用户不再只看到 traceback 或模糊 blocked，而是看到结构化的错误分类、恢复策略和下一步操作。

当前分类（7 类）：

```text
candidate_workspace  候选工作区创建、隔离、promotion 失败
configuration        配置缺失、schema 不一致、版本不同步
internal             内部运行时错误、未分类异常
plugin               插件 manifest blocked、hook 订阅非法、schema missing
promotion            candidate promotion queue 阻塞、审批未决
provider             模型路由不可用、gate 报告缺失、provider 响应异常
schema               持久化对象 schema 校验失败、版本不兼容
```

每个分类包含：

```text
error_type       分类标识
user_summary     用户可读的一句话解释
auto_recovery    系统可能自动恢复的策略
next_action      建议用户执行的命令
release_gate     是否默认阻断 release
```

实现位置：`src/asteria_runtime/core/error_taxonomy.py`。

## 25.6 SchemaMigrationRegistry

实现位置：`src/asteria_runtime/core/schema_migration.py`。

用途：为 JSON/JSONL 持久化对象提供版本化迁移链。每次迁移记录审计日志。

```json
{
  "schema_name": "policy_config",
  "from_version": "0.1.0",
  "to_version": "0.2.0",
  "description": "Add feature_flags and capability_flags top-level sections."
}
```

当前已注册迁移：
- `policy_config` 0.1.0 → 0.2.0
- `plugin_manifest` 0.1.0 → 0.2.0

## 25.7 FeatureFlag 与 CapabilityFlag

实现位置：`src/asteria_runtime/core/flag_resolver.py`。

**FeatureFlag**：控制功能是否启用，存储在 `policy_config.feature_flags`。

```json
{
  "name": "debug_export",
  "enabled": true,
  "requires": ["real_model"]
}
```

**CapabilityFlag**：描述环境能力是否具备，由 `FlagResolver` 自动推导。

```json
{
  "name": "strong_model",
  "available": true,
  "reason": "Strong model route is configured."
}
```

**FlagResolution**：解析结果，同时检查功能开关和环境能力。`active` 为 `true` 当且仅当 `enabled=true` 且所有 `requires` 能力均 `available=true`。

## 26. HandoffPackage

文件：

```text
.asteria/context/handoffs/<timestamp>.json
```

用途：

给另一个智能体或未来运行续接。

```json
{
  "schema_version": "0.1.0",
  "handoff_id": "handoff-0001",
  "from_agent_id": "agent-0001",
  "to_role": "ReviewerAgent",
  "snapshot_id": "snapshot-0001",
  "current_task_ids": ["task-0003"],
  "recent_artifacts": ["artifact-0003", "artifact-0004"],
  "known_risks": ["缺少移动端检查"],
  "recommended_next_command": "review",
  "created_at": "2026-04-27T14:30:00+08:00"
}
```

## 27. 状态转移约束

### 27.1 Task 状态转移

```text
backlog -> ready
ready -> in_progress
in_progress -> testing
testing -> reviewing
reviewing -> done
in_progress -> blocked
testing -> blocked
reviewing -> blocked
blocked -> ready
in_progress -> discarded
```

禁止：

- `done -> in_progress`，除非创建新任务。
- `discarded -> done`，除非恢复为新任务。

### 27.2 Run 状态转移

```text
queued -> running
running -> paused
paused -> running
running -> blocked
blocked -> running
running -> completed
running -> failed
running -> cancelled
```

## 27.3 ValidationRun

文件：

```text
.asteria/validation_runs/<validation_run_id>/summary.json
```

用途：

记录真实模型小任务验证的完整控制面证据。`ValidationRun` 不是普通运行日志，而是从 gate 到实战验证之间的发布证据对象，用来回答“当前配置是否可以扩大真实任务验证”。

核心字段：

- `validation_run_id`：本次验证运行编号。
- `status`：`blocked`、`dry_run`、`completed` 或 `failed`。
- `preflight`：`doctor` 和 `gate-status` 的机器可读结果。
- `budgets`：验证任务使用的迭代数、每轮任务数、research/并发策略。
- `route_expectations`：本次验证对 strong/medium route 的期望、允许降级策略和 fast-path 例外。
- `run_result`：底层 `RunCommand` 返回的运行结果。
- `evidence`：聚合 `model_calls`、`worker_results`、`task_execution_evidence`、`merge_gate_evidence` 和 `cost_report` 后得到的验证证据。
- `next_actions`：扩大验证、修复路由或检查阻塞点的建议。

设计约束：

- 只有 `gate-status.stage == ready_for_small_real_task_validation` 才能正式启动小任务验证。
- dry-run 只写计划和 preflight 证据，不调用模型。
- 发布/validation suite 必须证明 strong 和 medium route 都健康并形成实际调用证据；低风险 scoped fast-path case 可以 0 strong，但必须记录 route 选择、`context_mode`、`fast_path_task_kind`、deterministic review 命中情况和产物验证证据。
- 后续 TUI / GUI / 验证自动化应优先读取 `ValidationRun`，不要散读多个运行产物自行拼状态。

## 27.4 AgentRunGraph collaboration protocol

`agent_run_graph.json` 的 `collaboration_summary.collaboration_protocol` 用来把多 worker 协作边界落成可审计协议：

- `isolation_model`：写入 worker 使用独立 candidate workspace，防止 child worker 产物互相污染。
- `review_agent_role`：ReviewAgent 汇总 child diff、冲突和 release risk。
- `debug_agent_role`：DebugAgent 基于失败 evidence 重试或替换失败 child worker。
- `merge_gate_role`：merge gate 在 promotion 前阻断 scope 冲突和验证失败。
- `promotion_queue_role`：promotion queue 统一承接人工审批、重试、拒绝或丢弃。

## 28. 历史 MVP 基线与当前 Runtime OS 对象

本节最初用于约束早期 MVP 的最小对象集合。当前阶段已经进入编排式 Asteria Runtime OS，不能再把下列列表理解为“其余对象以后再说”。它只保留为历史基线，用于说明哪些对象曾经是最小闭环。

历史 MVP 基线包含：

1. `ProjectConfig`
2. `PolicyConfig`
3. `Run`
4. `GoalSpec`
5. `Task`
6. `DecisionPoint`
7. `ContextSnapshot`
8. `ToolCall`
9. `ModelCall`
10. `Artifact`
11. `EvalReport`
12. `CostReport`
13. `Event`
14. `RuntimeHookEvent`
15. `PluginManifest`

当前 Runtime OS 主线还必须维护以下对象和协议，它们已经不再是可推迟的 V1 附属项：

1. `TaskGraph`
2. `RuntimeProfile`
3. `ContextMount`
4. `ContextPackage`
5. `PromptEnvelope`
6. `CapabilityManifest`
7. `AgentRoleContract`
8. `WorkerInvocation`
9. `WorkerResult`
10. `ToolObservation`
11. `TaskExecutionEvidence`
12. `MergeGate`
13. `AgentRunGraph`
14. `ValidationRun`
15. `ErrorTaxonomy`
16. `SchemaMigrationRegistry`

后续优先级以 [当前状态与路线.md](./当前状态与路线.md) 为准；本文负责定义对象语义和 schema 边界，不单独决定研发顺序。

## 29. Schema 校验策略

实现时应提供：

- JSON Schema 文件。
- 读入时校验。
- 写出前校验。
- 版本迁移函数。
- 默认值填充。
- 清晰错误消息。

推荐目录：

```text
schemas/
  current_session.schema.json
  project_config.schema.json
  policy_config.schema.json
  run.schema.json
  goal_spec.schema.json
  task.schema.json
  decision_point.schema.json
  context_snapshot.schema.json
  tool_call.schema.json
  model_call.schema.json
  artifact.schema.json
  eval_report.schema.json
  cost_report.schema.json
  event.schema.json
```
