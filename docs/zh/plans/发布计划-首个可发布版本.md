# 发布计划 · 首个可发布版本（v0.2）

**状态**：ACTIVE — 当前唯一发布主线（2026-06-28 起）
**目标版本**：v0.2-alpha「编程 harness + Studio 会话 MVP」
**总计划**：[`研发总计划.md`](../研发总计划.md) · [`当前状态与路线.md`](../当前状态与路线.md) §4

> 本文是把工程推到"装得上、能跑、拿得到验证结果"的**唯一可执行清单**。
> 收敛背景与已删机制见 [`已删除与已替代登记.md`](../已删除与已替代登记.md)。
> 一句话方向：**核心循环已是真资产，停止往上盖编排/功能，先把"真实 provider 跑通一个改动"钉死并能发布。**

## 1. 发布判据（Definition of Done，可测）

| # | 判据 | 验证方式 | 现状 |
| --- | --- | --- | --- |
| R1 | 真实 provider 跑 `small_code_change` score ≥ 0.8，**连续 3 次稳定**；streaming/fallback 不再是失败主因 | `studio_user_tasks.json` + real provider，重复 3 次 | ⚠️ 历史 3 次真实任务 1 过 2 败，主因 provider streaming/fallback |
| R2 | 默认 `--help` 只暴露分组后的用户命令（Start/Support），维护者命令隐藏 | `asteria --help` + `test_top_level_help_groups_command_surface` | ✅ **已完成**：help 已分 Start(7)/Support(6)，维护者命令不在 help；"44 命令"只是实现文件数 |
| R3 | wheel 安装后一条 `asteria goal "..."` 端到端真实 provider 跑通并产出文件 | wheel smoke + real_model_smoke | 🟡 smoke 存在，需绑真 provider 复签 |
| R4 | 一页 README/快速开始；`运行命令.md` 与收口后命令一致 | 人工核 + doc 契约 | 🟡 命令文档需随 R2 更新 |
| R5 | 主路径回归全绿 + 一份可复现发布签字（脱敏矩阵/命令结果） | 见 §4 green checks | 🟡 单元绿，缺发布签字 |

**冻结项（发布前一律不做）**：parallel_writes 默认开启、新编排 Wave、12 Agent 新类、无真实 friction 证据的 Studio 新功能、为单个失败任务加 keyword/recovery 尾巴。

## 2. 里程碑（严格顺序）

### M1 · 止血收口（消除噪声 + 用户面收窄）
- [x] 文档归一：建删除/替代登记、6 份非 S74 plan 标"非当前工作"、修过期 ACTIVE、删 50 份历史报告快照
- [x] `.asteria` 清理：1.1G → 76M（删 stale run worktree 副本）
- [x] **CLI surface 已收口**（核实）：`asteria --help` 已是 Start/Support 分组、维护者命令隐藏；无需再改
- [x] 修预存在契约漂移：`test_control_surface_commands.py` 的 **4 个 `test_gate_status_*`**。**结论=测试漂移，非命令逻辑漂移**：validation 套件在 9646d22（2026-06-09，CL-010 subagent loop）新增第 8 个场景 `validation_subagent_delegation`（`real_model_acceptance.py` SUITES.validation 现 8 项），但这 4 个 fixture 仍是 5/28 写的 7 场景形态；`gate_status_command.py:_has_complete_acceptance_suite` 正确要求全 8 项完整证据，故旧 fixture 被判不完整→validation 视为缺失→stage 落到 `ready_for_validation_suite`。**未动 DO_NOT_TOUCH 文件**，只把 4 个 fixture 的 aggregate/scenario_metadata/断言从 7→8 对齐。全文件 60 passed。

### M2 · 钉死核心 MVP（真正的工程重点）

已定位 5 条怀疑点（2026-06-28 代码勘察），按优先级：

| # | 怀疑根因 | 锚点 | 可否离线修+测 |
| --- | --- | --- | --- |
| 1 🔴 | streaming 所有 delta 无 content 时 `"".join([])==""` 通过 `content is None` 检查，空响应被静默接受 | `models/http_transport.py:~200`、`models/openai_compatible.py:~268` | ✅ 防御性检查 + 单测 |
| 2 🔴 | ReviewAgent 3-tier(strong/medium/cheap) fallback 逐个独立 90s deadline、不共享剩余预算，最坏 ~270s 拖垮 small_code_change 总预算 | `agents/review_agent.py:31-46`、`models/request_deadline.py:15-33` | ✅ 共享 deadline/限 tier + 单测 |
| 3 🟡 | stream deadline 与 idle timeout 钳在同一值，超时分类错误影响 fallback 触发 | `http_transport.py:125`、`request_deadline.py:32` | ✅ |
| 4 🟡 | streaming SSE 只取 `delta.content`，遗漏 `delta.tool_calls`（tool_use 模式无输出） | `http_transport.py:154-159` | ✅ |
| 5 🟡 | streaming setup HTTP 5xx 两路都失败时不触发 strong→medium fallback | `openai_compatible.py:206-214`、`models/routing.py:65-72` | ⚠️ 需构造 5xx |

- [x] **#1 已修+测**：`_parse_response` 检测空/纯空白响应（无 tool_calls）→ 抛 `provider returned empty response content`，并加入 `_should_retry` 可重试集（`test_openai_compatible_client_retries_and_raises_on_empty_content`）
- [x] **#2 已修+测**：ReviewAgent 加 `review_fallback_total_seconds` 总预算（默认 120s），超时即停后续 tier 转 deterministic fallback（`test_review_agent_fallback_budget.py`）
- [ ] 按需修 #3–#5
- [ ] 验证：查历史失败 `model_calls.jsonl` 是否 `content:""` + `finish_reason:stop`；查 review fallback `attempted_tiers` 长度 vs 总耗时
- [ ] `small_code_change` 真实 provider golden：**连续 3 次 ≥0.8**，记录耗时/调用/repair 分解
- [ ] 把 provider 等待作为 SLO/可观测，不新增 Runtime 恢复控制器（遵守 ADR-0010/0014）

### M3 · 发布工程
- [ ] wheel 构建 + 安装 smoke 绑真实 provider 复签
- [ ] 一页 README/快速开始（装→`goal`→看结果→`accept`）
- [ ] 版本号 → v0.2-alpha、CHANGELOG（从 git 提炼，不堆叙述）

### M4 · 发布签字
- [ ] 跑全部 green checks（§4），出可复现脱敏证据
- [ ] 打 `v0.2-alpha` tag

## 3. 当前进度判断

- **代码质量审计（2026-06-28）**：ruff 已清零；mypy 129 错误/22 文件（类型债，中度）；结构债集中在 `execute_command` 巨石（2823 行）+ 无 BaseCommand 样板。详见会话结论。
- **冻结编排/swarm 执行机器已删除**（2026-06-28）：净删 ~9970 行（15 模块 + 23 测试 + 12 孤立 gate），保留 readonly_fanout 与 `asteria route`。unit 759 passed，零新失败。见 [`已删除与已替代登记.md`](../已删除与已替代登记.md) §3.1。
- **预存在 integration 债**：integration 套件有 **7 个预存在失败**（phase6/6b/6c/8a gate-manifest、phase7 beta、replan×2），经 stash 确认早于本次改动；待独立修复。
- **M1 完成**：文档归一✅、`.asteria` 1.1G→76M✅、CLI 用户面（核实已收口）✅、4 个 gate_status 契约漂移测试已修✅、冻结执行机器删除✅。
- **下一步 = M2**：**真实 provider streaming/fallback 可靠性**是唯一真正卡发布的工程问题（历史 2/3 真实任务败于此）。
- **关键路径**：M2 的 provider 修复 + `small_code_change` 连续 3 次 ≥0.8 是最大不确定项；M3/M4 是确定性收尾。
- **诚实修正**：早期"44 命令暴露"是按实现文件数算的；用户 `--help` 实际只见 13 个分组命令，CLI 面并未走偏。真正的债在 provider 可靠性，不在命令面。

## 4. Green checks（发布前必过）

```powershell
pytest tests/unit/test_documentation_contracts.py -q
pytest tests/integration/test_execute_command.py -q
python scripts/steady_iteration_check.py --root . --skip-b6 --skip-wheel
# 真实 provider（维护者显式启动）：
python scripts/s74_beta_matrix_evidence.py --root . --live
node studio/scripts/run-detail-smoke.mjs
```

## 5. 不在本版本范围（v0.3+）

多智能体真并行放量（disjoint write / swarm / L3 真执行）、12 Agent 独立类、外部非维护者 Beta 放量、North Star 队列产品化。这些代码已在且 opt-in 冻结，**有真实收益证据 + DecisionPoint 才启动**。
