# 当前状态与路线

更新时间：2026-05-29

> 2026-05-29 ContextBudgetMeter 第一阶段：已新增模型请求级 context 估算。每次模型调用前按实际 `ChatRequest.messages` 估算 `latest_context_estimated_tokens`，写入 `cost_report.json`，同时记录 `max_context_estimated_tokens`、`context_window_tokens`、`context_window_ratio` 和 `context_pressure_status`。`BudgetController.pressure()` 已把 `context_window` 作为独立压力维度，`/run` 的 near-limit compact 现在可以由真实 context window ratio 触发。当前仍是估算型 meter，后续要把 PromptEnvelope / ContextEnvelope / tool output 分段归因、compact boundary 和 subagent 隔离策略补齐。

> 2026-05-29 预算治理更新：默认策略改为 `autonomous_long_task`，`max_tool_calls_per_goal` 从短闭环验证口径提升为风险加权 emergency brake。`validation` profile 仍保留较严格阈值用于发布验证；`balanced` 用于普通研发；`autonomous_long_task` 用于长期自主迭代。系统稳定性不再依赖固定 raw 工具次数，而依赖目标进展、上下文压力、修复/replan 限制、权限风险、provider 健康和重复低价值循环检测。

> 2026-05-28 同步记录：`AgentLoopProfileRegistry` 已接入 Plan/Run dispatch。Plan 在生成 `task_plan.json` 时写入每个 task 的 `agent_loop_profile`，并持久化 `agent_loop_dispatch.json`；Run 在进入执行前重新加载同一 dispatch，写入 `user_progress.jsonl` evidence 事件。Tool 执行网关现在会把每次能力调用的 `allow/ask/deny`、`intent/task_kind/risk/permission_mode` 和 reason 写入 `capability_decisions.jsonl`、`tool_observations.jsonl` 与 `user_progress.jsonl`。真实任务验证清单已扩展 Research / Brainstorm / Multi-agent 三条 profile。Studio 继续保持薄消费：优先读取 runtime-native `user_progress.jsonl`、events 和 artifacts，不把完整 Studio UI 重新列为后端主线阻塞项。

> 2026-05-28 追加同步：MCP/Skill 权限审计已抽成通用 `CapabilityDecisionRecorder`，Tool 入口已改为复用该 recorder；但 Tool/MCP/Skill 仍保持分离执行模型，只共享 capability decision 证据协议。未来 MCP 入口应是 server/session/protocol adapter，Skill 入口应是按需加载的过程知识/产物能力 adapter，不复用 ToolExecutionGateway。Research / Brainstorm / Multi-agent profile 已补 `output_contract`、`validation_contract`、`failure_recovery` 和 `decision_escalation`。`validation-run` 与 `gate-status` 已新增 runtime progress metrics：profile 覆盖率、permission reason 覆盖率、runtime-native progress 覆盖率。`agent_loop_dispatch` 摘要已进入 PromptEnvelope / ContextEnvelope 审计上下文。

> 2026-05-28 工具边界纠偏：当前 Asteria 的 `ToolExecutionGateway` 和 `src/asteria_runtime/tools/` 是内部 runtime execution registry，不等同于 Claude Code 等主流 agent 产品面向模型暴露的完整 tool surface。已在 CapabilityManifest 中加入 `tool_surface_contract`，显式标注“内部 runtime tools 已实现、模型侧标准工具面仍不完整”。后续应补专用 read/list/search/edit/shell/todo 等模型工具原语和 adapter，而不是把当前内部 `read_file/write_file/run_command` 误称为完整标准工具集。

> 2026-05-28 追加同步：已新增 `ModelToolSurfaceAdapter`，在 PromptEnvelope / CapabilityManifest 中输出稳定的模型侧工具原语和参数契约：`read_file`、`list_files`、`glob -> find_files`、`grep -> search_text`、`write_file`、`edit_file -> apply_patch`、`shell -> run_command`、`run_tests`。随后补上 `todo_read/todo_write`，作为 run-local 的模型规划状态工具，读写 `.asteria/runs/<run_id>/model_todos.json`，可从 `task_plan.json` 派生初始 todo，但不直接修改正式 task plan。ExecutionAction 归一化已接入模型工具名翻译，模型输出 `grep/glob/shell` 时会转成内部 `search_text/find_files/run_command`，并保留 `model_tool_name` 审计字段。这个 adapter 只做模型面标准化和内部 registry 映射，不改变 ToolExecutionGateway 的执行边界；如果内部 registry 没有对应后端，模型侧原语必须显示 `missing/deny`，不能用旧宽泛工具假装完整覆盖。

> 2026-05-28 MCP adapter 同步：已新增独立 `McpAdapter` / `StdioMcpSession` 第一版。MCP 走 server/session/protocol 调用入口，支持官方 SDK 当前使用的 stdio JSONL framing，也保留 `content-length` 兼容模式；调用前复用 `CapabilityDecisionRecorder.decide_mcp()` 写入 `capability_decisions.jsonl` 和 `permission_decision`，调用结果写入 `mcp_invocations.jsonl`。为保持 Studio/CLI 薄消费兼容，用户进展暂复用 `channel=tool,event_type=tool_output`，并在 `data.capability_type=mcp`、`data.adapter=mcp_protocol_session` 中区分 MCP；不复用 `ToolExecutionGateway`。已用官方 `@modelcontextprotocol/server-everything@2026.1.26` 的 `echo` tool 完成真实 stdio smoke，验证路径为 `McpServerConfig -> StdioMcpSession -> McpAdapter.invoke_tool()`。

> 2026-05-28 本轮完成态：Agent-facing tool surface 已进入 Plan/Run/Worker 主循环。Run 会按 task allowed tools 生成模型侧工具目录，CoderAgent 只看到 `read_file/list_files/glob/grep/write_file/edit_file/shell/run_tests/todo_read/todo_write` 等稳定原语，执行前再映射回内部 registry，并把 `model_tool_name`、permission decision、tool observation 和 `user_progress` 串起来。Skill adapter 已具备 `SKILL.md` discovery、参数/产物契约摘要、失败分类、权限审计与用户进展事件；MCP adapter 已补配置、timeout、retry、session close、失败分类、调用摘要和默认配置模板。Studio run detail 默认消费 `user_progress.jsonl`，raw evidence 只进入 Inspector。真实任务验证目录和恢复链路压测已作为长期回归资产接入 gate/validation evidence，而不是发布策略文档。

> 2026-05-28 能力配置口径：Skills 分为全局 Skills 与 workspace 项目级 Skills。全局 Skills 提供跨项目可复用工作流，workspace Skills 提供项目约定、模板和覆盖项；同名 skill 由 workspace 版本覆盖全局版本，但 catalog/evidence 必须保留 `scope`。MCP 是产品/用户级外部连接器配置，默认在产品层配置一次，workspace 只调 timeout/retry 等运行限制，不默认重新定义 server catalog。

> 2026-05-28 最新收口：已新增任务级 `capability_catalog`，把模型侧工具、Skills、MCP server catalog 和权限状态合并为每个 task 的可审计选择面，记录 visible / selected / skipped / blocked 及原因。`validation-run` 会在非 dry-run 预检中记录受控 runtime matrix evidence，用同一套 `.asteria/runs/*` 证据补齐 Skill、MCP、profile、permission reason 和 `user_progress` 覆盖。`runtime_validation_matrix` 已区分 implementation missing、evidence missing 和 historical evidence noise；`gate-status` 新增 readiness explanation，`capability-report` 新增 Evidence health，避免 release/acceptance/matrix/route 口径混在一起。

> 2026-05-29 真实验证同步：受控 runtime matrix evidence 已把 `runtime_validation_matrix` 拉到 8/8；真实 provider validation 场景已覆盖 7/7，当前发布门禁消费 `.asteria/verification/real_model_acceptance_validation_combined.json`，来源为首批 5 个通过场景和补跑的 `validation_refactor` / `runtime_request_resume`。`gate-status` 现在要求完整 validation suite 证据，指定场景子集不会再被当成完整发布门禁；`run_with_heartbeat` 已补 Windows 超时进程树清理，避免 wrapper 超时后遗留子进程。

> 2026-05-29 工具预算口径调整：对齐 Claude Code 一类产品的设计哲学，工具治理不再把所有调用视为同一风险。`tool_calls` 继续记录原始次数用于审计；预算守卫改看 `tool_budget_units`。默认 read/search/list/diff/todo_read 权重为 0，允许 agent 充分探索上下文；verification/state 低权重；write/shell/MCP/Skill/external 高权重并继续走权限与 reason 记录。发布验证 suite 仍严格，产品运行时则按风险、成本、时间和修复次数治理。

本文是 Asteria 当前阶段的事实入口。进入开发前先读根目录 `AGENTS.md`，再读本文，然后按 [文档导航.md](./文档导航.md) 选择详细设计文档。

本文只记录当前事实、长期有效口径、当前差距和下一步优先级。过程性旧计划不放在这里；已过时、错误或理解不正确的内容应直接从长期文档中删除、改写或归档，而不是再新增诊断文档解释旧错误。

## 1. 当前阶段

Asteria 当前处于：

```text
编排式 Asteria Runtime OS 阶段
```

产品能力目标是：

```text
本地优先、可恢复、可审计、成本受控的长任务自主开发运行时。
```

架构总原则：

- 模型主导工作循环。
- Runtime 负责护栏、证据、权限、计费、schema、恢复和验证边界。
- 状态机只作为给大模型和专业智能体的角色工作骨架建议，是智能体协同模型策略，不绑死，不做业务流程脚本。
- 程序负责边界、协议、权限、预算、证据、schema 和 fallback；模型在边界内根据目标、上下文和能力清单动态组织工作。

当前重点不是 unrestricted agent chatroom，也不是让 Studio/dashboard 反向牵引后端，而是把紧凑用户目标稳定转成经过验证的产物：目标规格化、任务分解、动态上下文、受控工具调用、证据记录、验证、修复、成本控制、最终交付。

## 2. 用户入口与过程可见性

用户入口：

- Goal / Plan 是主入口。
- Ask 是轻量入口。
- Brainstorm、Research、Debug、Review、Coder、多智能体、`run/status/review/accept/debug/gate/validation` 等专业链路或命令，是专家命令、维护命令或产品背后的 runtime action。

交互侧要求：

- 无论背后走哪个链路，用户都必须能看到实时的大模型判断结论。
- 接口调用过程、工具调用过程、关键思考摘要、验证过程和失败修复过程必须能进入用户进展事件。
- 过程完成后，系统必须整理最终结果、交付物、验证结论、残余风险和下一步。
- 普通用户不应看到裸 trace、schema dump、provider raw error 或 backend 字段；这些进入 debug / ops / Inspector。

Studio 定位：

- 当前优先完成通用智能体 Asteria 的后端 runtime。
- Studio 是 Asteria 的独立产品交互面，是通用智能体的前端载体，不只是命令包装器。
- Studio 未来消费 runtime evidence、user_progress 和 Inspector，不决定后端核心架构优先级。
- Studio 的产品设计长期有效，负责用户心流、实时过程展示、文件预览/对比、Git 集成、MCP 管理、plugin 管理、skills 管理、session 管理、用户管理、token 额度查看、权限设置、模型设置和展示/配置管理。
- 后端协议稳定前，Studio 进度可以略落后；但 Studio 的整体设计不应被临时后端实现反向绑死。

## 3. 当前事实

已经具备的工程基线：

- CLI/runtime loop 已有 `init`、`goal/run`、`status`、`review`、`accept`、`doctor`、`gate`、`validation` 等入口。
- `.asteria/` 下已经持久化 run、task、worker、runtime profile、context mount、evidence、validation、report 等运行对象。
- Runtime OS 对象已经覆盖 TaskGraph、WorkerInvocation、WorkerResult、RuntimeProfile、ContextMount、TaskExecutionEvidence、MergeGate。
- ContextPackage 已能按 read scope、write scope、artifact refs、failure refs、decision refs、validation refs 裁剪 worker 上下文。
- PromptEnvelope、CapabilityManifest、AgentRoleContract、ToolObservation 已有第一版，模型调用开始记录 role、route、deadline、prompt envelope、capability manifest 等审计字段。
- 模型路由已支持 strong/medium/cheap 默认兜底，缺少 env/local route 或 capability profile 过期时不会卡死在 route guidance。
- ReviewAgent 已有 strong -> medium -> cheap 的降级 fallback，失败时能产生可审计 metadata。
- `status --debug`、JSON、gate、doctor、acceptance 等工程控制面已有较多底层证据。
- `ActiveGoalMemory` 已同时写 `.asteria/memory/active_goal.json` 和 `.asteria/memory/active_goal.md`；JSON 是结构化长期状态协议第一版，Markdown 是用户可读永久记忆。
- 普通 `status` 已开始从结构化长期状态渲染用户状态卡片，而不是整段 dump Markdown。
- Plan 已产出 `WorkspaceEnvelope`，Chat/Status/Sessions 已开始消费该 envelope；新 envelope 已包含 `input_roots`、`artifact_root`、`worktree_policy` 和 `scope_summary`。
- 用户级 `WorkspaceRegistry` 已落地到 `%USERPROFILE%/.asteria/workspaces.json`，只保存 recent/current workspace、output_root 和 artifact_root 指针，不保存运行证据或密钥。
- Plan/Goal 已支持 `--input-root`、`--output-root`、`--artifact-root` 和 `--worktree-policy`；Plan 输出、Status 用户视图、最终报告和 `user_progress` 已展示 output/artifact 落点；artifact evidence 已记录 workspace_id、absolute_path、output_root 和 artifact_root。
- 权限策略已有 `ask_everything`、`reviewed_auto`、`auto` 三档基础 profile，工具权限侧已开始使用 permission profile。
- Chat 已按问题意图挂载动态上下文：普通 Ask 不再默认读取 `.asteria/memory/active_goal.md`，状态/进展/计划/下一步类问题才挂载长期任务记忆。
- Chat 模型请求已携带 `ContextEnvelope`，并在 metadata 中记录 envelope id/hash/path；旧 `safe_project_context` 兼容字段已下线；Chat envelope payload 已接入 CapabilityInvocationPolicy 第一版。
- ChatIntentPolicy 已从 ChatCommand 中独立出来。
- ContextPackageBuilder 已为 worker 任务级上下文包嵌入同类 ContextEnvelope，并将 envelope 持久化为独立审计文件；CoderAgent/DebugAgent 模型调用已记录 worker context envelope path/hash；capability report 已纳入 ContextEnvelope 审计统计。

最近完整验证基线：

- `pytest tests/unit -q`：424 passed。
- `pytest tests/integration -q`：165 passed，1 skipped，26 deselected。
- `mypy src`：通过。
- `ruff check src tests`：通过。

## 4. 当前主要差距

当前差距以 [代码现状差距与研发计划.md](./代码现状差距与研发计划.md) 为准。摘要如下：

1. Runtime 产品契约正在收敛为代码，但覆盖面还不完整。
   - 文档第一刀已经完成，后续是持续治理，不再作为阻塞主线研发的独立阶段。
   - WorkspaceEnvelope、权限 profile、Chat 动态上下文、ContextEnvelope、全局 workspace index、输出目录产品化和多工程 CLI 选择已经落地第一刀。
   - Studio workspace 配置面、ContextEnvelope 更多消费面和 user_progress 全链路覆盖仍需继续实现。

2. 长任务记忆协议第一阶段已收口，后续重点是真实恢复样本。
   - 当前已有结构化 `active_goal.json` 和 Markdown 用户视图。
   - 已记录 `source_run_id`、`updated_by`、`update_reason`、当前目标、完成项、阻塞、问题和下一步。
   - `review`、`resume`、`repair` 来源、损坏 JSON 回退和跨 run 冲突提示已落地；后续需要更多真实任务样本验证恢复质量。

3. Status 用户视图已完成第一阶段，但还需要继续产品化。
   - 当前普通 `status` 已从结构化长期状态渲染状态卡片。
   - 后续要把状态卡片进一步接入 user_progress、Studio 和更多真实任务验证。

4. 用户实时过程协议第一阶段已覆盖主链路，后续重点是事件质量。
   - 当前已有 `user_progress`、模型/工具/文件/验证事件，`/run` 已把 workspace/input/output 选择、最终报告落点、模型判断、文件变化和验证结论写入用户过程线。
   - `review`、`resume`、`replan`、`research`、`brainstorm`、`accept`、`compact`、`handoff`、`debug/repair` 已补用户级事件；后续优化标题、摘要、证据引用和 Studio 展示层。

5. ContextPackage 和 Chat Context 第一阶段已收敛，仍需扩大审计消费面。
   - 长任务 worker 已有 ContextPackage。
   - ChatContextBuilder 已负责构建 Chat 上下文、生成 ContextEnvelope 并持久化 envelope 文件。
   - ContextPackageBuilder 已为 Goal Loop worker 任务级上下文包嵌入同类 envelope，并写入独立审计文件。
   - CoderAgent/DebugAgent 模型调用已记录 worker context envelope path/hash，并已把 ContextEnvelope 纳入 capability report 审计统计。
   - 剩余目标是把 Review/Plan 等更多 Goal Loop 模型调用、weekly/gate 视图、user_progress 和 Inspector 继续接入同一审计链。

6. 工具、MCP、Skills 调用策略已进入第一阶段，任务级能力选择解释已落地第一刀。
   - `CapabilityInvocationPolicy` 已覆盖 Chat/Goal/Plan/Tool，Tool/MCP/Skill 都记录 capability decision reason。
   - 任务级 `capability_catalog` 已把模型侧工具、全局 Skills、workspace Skills 和产品级 MCP servers 汇总为可审计选择面，并记录选择/跳过/阻断原因。
   - 下一步是把真实模型选择行为与 catalog 对齐，形成更强的审计摘要。

7. Research、Brainstorm、Multi-agent 场景型 loop profile 已落地第一阶段。
   - 目标仍是让每条链路按自身业务语义运行。
   - 核心 Goal Loop 负责动态调度和最终验收，不把这些原子能力压平成同一个通用模板。
   - 已通过受控 matrix evidence 覆盖；后续需要更多真实 provider 验证样例证明这些 profile 的 output/validation/recovery 契约能长期稳定。

8. Workspace 和输出位置还没有产品化。
   - Asteria 是通用目标完成产品，不应默认只在当前目录做单工程开发。
   - 目标是让用户能选择 workspace、输入目录、输出目录、候选工作区策略、git 策略和权限模式。
   - 多工程目标必须明确每个目录的 read/write scope，并让用户知道产物输出到哪里。
   - 单工程 run/session/evidence/artifact 必须落在 workspace 内；用户目录下的全局 `.asteria/` 只保存 recent workspace、current workspace、模型 route、账号引用和 UI 偏好。

## 5. 当前研发优先级

### 已完成：长期文档本体第一刀收敛

- 已直接修正当前状态、代码计划、架构、模型主导设计、数据模型、用户交互、运行命令、Studio 设计、治理、评估、安全、验证和验收等长期文档。
- 已删除过程性诊断文件，不再用新增文档解释旧错误。
- 后续文档治理只服务代码实现，不再阻塞主线研发。

### 已完成：Workspace、权限模式与用户进展事件第一阶段

- Plan/Goal 已形成 WorkspaceEnvelope，并记录 input_roots、output_root、artifact_root、worktree_policy 和 scope_summary。
- Chat/Status/Sessions 已开始消费 WorkspaceEnvelope。
- 用户级 WorkspaceRegistry 已记录 recent/current workspace、output_root 和 artifact_root。
- 权限模式已有 `ask_everything`、`reviewed_auto`、`auto` 三档基础 profile。
- 工具权限侧已开始使用 permission profile。
- 工具成本侧已从“原始调用次数硬卡”调整为“原始调用数审计 + 风险加权预算”：低风险读取和搜索不应阻断上下文探索，高风险写入、shell、MCP、Skill 和外部连接继续严格受控。
- user_progress 已增加 workspace、permission、tool、file、validation、model_decision 和 final_report 等事件；`/run` 会把 workspace/input/output 选择、最终报告落点、模型判断、文件变化和验证结论写入实时过程线。

### 已完成：Ask / Chat 动态上下文第一阶段

- 普通 Ask/Chat 不默认读取 `.asteria/memory/active_goal.md`。
- 状态/进展/计划/下一步类问题才读取长期任务记忆。
- 调试类问题才暴露 backend 字段。

### 已完成：长期任务记忆协议化与 Status 用户状态视图第一阶段

- active goal memory 已从 Markdown 渲染副产物升级为结构化长期任务状态第一版。
- Markdown 继续作为用户可读永久记忆。
- `run` 和 `accept` 更新时已记录来源和原因。
- 普通 status 已输出用户状态卡片：当前目标、已完成、当前状态、需要用户做什么、下一步。

### 已完成：长期任务记忆协议补齐第一阶段

- 已补 `review`、`resume`、`repair` 的细分更新来源和原因。
- 已增加损坏记忆回退、跨 run 冲突提示和恢复链路 pressure report。
- 后续只保留真实任务样本扩展和 Studio 展示质量优化。

### 已完成：ContextEnvelope 第一阶段收敛

- 已新增 Chat 可使用的 ContextEnvelope 结构。
- 已明确 audience、intent、sections、refs、redaction policy。
- Chat 模型请求已记录 envelope id/hash/path，envelope 已持久化为审计证据。
- ContextPackageBuilder 已嵌入并持久化 worker envelope，旧 `safe_project_context` 已下线，ChatIntentPolicy 已独立。
- CoderAgent/DebugAgent 模型调用已记录 worker context envelope path/hash。
- capability report 已汇总 context envelope 数量、hash 和模型调用关联统计。
- Chat envelope payload 已接入 CapabilityInvocationPolicy 第一版。
- ContextEnvelope 同时服务模型输入、用户过程展示和审计证据，不只是给模型拼 prompt。

### 已完成：能力调用策略第一阶段

- ChatContextBuilder、Goal/Plan/Tool 执行侧已接入 CapabilityInvocationPolicy 第一版。
- 普通 Ask 默认 no tool/no MCP/no skill。
- 长任务按 task_kind、risk、permission 开放能力。
- 每次能力选择记录 reason，失败进入 diagnose/repair/replan/ask/stop。
- 后续补任务级 capability catalog 和选择/跳过摘要。

### 已完成：场景型智能体链路与专业入口第一阶段

- Research、Brainstorm、Multi-agent 已注册为可匹配、可审计、可进入 worker runtime context 的 loop profile。
- 能力注册层已把工具、MCP、Skills 和智能体链路暴露为模型可理解的 manifest/profile 第一版。
- Research 链路输出深度研究结论、证据等级、验证动作和未确认风险。
- Brainstorm 链路输出开放型候选方向、项目特征、机会、风险和可进入 Plan/Goal 的建议。
- 多智能体链路支持 UI、架构、测试、代码等角色在隔离上下文和 sandbox 中并发工作，再由 Review/MergeGate 收敛。
- 普通用户看到实时过程、阶段性判断和体系化结果；debug/ops 视图进一步展示 worker graph、provider route、候选差异和合并细节。

### P2：Studio 产品交互面

- Studio 作为独立工程长期建设，不作为 Runtime 后端主线的前置依赖。
- Studio 消费 runtime evidence、user_progress、Inspector、配置和管理接口。
- Studio 重点是用户心流、实时过程、文件预览/对比、Git、MCP、plugin、skills、session、用户、token、权限和模型配置管理。
- 后端协议稳定前，Studio 进度可以略落后；但 Studio 的产品设计不能被临时后端实现反向绑死。

## 6. 权威文档

当前开发默认只读：

1. [当前状态与路线.md](./当前状态与路线.md)
2. [代码现状差距与研发计划.md](./代码现状差距与研发计划.md)
3. [大模型循环与动态上下文设计.md](./大模型循环与动态上下文设计.md)
4. [Asteria 模型主导运行时设计.md](./Asteria%20模型主导运行时设计.md)
5. [数据模型.md](./数据模型.md)

按需再读：

- 产品边界：[产品规格.md](./产品规格.md)
- 架构边界：[架构设计.md](./架构设计.md)
- 用户交互：[用户交互模型.md](./用户交互模型.md)
- CLI 参考：[运行命令.md](./运行命令.md)
- 工程横切：[工程化体系.md](./工程化体系.md)
- 治理规则：[工程治理体系.md](./工程治理体系.md)
- 测试与验收：[质量与评估.md](./质量与评估.md)
- Studio 产品面：[Asteria Studio 产品与架构.md](./Asteria%20Studio%20产品与架构.md)
- 真实模型证据：[真实模型验收.md](./真实模型验收.md)
- 验证操作：[验证试运行手册.md](./验证试运行手册.md)

## 7. 下一步

已收口的项目不再列为待办：agent-facing tool surface 主循环接入、Skill adapter 入口、MCP adapter 入口、真实任务验证目录、Studio/CLI `user_progress` timeline、恢复链路压测摘要都已进入代码和测试。Skills 当前按 `global` / `workspace` scope 合并；MCP server catalog 默认是产品/用户层配置，workspace 只覆盖 timeout/retry 等运行限制。

下一段研发重点保持为“真实运行证据和策略闭环”：

1. 真实 provider 验证样例：把 validation 7/7 与 runtime matrix 8/8 固化为可重复回归流程，并继续扩大 Skill/MCP/profile/progress/recovery 的失败和恢复样本。
2. 能力选择策略：把任务级 capability catalog 接到更多模型入口，并记录模型实际为什么选择或跳过某项能力。
3. 阈值与阻断策略：继续按风险加权治理工具预算，让 `gate-status` 和 `capability-report` 对 provider route、permission reason、progress coverage、完整 suite 缺失、recovery pressure 给出稳定的通过/阻断解释，减少历史证据噪声。
4. 长任务稳定性：继续扩大 resume/replan/repair 的真实任务样本，特别是权限阻塞后继续、并发 run、候选 workspace 和损坏 memory 的组合场景。
5. Studio 薄展示：前端继续只消费 `user_progress` 主线；Inspector 展示 raw evidence、adapter invocation、provider route 和 schema refs。
