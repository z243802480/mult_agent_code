# 当前状态与路线

更新时间：2026-07-04

**执行真源**：[`AGENTS.md`](../../AGENTS.md) → [`研发总计划.md`](./研发总计划.md) → 本文（短事实快照）→ 当前 Slice brief / 可复现 evidence。

文档职责与冲突处理见 [`文档体系总览.md`](./文档体系总览.md)。当历史报告、自动路线图和本文冲突时，以本文的最新证据判断为准。

## 1. 当前判断

```text
ACTIVE_PHASE：Post-S76 商业化就绪收敛（Commercial-Readiness convergence）
ACTIVE_SLICE：S77（全系统商业化就绪审计 → 针对性 R&D 重排；审计/收敛检查点，非新编码 Slice，真编码前仍须 brief）
审计签字：docs/zh/reports/S77-commercial-readiness-audit-20260704.md
Brief(前)：benchmarks/reference_briefs/S76-studio-main-content-liveness.md（S76 前端产品化已交付）
实施计划(前)：docs/zh/plans/S74_POST_S73_BETA_CONVERGENCE_PLAN.md
```

S61–S73 已证明 Asteria 具备 route、spawn、L3 workflow、live provider、verifier 和显式 parallel writes opt-in。当前不继续新增编排 Wave；S74 只做产品收敛：恢复快速可靠的默认主路径，用真实任务判断复杂能力是否值得保留或扩大。

> **2026-07-02 状态**：S74 全系统审计后的 **P0→P4 诚实/安全收敛已全部落地并推 origin/main**，发布 **`v0.2.0a2`**（P0 安全·P1 信任·P2 闭环·P3 桌面·P4 债务的诚实 capstone；全程零 DO_NOT_TOUCH 改动）。**S75 续做外部 Beta 安全前置**：`beta_safe` access profile 已交付（一行 pin 硬关 shell/network + `doctor` 核实关停态 + 红队测试证明真关，全锁外）——放量**安全收口的收窄机制已在手**（策略层闸门，非内核 sandbox），operator 可 pin 部署把攻击面降到 sandbox 落地前最小。下一步：真实规模再红队一轮 + 用户拍板发邀请；review/repair 门控经调研已诚实化（repair ledger 判定过度设计弃、/run 内联 review 列 DecisionPoint 建议缓）。

> **2026-07-04 状态（S77 商业化就绪审计）**：完成**全系统商业化就绪审计**（15 子系统 · 36 代理 · 对抗式验证每条「已实现」声明；签字 [`S77-commercial-readiness-audit-20260704.md`](reports/S77-commercial-readiness-audit-20260704.md)）。结论:实现广度≈**71%**（权重压在管道层;Agent 循环内核 67·验证 64·评估 gate 55 低）、市场化裸分 **37/100**→按真实利基调后 **43–45**、**今天不可商业推广**（差距是能力差距非打磨）。**P0**:无 OS 沙箱、无 LICENSE、假默认档静默假输出+无一方前沿 provider;**P1**:自主 repair/replan/resume 环未闭合、质量 gate 量 UX 结构非正确性、S69 验证器源码不存在;**P2**:生态休眠 + 发布卫生。**4 条战略分叉（商业模式/OSS·目标市场 vs provider 区域·可审计防篡改深度·bus-factor）须用户拍板,勿独断。** 近期第一批小时~天级:LICENSE + 假默认档 fail-loud + 版本/wheel/docs 对齐。冻结不变。**定位修正（用户拍板）**：本项目=**团队内部 AI 编程发动机/国产化基础设施**（非商用竞品）,目标中国/APAC、栈 glm/minimax 已适配、不重接 Anthropic/OpenAI,LICENSE 为内测专有约束——故"商用就绪"market score 非操作目标,沙箱/西方 provider 降级,真正内部优先级=闭合自主环+真正确性 eval+工具生态+诚实化（详见 §16.1 + 记忆 product-positioning-internal-engine）。**第一批已落地**：专有 LICENSE + pyproject 元数据、wheel 对齐 0.2.0a2、S69 缺席如实标注;假默认档 fail-loud 经核实牵动 3 测试+D-3 决策（运行时真实默认已是 minimax 真的）,降为定向项。

## 2. 已实现快照

| 层 | 当前能力 | 默认策略 |
| --- | --- | --- |
| Runtime 主路径 | Goal → Plan → Tool Use → Verify → Review/Accept | `session_agent`、bounded loop |
| 模型决策 | `tool / subagent / repair / replan / ask / stop` | 模型选择；Runtime 校验权限、安全、预算和动作一致性 |
| MCP / Skills | 配置 MCP server / 放 `.asteria/skills/<name>/SKILL.md` → 自动发现 → `mcp__server__tool` / `skill__name` 进入模型工具面 → gateway 路由 → 调用边界 `decide_mcp`/`decide_skill` 门控 | 2026-06-29 接入（用户授权的生产接线）；默认无配置 = 零行为变化；MCP/Skill 均为"另一个工具名"，不引入第二控制面 |
| Context | GoalSpec/Coder/Review 角色上下文投影；compact/handoff | 完整 envelope 与 raw evidence 留在持久化层和 Inspector |
| Review（独立步） | deterministic-first + tiered review | 由 `asteria review` / accept 产出 `eval_report.json`；`/run` 本身不内联跑 review、review_status 缺省 `unknown`（真接回 /run 属 DecisionPoint）。简单产物轻量验证；bugfix 必须命令验证 |
| Workspace | candidate / merge / promotion / rollback | 单写者默认；parallel writes 仅显式 opt-in |
| Studio | session narrative、动作按钮、diff、权限卡、Inspector | 主窗口只讲用户任务进展；内部证据进入 Inspector |
| Provider | 多 provider、deadline、stream telemetry、`call_health` | fallback 成功标记 degraded，不冒充健康主路径 |
| Worker transport | 单文件 / 文档 fast path 试行 `tool_use` | 仅在 native tool-call 支持的 provider route 上灰度；2026-06-09 rolling matrix 还未证明它能稳定减少 single_file_bugfix 的额外 medium execution |

历史阶段与 Slice 签字证据位于 [`reports/`](./reports/)；机制设计入口位于 [`文档导航.md`](./文档导航.md)。

> **2026-06-29 增量（用户授权，超出 S74 收敛范围）**：完成全系统文档×实现对账并落地一批修复——
> 前端完整性（真 bug：首个 user_message 前的步骤被吞、首屏 bootstrap wrong-empty 闪烁（362e6a3 占位）；中文系统串替换为英文字面量、narrative 死代码清理、firstText 三副本合一、subagent_summary narrative 节点；`tsc --noEmit` 通过）、
> **P0 schema 完整性**（运行时加载的根 user_progress 契约恢复校验 + capability/mcp/skill 三条调用流补
> schema；另查出 18 对同名 schema 漂移已锁住待办）、文档真源归一（删 9 个幻影命令、循环图去
> auto-Gate→Repair/DebugAgent、评估指标去夸大、供应商澄清）。并按"生产标准"指示**真正接通 MCP 与
> Skills**（此前仅适配器/发现存在、未接入循环）：二者都做成"另一个工具名"经 gateway 路由（守
> ADR-0014/0015 单循环），各 5 slice + 测试；先过 Plan agent 设计再实现。**一处权限策略变更**：
> skill 从硬限定 `_ARTIFACT_SKILLS` 改为由 `allowed_skills` 契约 + 权限模式门控（镜像 MCP）。
> 验证：855 unit + execute_command 集成全绿，ruff clean（含本次复核修掉的 2 个测试文件 lint）。
> **2026-06-29 复核后补强（transport parity）**：原已知缺口「MCP/Skill 仅在默认 json 主路径可见」已补——
> `worker_transport` 增 `native_tool_specs_from_surface`，CoderAgent 在 `tool_use` 下把发现的
> mcp__/skill__ 暴露为 native tool-def、在 slim 上下文下保留 mcp/skill 子集；无 MCP/Skill 配置时零行为变化，
> +9 测试覆盖四象限（json/tool_use × full/slim）。另：docs/en 镜像曾残留 2 个幻影命令
> （acceptance-trend/release-gate），本次一并删除。
> **卫生收尾（2026-06-29）**：① `grep`/`search_text` 补真正则（`regex=true`，默认仍 literal、不破坏既有行为；
> 非法 pattern fail-closed；+5 测试）——此前"叫 grep 实为字面匹配"的契约落差已补齐；② `DebugAgent`
> 标注为 deferred placeholder（triage KEEP_PLACEHOLDER，不删但不再像意外死代码）；③ 补一条全链 e2e
> （stub model 真发 `mcp__` → propose_action → gateway → McpAdapter → observation），堵上"只手注入调用"的测试缺口。
> ④ 按用户决策，north-star flags（`--toward-north-star`/`--max-slices`）从默认 `run`/`goal` help 隐藏
> （`argparse.SUPPRESS`，仍对 maintainer 可用）——North Star 已冻结，默认入口不再露出激活旗标。
> 865 unit + 集成全绿。
>
> **2026-06-30 增量（用户授权 · Studio 审批模型成熟化 + 真 bug）**：用户反馈 candidate/accept 模型反直觉、要对齐成熟工具（Cursor/Claude Code），并报 session 切换内容不变。先三代理+Plan agent 查清地面真相（**文件默认就直接改**：candidate 验证过即 auto-promote 回真实文件；candidate 是内部 verify-before-write/rollback/并行安全 buffer=North Star 差异化，**保留**；底部 Accept 是事后 finalize；inline 命令审批已存在但只针对 shell）。落地：
> ① **session 切换 bug 修复**：`clearSelection` 漏清 `runDetail`/`selectedRunId`，Thread 继续渲染旧 run 的 runtimeEvents——一处修复覆盖 select/new/delete。
> ② **Accept→Finalize 诚实化**：默认下改动已落地，文案改为"N files changed in your workspace — review, keep or revert, then finalize"，状态 chip accept→applied。
> ③ **权限分级可见且真生效**：Composer 3-mode 分段控件（Ask first/Auto-edit/Full auto → ask_everything/reviewed_auto/auto）接线到 runtime `--permission-level`（此前 runtimeCommand 从不传，纯装饰）；新增 `studio/lib/permission-level.mjs`（纯函数+11 单测）；默认 reviewed_auto→balanced=现状零行为变化。
> ④ **补齐风险路由（诚实契约）**：此前文档称 reviewed_auto 把风险改动转 `pending_manual_approval`，但 MergeGate 只查 write_scope+verification、`irreversible_delete` 硬守卫只在名单里**从未强制**——均为声称未实现。现 MergeGate 加 `classify_change_risk`（高风险 task / 敏感路径：依赖·lock·CI·容器·secret-adjacent / **删除已跟踪文件**）只标注不阻断；gateway 在 reviewed_auto+risky 时走**既有** pending rail（守 ADR-0014/0015，不建第二控制面）；ask_everything 仍写前 gate、auto 仍全自动。**默认行为变化**：默认下改 package.json/CI/lockfile/删文件/高风险 task 会暂停等 inline 批准，普通源码改动仍直接落地。未碰 DO_NOT_TOUCH（execute_command 经既有 promote_changes 回调）。
> 验证：825 unit + 61 相关 integration 全绿、ruff clean；前端 tsc+vite build 绿、computed-style 验证；server.mjs `node --check` 通过。
>
> **2026-07-02 增量（用户授权 · 全系统「文档声称 vs 代码现实」审计 + P0 安全止血）**：12 维、34 代理、
> 1235 次工具调用的全系统审计（[`S74-full-system-claims-audit-20260702.md`](reports/S74-full-system-claims-audit-20260702.md)）
> 确认 **22 条 critical/high 裂缝、0 被推翻**：后端核心比预期真，裂缝聚集在 ① shell 安全强制被超卖
> （`cat .env` 可直读、`curl` 可直传）② 权限承诺是标签（ask==allow、approve_similar 假证据）
> ③ 前端诚实化未闭环（罐头 chat / 假 AI Debug）④ 闭环断点 + 能力不可见。用户拍板执行顺序重排为
> **P0 安全止血 → P1 信任修复 → P2 核心闭环断点 → P3 桌面赌注 → P4 债务收敛 + 外部 Beta 邀请**，
> 外部 Beta 邀请与 re-tag 延后到 P0+P1 之后（详见研发总计划.md §16）。
>
> **P0 已落地**（[`S74-p0-security-hardening-20260702.md`](reports/S74-p0-security-hardening-20260702.md)）：
> ShellGuard 加保护路径/secret 预扫（堵 `cat .env`）+ `allow_network` gate（堵 curl/wget/nc/ssh/git-clone
> 外传）+ 破坏性命令深扫（堵 `find -delete`、解释器 `rmtree`、引号包裹的 `Remove-Item`）；`.asteria/`
> 纳入默认 `protected_paths` 防自提权；`runtime_request` packaged schema 补 `auto_applied` +
> `JsonlStore.rewrite_all` 校验写（修复毒化风险）。红队 31/31 攻击命令拦截、23/23 合法命令 0 误报；
> 随后 4 代理对抗红队追加 34 条候选绕过，第二轮加固后 28/34 拦截（`security/shell_guard.py`：
> cmd.exe caret 折平、目录规则大小写/尾点归一、path-qualified 命令名还原、wrapper payload 二次扫描、
> 网络 LOLBin + 深层正则、破坏动词补全、重定向无空格漏洞）。均 additive，未碰 `execute_command.py`/
> `run_command.py`/gate 栈。验证：unit 863（security 82）+ integration 198 子集 + doc_contracts 22 全绿。
>
> **诚实定性（未解决，写入放量前置风险）**：shell 命令 denylist 是减速带，不是安全边界。解释器任意
> 代码 payload（反射/拼接/urllib/fetch）和无处不在的覆盖命令（cp/mv）无法被静态扫描彻底收住——
> **唯一彻底解是 sandbox（KEEP_PLACEHOLDER，进程级文件/网络隔离）**。external Beta 放量前，shell
> 应默认收窄/关闭或跑受限容器；这已列为放量 DecisionPoint 的前置风险，不得被「denylist 已加固」
> 误读为「shell 已安全」。

## 3. S74 真实证据

| 检查 | 最新结果 | 结论 |
| --- | --- | --- |
| 文档与主路径聚焦回归 | `116 passed` | 角色上下文、fast path、Review 契约全绿 |
| steady iteration | doc contracts + Execute + golden + CL-010 全绿 | 基线可复现 |
| **S74 Golden Beta**（2026-06-10） | 三类默认 Session 任务共 9/9 通过；脱敏摘要已提交 | 可复现默认主路径已复签；原始 evidence 继续留在本机 `.asteria` |
| CL-010 + capability_ref 规范化 | subagent `skill`→`subagent` transport 修正 | 委派 dispatch 不再因 type 漂移失败 |
| Week-1 DecisionPoint | `reports/S74-week1-decision-draft-20260609.md` | **正式复签**：默认 Session 保持唯一主路径；复杂编排继续 opt-in 默认关闭 |
| Studio/runtime 一致审计 | `s74_session_telemetry_audit` + run-scoped Studio benchmark | CC/Codex 过程审计；SLO 仅 warning（ADR-0010） |
| 2026-06-10 Golden Beta | CL-011 前 small_cli 失败 161.7s；删除后 small_cli 连续 3 次通过（26.8–116.2s），doc_update 通过 108.6s | 默认 Session 主路径成立；耗时波动主要来自 provider，smoke 不再控制恢复 |
| Golden Beta 三类基线 | small CLI、doc update、context maintenance 共 9/9 通过；全部 2 medium calls、0 repair | 默认 Session/context/tool 主路径冻结；进入 provider SLO 与外部 Beta friction |
| Studio 产品主路径 | build、Session contract、user-thread copy、interactive main path 3/3、run-detail、beta workflow 全绿 | 当前不新增 Studio 功能；等待外部 Beta friction |
| Beta 发放与 friction 边界 | maintainer beta trial smoke 全绿；非维护者样本 `0`；maintainer/sim/unknown 仅进入诊断附录 | 工程可发放，但不能用维护者证据代替用户价值签字 |
| 历史灰度 single_file / bugfix / 委派 | 见 POST_S73 计划 §6 | 主路径分钟级；委派 CL-010 后 114s/3 calls |

证据原则：`.asteria/` 是本机运行证据，不是跨 checkout 的项目事实。阶段签字必须包含可提交的脱敏矩阵摘要或可复现命令结果。

## 4. 综合下一步计划

**唯一发布主线**：[`plans/发布计划-首个可发布版本.md`](./plans/发布计划-首个可发布版本.md)（v0.2 可发布判据 R1–R5 + 里程碑 M1–M4）。S74 收敛已并入该计划的 M1/M2。

**2026-07-02 起当前执行顺序**（全系统审计后用户拍板重排，见研发总计划.md §16）：

1. ~~**P0 安全止血**~~ ✅ 已落地（`1fdd64c`）→ [`S74-p0-security-hardening`](reports/S74-p0-security-hardening-20260702.md)。
2. ~~**P1 信任修复**~~ ✅ 已落地（`b921028`）→ [`S74-p1-trust-repair`](reports/S74-p1-trust-repair-20260702.md)：删假证据/标签、MCP/Skill ask 诚实降级为 allow、chat 罐头披露、前端文案（AI Debug Agent→Run Diagnostics、删假「Done.」、plan 开场误标 final、离线告警补全局路由）。
3. ~~**P2 核心闭环断点**~~ ✅ 已落地（`36ac4c1`）→ [`S74-p2-closure-breakpoints`](reports/S74-p2-closure-breakpoints-20260702.md)：skill/MCP 回灌·4 工具解禁·route_fallback 落盘·chat 历史（4 真做）；`/run review`·修复预算 2 项撞 DO_NOT_TOUCH 走诚实降级 + DecisionPoint。
4. ~~**P3 桌面赌注**~~ ✅ 已落地（`8e07b3a` 诚实批 + `f579edf` 功能批）→ [`S74-p3-desktop-stakes`](reports/S74-p3-desktop-stakes-20260702.md)：去占位真流式·事件 id 去重·Inspector raw evidence·文档超卖（诚实/bugfix）；Stop/中断·会话搜索·token 用量面板（无 USD）·capability_decisions（用户绿灯新功能）。
5. ~~**P4 债务收敛**~~ ✅ 本批已落地（`ef7428e`）→ [`S74-p4-debt-honesty`](reports/S74-p4-debt-honesty-20260702.md)：init 幂等·文档超卖降级·session_id 去 null·死 MCP 降级；schema 双向漂移/real_model mypy/Studio 会话 id 全链路/acceptance 计分 均撞 DO_NOT_TOUCH → DecisionPoint。
6. ~~**re-tag v0.2.0a2**~~ ✅ 已打并推（`c8d3a61`，CHANGELOG 已录，P0-P4 诚实 capstone）。
7. **外部 Beta 邀请**（门槛 P0+P1 已满足，仍待放量）：属对外发送=**用户本人操作**；**前置安全收窄机制已交付（S75）**：`beta_safe` access profile 让 operator 一行 pin 部署硬关 shell/network、`doctor` 核实关停态（[`S75 signoff`](reports/S75-beta-safety-gate-and-verify-honesty-20260702.md)）。仍属**策略层闸门非内核 sandbox**（denylist 定性不变），但已把 sandbox 落地前的攻击面降到最小；剩「真实规模再红队一轮 + 用户发邀请」。
8. **遗留 DecisionPoint**（解锁 DO_NOT_TOUCH 才能真做，建议缓）：真接 `/run review`（现有 `review`/`accept`+`runVerificationHint` 已是主流形态）、schema 漂移同步、真写 Studio 会话 id、acceptance 计分诚实化。**repair 预算 ledger 经调研判定过度设计已诚实弃**（不再列 DecisionPoint）。

**安全裂缝不堵不放量**：外部 Beta 邀请必须在放量安全收口之后——**收窄机制（`beta_safe` pin）已在手**，operator pin + `doctor` 核实即达收口，彻底隔离仍待 sandbox（KEEP_PLACEHOLDER）。历史 S74 收敛节奏见
[`plans/S74_WEEK1_CC_CODEX_EXECUTION_PLAN.md`](./plans/S74_WEEK1_CC_CODEX_EXECUTION_PLAN.md) §0；
全系统裁决矩阵见 [`plans/S74_SYSTEM_AUDIT_VERDICT.md`](./plans/S74_SYSTEM_AUDIT_VERDICT.md)。

当前 `beta_friction_aggregate` 的非维护者样本为 `0`。在获得重复真实摩擦前，冻结 Studio
新功能与默认 Runtime 路径改动；维护者 smoke 只证明机制可运行，不代替用户价值证据。
Beta 发放前检查已覆盖任务包、维护者试跑 smoke 与 Release 构建；试跑模板已要求记录首次有效动作、
最长无反馈等待、下一步理解和 Inspector 查证价值。Provider SLO 继续复用 Golden Beta 摘要，
复杂能力继续复用 S74 配对矩阵，不新增第二套 readiness/gate。

详细执行与停止规则见 [`plans/S74_POST_S73_BETA_CONVERGENCE_PLAN.md`](./plans/S74_POST_S73_BETA_CONVERGENCE_PLAN.md)。

## 5. 冻结项

- 新编排 Wave 或新 Agent 类。
- `parallel_writes` 全局默认开启。
- 无真实 friction 证据的 Studio 新功能。
- 针对单个失败任务增加 keyword/parser/recovery 尾巴。
- 把 route、gate、capability、raw evidence 搬回 Studio 主会话。

## 6. 常规验证

```powershell
pytest tests/unit/test_documentation_contracts.py -q
pytest tests/integration/test_execute_command.py -q
python scripts/steady_iteration_check.py --root . --skip-b6 --skip-wheel
python scripts/s74_beta_matrix_evidence.py --root . --import-summary <summary.json>
# 维护者明确启动 live provider/scenario：
python scripts/s74_beta_matrix_evidence.py --root . --live
node studio/scripts/run-detail-smoke.mjs
```

真实 provider 验证应始终绑定当前源码，并记录统一结果字段；不得用旧安装包结果代替当前源码签字。
