# 当前状态与路线

更新时间：2026-06-05

本文是 **短事实快照**。长期路线、Phase、Vibe Slice、Triage 锁、可行性闸门以 [`研发总计划.md`](./研发总计划.md) 为唯一执行入口；进入开发前读 `AGENTS.md` → 研发总计划 → 本文。

## 1. 当前阶段

```text
Phase 1 进度协议（S1–S4 + S1b MERGE）已完成
Phase 2 Studio 对齐（S5–S6）已完成
ACTIVE_SLICE：S7（MVP 终点）— 契约测试已绿；待 real provider 签字
Phase 3 入口：S7 签字后启动（见 docs/zh/reports/S7-mvp-signoff-pending.md）
```

产品方向：Harness 优先的通用长任务智能体；参考 OpenCode / Claude Code / Codex-rs 不造轮子；后端优先 + Studio 并行。

## 2. Implemented（代码 + 测试已存在）

| 领域 | 事实 |
| --- | --- |
| Harness | `AgentLoopDecision` 六动作；bounded loop；800+ tests |
| 主命令 | `init`, `goal`/`run`, `plan`, `chat`, `status`, `review`, `accept`, `debug`, `execute`, `resume`, `sessions` |
| 进度协议 | S1–S4：`user_progress` main 事件 + `status --json` 投影 plan/tool/verify/final |
| MERGE_TRIM | 删 `runs_command`、`validation`→`validation-run --dry-run`；顶层 help 隐藏 maintainer |
| Studio | Thread 消费 `transcript_kind`；`runtime_progress.plan` 与 status 对齐；run-detail-smoke 绿 |
| 安全 | candidate workspace、merge gate、promotion queue、protected paths |
| 运维/CI | gate、acceptance、evidence-bundle（**隐藏，不删**） |

## 3. In Progress / 待签字

- S7 MVP：`studio-benchmark small_code_change` ≥ 0.8 + **real provider**（见 `benchmarks/phase2_mvp_gate.json`）
- CapabilityManifest 注入与 catalog 审计对齐

## 4. Designed-not-built（Phase 3+）

见 [`deferred/NORTH_STAR_RFC.md`](./deferred/NORTH_STAR_RFC.md)、[`deferred/SWARM_SANDBOX_RFC.md`](./deferred/SWARM_SANDBOX_RFC.md)。

## 5. 可行性闸门状态

| 闸门 | 状态 |
| --- | --- |
| Phase 0（doc contracts） | 已通过 |
| Phase 1（进度协议 + MERGE） | 已通过（S1–S4、S1b） |
| Phase 2 / S7 MVP（real provider） | 契约：run 级 benchmark 绿（见 `test_s7_golden_benchmark.py`）；**real provider 待签字** |

MVP 终点：`benchmarks/studio_user_tasks.json` → `small_code_change` score ≥ 0.8。

## 6. 常规验证

```powershell
pytest tests/integration/test_s7_golden_benchmark.py tests/unit/test_plan_progress_contract.py -q
node studio/scripts/run-detail-smoke.mjs
python -m asteria_runtime studio-benchmark --root . --run-id s7-golden-run --json
```

## 7. 文档入口

- 执行计划：[`研发总计划.md`](./研发总计划.md)
- Vibe Slice：[`benchmarks/vibe_slices.json`](../../benchmarks/vibe_slices.json)
- Reference briefs：[`benchmarks/reference_briefs/`](../../benchmarks/reference_briefs/)

## 8. Phase 3 入口（S7 签字后）

S7 real provider 签字完成后：

1. 更新 `ACTIVE_SLICE` → S8，`ACTIVE_PHASE` → Phase 3  
2. 执行 rolling scoped real cases（见 `benchmarks/phase2_mvp_gate.json`）  
3. 扩展用户命令 smoke：`tests/integration/test_user_command_smoke.py`（chat / handoff / compact / sessions）  
4. **禁止** 提前启动 North Star / 蜂群（见 `deferred/` RFC）
