# 项目驾驶舱

更新时间：2026-06-09T23:37:14+08:00

这是一页式进度跟踪页。它从当前状态与证据文档渲染，目的是让你只看这一页，就能判断当前完成度、灰度项、主风险和下一步。

如果本页和 `reports/`、`当前状态与路线.md`、`研发总计划.md` 冲突，以最新证据和当前状态页为准；本页只负责给出可执行的驾驶视图。

## 1. 当前结论

```text
系统已经具备可运行的本地-first 长任务 harness 主链路，但仍处在产品收口和灰度校准阶段，不适合再扩大复杂编排默认面。
```

### 完成到什么程度

| 维度 | 现状 |
| --- | --- |
| 主链路 | Plan/Todo -> Tool Use -> Verify -> Repair/Ask/Stop |
| 当前阶段 | Post-S73 Beta convergence |
| 当前步骤 | Run the rolling small gray matrix and keep tool_use experimental. |
| 证据链 | real-model-smoke --matrix-preset rolling-fastpath-v1 --no-research --model-max-retries 0 |
| 质量控制 | passed / n/a |
| 灰度编排 | session_agent |

### 仍未收敛

| 维度 | 当前状态 |
| --- | --- |
| 当前阻塞 | 单文件 / 文档 fast path 试行 `tool_use` |
| 待决数量 | 1 |
| 最近失败 | none |
| 最新矩阵 | 2026-06-09 rolling `single_file_bugfix` still shows extra medium execution/repair. |
| 路线健康 | healthy |

## 2. 当前只看这几个指标

1. strong 调用是否只随风险升级出现
2. 总等待时间是否被压住
3. medium execution / repair 是否下降
4. 最终报告是否仍然可读
5. slim / focused context 是否真的覆盖到目标任务

## 3. 当前主风险

1. 路线漂移：旧结论容易被新文档或局部样本覆盖。
1. 局部最优：某个任务样本变好，不代表主路径已稳定。
1. 信息碎片化：报告、路线、验证各自说话，最后没人能判断真相。
1. 实现偏航：为了修一个 case 继续加分支，最后把主路径弄复杂。

## 4. 下一步只做什么

### 只保留一条主线

- 固定滚动小灰度：doc/simple_file、single_file_bugfix、context-heavy maintenance
- 继续记录：strong 调用、总等待时间、medium execution、repair 次数、报告可读性
- 对 tool_use 继续保持实验态，不升默认

### 只做一类判断

- 这轮有没有比上一轮更短的主路径
- 这轮有没有更少的强模型介入
- 这轮有没有更少的 repair 和二次执行
- 这轮最终报告是不是更容易读

## 5. 当前主路径

Main path:
- path: Plan/Todo -> Tool Use -> Verify -> Repair/Ask/Stop
- active: Tool Use
- current: Run the rolling small gray matrix and keep tool_use experimental.
- next: asteria real-model-smoke --matrix-preset rolling-fastpath-v1 --no-research --model-max-retries 0

## 6. Todo 视图

Todo:
- ACTIVE_SLICE=S74; continue doc/simple_file, single_file_bugfix, and context-heavy maintenance gray samples.
- current: single_file_bugfix (in_progress)

## 7. 使用方式

每次跟进只问这四句：

1. 现在完成到哪了？
2. 最大风险变了没有？
3. 最新证据支持什么？
4. 下一步唯一该做什么？

本页由 `scripts/write_project_cockpit.py` 生成，可随当前状态与证据重建。

## 8. 参考来源

- [文档体系总览](./文档体系总览.md)
- [当前状态与路线](./当前状态与路线.md)
- [真实模型验收](./真实模型验收.md)
- [研发总计划](./研发总计划.md)
