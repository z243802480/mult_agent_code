# 自动路线图

- 状态：fast_path_runtime_stabilization
- 更新时间：2026-06-03T23:10:00+08:00

> 本文是 `roadmap-update` 的默认生成输出位置，用作机器生成路线摘要；当前事实和优先级仍以 [当前状态与路线.md](./当前状态与路线.md) 为准。

## 产品简述

Build a local-first multi-agent autonomous development runtime that turns compact goals into verified artifacts through planning, controlled tool use, validation, repair, context compression, cost control, and final reporting.

## 当前能力

- Runtime OS 主链路已具备 goal -> plan -> execution -> validation -> artifact -> report 的可审计闭环。
- Fast Path / Tiered Review / Context Slimming 已进入主路径：低风险文档和简单文件任务默认 medium/deterministic，强模型只在风险升级、发布验收或显式维护者复审时使用。
- 真实 provider `doc_update` P0 case 已从 4 次模型调用、1 次 strong review、1 次 repair、103.024 秒，收敛为 2 次 medium 调用、0 strong、0 repair；最新 readback review 修复后复验为 47.850 秒。
- rolling fast path v1 已固定 `doc_update`、`single_file_bugfix`、`context_maintenance` 三项；scope 修复后风险 case 复验为 `single_file_bugfix` 3 次 medium、0 strong、0 repair、41.090 秒，`context_maintenance` 2 次 medium、0 strong、0 repair、33.073 秒。
- ContextBudgetMeter v2、capability catalog、worker/candidate/promotion evidence、RuntimeReadinessGate 和 v0.2 rolling validation 摘要已经形成可观测基础。
- 真实 disjoint write workers 仍默认关闭，后续必须继续受 sandbox、candidate promotion、merge gate 和 recovery evidence 约束。

## 里程碑

- M1 [done]: Runtime OS 主骨架、schema evidence、bounded loop、candidate/promotion/recovery 和 gate 基线落地。
- M2 [done]: alpha.2 scoped dogfooding、route guidance refresh 和 fast path P0 修复完成。
- M3 [in_progress]: 用 3-5 个 scoped 真实 provider 任务校准 fast path、context slimming、deterministic review 和 provider latency。
- M4 [in_progress]: Studio 会话主路径定型，让用户无需理解内部命令即可看见 plan/todo、tool use、verify、repair/ask/stop、result 和 next step。
- M5 [pending]: 并行写 worker sandbox 灰度，前提是 disjoint write gate、candidate promotion、merge gate 和 rollback/reject/discard recovery 全部可见且可阻断。

## 风险

- 真实 provider latency 仍可能被 strong review、route timeout 或上下文过大放大；必须继续用 matrix summary 和 `model_calls.jsonl` 校准。
- 低风险任务如果缺少 explicit artifacts / verification evidence，deterministic-first review 不能强行通过。
- Studio 若过早暴露 gate/provider/raw evidence 字段，会把主路径重新复杂化。
- 真实并行写 worker 在 sandbox/promotion/recovery 未稳定前不能放量。

## 下一步

1. 继续滚动复验固定 fast-path 子集，覆盖 `doc_update`、`single_file_bugfix` 和 `context_maintenance`，重点观察 strong 调用、repair 次数、provider 等待时间和 medium schema retry。
2. 对比每个 case 的 strong 调用次数、模型等待时间、context_mode、slim 覆盖率、deterministic review 命中率和用户可见 next step。
3. 把稳定指标补进 `gate-status` / `status` / evidence bundle 的控制面摘要。
4. 继续推进 Studio 主路径，只消费 `user_progress`、runtime progress、artifact、DecisionPoint 和 Inspector raw evidence。
