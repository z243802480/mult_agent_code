# v0.1.0-alpha.1 验收报告

日期：2026-06-01

## 结论

`v0.1.0-alpha.1` 已完成最小真实证明，可以进入内部受控试跑候选包装；不能直接扩大内测放量。

本轮证明了真实 provider scoped `validation-run` 能在 release gate 打开后完成，并把 probe results、run evidence、evidence bundle、ops signal analysis、weekly report 和 roadmap 串成准入证据链。剩余风险集中在 missing probe 扩面、恢复压力样本和真实 disjoint write workers 继续关闭。

## 已通过证据

- strong route：`AGENT_MODEL_STRONG_NAME=glm-5.1` 下 route guidance 从 hard block 恢复为 review。
- core acceptance：13/13 passed。
- acceptance gate：`release_ready=true`。
- validation suite：初始 6/7 暴露 `validation_multi_file_scope` 的真实模型 typo 与 repair 非 JSON 问题；DebugAgent 确定性修复兜底后 targeted rerun 通过，gate-status repair closure 合并为 7/7。
- real scoped validation-run：`.asteria/validation_runs/validation-2026-06-01T201146-0800-1cfce2bd/summary.json`，对应 run `.asteria/runs/run-20260601-0002/`。
- evidence bundle：`.asteria/evidence_bundles/evidence-2026-06-01T201619-0800.zip`，包含 validation run summaries、ops usage signals/analysis 和最近 run evidence tail。
- ops signal：`usage-signal-0002` 记录 alpha.1 真实 provider validation-run 被 accepted。
- weekly/roadmap：`weekly-report 2026-W23` 和 `docs/zh/自动路线图.md` 已消费 usage signal analysis。
- usage signal supersede：旧的 `route_guidance_blocked / strong_goal_spec_unstable` blocker 仍保留在原始 summary 中，但已由新的 accepted signal 标记为 `superseded_signals`；weekly/roadmap 使用 `active_summary` 判断当前风险，避免历史 blocker 继续误导下一轮研发计划。

## 当前阻断

这些阻断不否定 alpha.1 最小证明，但阻止扩大内测批次：

- `validation-run` probe 中 subagent selection、readonly fanout、readonly write-tool blocking、disjoint write unsafe fanout 和 parent loop stop 仍是 `missing_evidence`。
- route guidance 仍为 review，需要继续收集 capability evidence。
- recovery pressure 仍缺 damaged_memory / multi_run_conflict 等压力样本。
- 旧 usage signal 中的 route_guidance_blocked / strong_goal_spec_unstable 不能删除，应作为 superseded 趋势解释保留；当前 active risk 以新 accepted signal 之后的 active summary 为准。
- 真实 disjoint write workers 默认关闭，不能绕过 candidate workspace、merge gate、promotion recovery 和 rollback/reject/discard 可见性要求。

## 下一阶段命令

```powershell
ruff check .
mypy src
pytest -q
python -m asteria_runtime /acceptance --suite core --allow-fake --root .
python -m asteria_runtime /acceptance-gate --suite core --root .
$env:AGENT_MODEL_STRONG_NAME='glm-5.1'; python -m asteria_runtime gate-status --root . --json
python -m asteria_runtime evidence-bundle --root . --json
python -m asteria_runtime ops-signal --summary --analyze --root .
python -m asteria_runtime weekly-report --root . --week-id 2026-W23
python -m asteria_runtime roadmap-update --root . --output docs/zh/自动路线图.md
```

## 研发推进顺序

1. alpha.1 收尾：把完整回归、验收报告、证据包和 docs 状态固定为 release candidate checklist。
2. probe 扩面：用 3-5 个 scoped 真实任务补齐 readonly/subagent/disjoint/parent-loop missing evidence。
3. 恢复压力：补 damaged_memory / multi_run_conflict 样本，让“阻断后怎么恢复”继续进入 graph/gate。
4. disjoint write workers：先在 fake path 验证真实并行 gate allow/block，再考虑打开真实并行 feature flag。
