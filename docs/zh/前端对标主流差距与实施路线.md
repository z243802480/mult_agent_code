# Studio 前端对标主流差距与实施路线（2026-07-16 全面审计）

> **单一真源**：本文接替《前端产品化路线.md》成为 Studio 前端对标主流的执行路线。原路线（S76 波段 I1–I14 / M1–M4 / INS / PREVIEW-1·2·3 / PR1–PR4）已闭合并删除，原文见 git history，登记见 [已删除与已替代登记](已删除与已替代登记.md) §2b。
> 触发：用户「全面排查下现在前端跟主流产品的差距，并把对标的实现思路完善到文档里面」（2026-07-16）。
> 方法：三路并行——①Studio 前端全量能力测绘（studio/src + server.mjs 逐文件核实，含文件:行号证据）；②主流产品前端调研（4 条产品线，官方文档优先，时效 2026-07：Claude Code 桌面/web/VS Code、OpenAI Codex 五 surface、GitHub Copilot cloud agent、Cursor 2.x–3.x、Devin/Devin Desktop、Google Jules、Manus、OpenCode/Crush/Cline/Goose）；③既有前端文档完成度盘点。信源与未核实项见 §7。

## 0. 继承的硬约束（原路线 §1/§2，仍然全部有效）

1. **主区内容丰富**——plan/工具/diff/进度端到主区，不止流式文本。
2. **紧凑·高级·去框化神圣**——richness 靠排版/字重/缩进/语义色/默认折叠，不靠新卡片背景；响亮色只留给需人决策的（diff/权限/错误）。
3. **绝对诚实**——无真事件不渲染；估算必标 `est.`；无定价不显 `$`；不在终止事件前显绿"done"；压缩/截断显式告知。
4. **长任务不崩**——窗口化、seq 游标幂等重连、磁盘 JSONL 永远是真源。
5. **派生而非杜撰**——新 UI 一律从运行时已流出的事件派生；无数据渲染空而非占位。
6. **抄主流交互语义，不自创设计结构**（习惯迁移成本）；**独有能力**（证据链/promotion/验证矩阵）**藏进 Inspector**，不稀释主线程。

## 1. 主流收敛范式（2026-07 · ≥3 家共有 = table stakes）

| # | 范式 | 谁在做（官方文档背书） |
|---|---|---|
| T1 | **常驻任务面板 / mission control**：跨会话状态总览（运行中/待批/已完成）+ 状态过滤 + 归档 + 直达产物 | Claude Code 侧栏(状态/项目过滤+归档)、Copilot Agents panel + repo Agents tab、Codex Web Tasks/Archive tab、Cursor 2.0 agent 侧栏、Devin 会话列表(状态标签+橙点未读+文件夹+favicon 状态点) |
| T2 | **session log 三件套**：推理叙事 + 工具调用分组折叠 + 内联 diff | 全部产品；Copilot Agents tab 明确"相似工具调用分组折叠+内联预览" |
| T3 | **transcript 密度分档**（多会话并行时用 Summary 扫结果） | Claude Code Normal/Verbose/Summary(Ctrl+O)、VS Code 折叠块 |
| T4 | **steer 统一语义**：输入在当前工具调用/turn 边界生效 + Stop 保留已完成工作 + pause | Claude Code、Codex(Enter 立即/Tab 排队·2026-02 默认开)、Copilot mission control、Jules(chat steer+显式 Pause)、Cursor 队列可重排 |
| T5 | **评论即指令**：diff 行级评论直接回喂模型改代码 | Claude Code desktop(行评→读评→新 diff)、Codex desktop(thread 内 diff 逐行评论)、Copilot(Files changed 评论即 steer)、Devin Review(评论→commit) |
| T6 | **AI 自审 diff 按钮** | Claude Code "Review code"(评论直接留在 diff 视图)、@codex review、Cursor Bugbot+2.1 评审侧栏、Devin Review |
| T7 | **checkpoint/rewind 含文件回滚**（对话/文件/两者三选） | Claude Code CLI+VS Code(4 动作/3 动作 fork)、Cursor Checkpoints restore、Cline 影子 git 三恢复、OpenCode `/undo`+session snapshots |
| T8 | **计划审批/编辑一等 UI**：计划可编辑/评论后再放行，反问澄清问题 | Jules Interactive Plan(审批/编辑/拒绝)、VS Code(计划=md 可行内评论)、ultraplan 重评审、Devin Interactive Planning+Agency(30s 无反馈自动执行)、Cursor 2.1 澄清问题+2.2 计划任务派生子 agent |
| T9 | **通知分层**：OS 通知（"完成且未在看时才发"）+ 状态圆点（favicon/图标） + 移动推送 | Claude Code(OS 通知+VS Code spark 圆点)、Crush(仅终端失焦时)、OpenCode attention 系统(通知+音效)、Devin favicon 绿/橙点、Cursor 2.2 系统通知、Copilot/Codex 移动推送 |
| T10 | **用量常驻可见**：usage ring / context indicator + 归因 | Claude Code usage ring+VS Code `/usage` 归因表、Copilot session log 显 token+时长、Devin ACU 图表、Cursor 聊天内 usage summary |
| T11 | **权限具名档位 + 网络默认关/防火墙 + allowlist** | Codex Auto/ReadOnly/FullAccess+云任务 agent 阶段默认断网、Cursor 三 Run Mode+Seatbelt 沙箱默认无网、Copilot 防火墙默认开+allowlist UI、Claude Code 五档 |
| T12 | **并行任务 worktree 隔离** | Claude Code(每会话自动 worktree)、Codex desktop(每 agent 一个 worktree)、Cursor(8 并行靠 worktree/远程机) |
| T13 | **移动/远程 = 控制面**（批准/steer/通知，非编辑器） | Claude Code Remote Control、Codex mobile+Codex Remote(QR 遥控)、Copilot mobile 实时推送、Cursor iOS+Live Activities、Devin PWA+Slack |
| T14 | **定时任务/Automations → review queue** | Codex Automations、Cursor Automations(3.8)、Jules Scheduled Tasks、Manus Scheduled 2.0、Claude Code Routines |
| T15 | **记忆管理 UI**（记忆需用户批准/可管理） | Cursor Memories(批准 UI)、Devin Knowledge(建/改/删/归档+建议审核)、Jules 仓库级 Memory |

**前沿差异化**（个别产品，代表方向，选择性跟进）：best-of-N 多路尝试+自动评审（Codex `--attempts N`、Cursor Multi-Agent Judging、GitHub Agent HQ 同题派三家 agent 对比）；cloud↔local handoff/teleport（Claude Code、Codex、Cursor 3.7）；审批请求先过 reviewer agent（Codex `approvals_reviewer=auto_review`）；多 agent 指挥中心（Devin Desktop Kanban+Spaces+ACP 接第三方 agent）；浏览器内自动验证（Claude Code autoVerify 每次编辑后自动截图/查 DOM/自修，Codex 内置浏览器+computer use，Cursor 内嵌浏览器/Design Mode）；会话分享/回放（OpenCode `/share`、Manus replay、Claude Code web 分享）；单任务 fan-out 100+ 子实例（Manus Wide Research）。

## 2. Studio 现状底座（2026-07-16 测绘结论）

**已对齐主流、不再是差距**（文件指针为证）：

- 叙事流+流式 tail+自动跟随+跳到最新（`Thread.tsx:218-247,392-401`）；60 turn 窗口化+加载更早（`Thread.tsx:39,266`）
- 工具卡内联 stdout/stderr+折叠（`ToolCallCard.tsx:24-44`）；工具簇折叠（`ConversationTurn.tsx:36-57`）→ **T2 达标**
- **三档密度 focus/normal/verbose**（`useViewMode.ts:3-9`）→ **T3 达标**
- mid-run steer 默认 ON（`server.mjs:711-753,834`·1.2.76）+排队持久化（`composerQueue.ts`）+side-ask（`SideChatPanel.tsx`）+pause/stop（`Composer.tsx:434-456`）→ **T4 达标**
- 内联 diff+聚合 chip（`InlineFileDiff.tsx`/`AggregateDiffChip.tsx`）+逐文件 review/stage/discard（`DiffReviewPane.tsx`）+每轮 Keep/Revert（`App.tsx:243-263`）
- plan/todo 清单+phase 条（`PlanChecklist.tsx`+`planModel.ts:52-63`+`PhaseStrip.tsx`，优先模型 `model_todos`）
- seq 游标无缝重连+幂等回填（`api.ts:217-254`）；SSE→轮询兜底+重连指示（`api.ts:200-322`+`MissionPaneHeader.tsx:100-112`）
- 权限卡风险分级+scope 预览（`PermissionCard.tsx:112-149`，读运行时 `permission_preview` 不客户端推断）；模式+权限档双下拉（`Composer.tsx:401-430`）
- 专家/子代理树：lineage+深度缩进+每专家 token+merge-gate 对账（`SubagentPanel.tsx`·1.2.57）
- Inspector 5 标签：预览(静态服务器+热重载+**dev server 反向代理**·`preview-server.mjs:69-90`)/改动/上下文预算快照(`ContextPanel.tsx:101`)/子 agent/证据(含 filter 搜索·`EvidenceExplorer.tsx:705,819`)
- 命令面板 Ctrl+K（`CommandPalette.tsx`）+快捷键体系（`useStudioKeyboard.ts`）；turn rewind 入口（`TurnRewindButton.tsx`，**不含文件回滚**）
- @文件引用+斜杠命令+自增长 textarea（`Composer.tsx:16,60-70,174-179`）；软删+Undo+导出/导入（`SessionList.tsx:63-86,233-243`）
- 主题 system/light/dark 防闪烁（`useTheme.ts`）；错误友好化+分类徽章+exit-reason 可行动文案（`friendly-error.mjs`/`RuntimeSnapshot.tsx:392-419`）
- 工程质量：ESLint9+Prettier+vitest 18+hermetic smoke 33+Playwright 2 全进 CI（`verify.yml:69-94`）

**2026-07-16 当晚用户目视复核抓到的审计盲区（已修·1.2.80）**：本审计的差距矩阵漏了「主体内容渲染质量」这一档——交互构件齐了，但答案本体的排版低主流一档。用户一句"主体内容的展示跟其他家还是有差距啊。还有排版"引出三处硬伤，当晚修复：①`splitLeadAndDetails` 把 ≥2 标题的模型答案**截肢**（首节之外全部塞进折叠的「运行详情」）→ 改 harness 尾巴标题白名单折叠，模型正文永不截肢；②自研 Markdown 行解析器假列表（`<p>` 冒充 ol/li）、无 hr/blockquote/斜体、标题全拍平 h3/h4 → 换 `react-markdown + remark-gfm`（真 GFM 结构，安全姿态不变：不渲染原始 HTML + 链接协议消毒）；③量表倒挂（h3=11px 比正文 14px 小、段落 margin 0 糊墙、行内 code 11px）→ 重建 em 基准量表（答案面 15px/1.7、h1 1.25em>h2 1.15em>h3 1.05em、0.7em 块节奏）。教训：**能力矩阵审计（有没有）抓不到渲染质量差距（好不好），后者必须真数据目视**。同晚第二轮目视再收四刀（1.2.81）：④会话列宽屏贴左→居中 768px 阅读列（含 Composer 对齐）；⑤「跳到最新」文字胶囊→主流圆形图标浮钮（顺修 smooth 滚动在后台文档静默失效=复发 [[thread-autofollow-scroll-fix]] 同款 bug class）；⑥顶部标题是乱码守卫的**第三个泄漏点**（header 直用 raw title·顺修守卫对 undefined 崩溃）；⑦Preview 标签跨会话泄漏别的会话产物（贪吃蛇出现在算法对话旁）→按本会话真实触碰的文件作用域化。第三轮（1.2.82）：⑧**轮内叙事顺序回正**——1.2.64 的 answer-first 把答案提到过程卡前面且错误声称这是主流做法；实际主流三家全是"用户消息→过程（安静折叠）→答案收尾"，答案永远是紧贴下一条用户消息的终块；已交换渲染序+顺序锁测试（该顺序历史上翻转过两次）。

**原路线遗留 ☐ 对账（诚实纠偏——多数其实已落地）**：I8 seq 游标 ✅（`api.ts:217-254`）；INS-2b 证据搜索 ✅（`EvidenceExplorer.tsx:705`）；INS-3b 子代理监控 ✅（`SubagentPanel`·1.2.57）；INS-3c 步骤 telemetry ◐（模型调用/路由时间线/每专家 token 已有，per-step latency 未拆）；PREVIEW-3 dev server 代理 ✅（`preview-server.mjs`）。**真未做的收编为**：PREVIEW-4→G10、INS-4→G11、INS-5 文件回滚→G7。另保留两条证据门控跟踪：PR2（失败/空跑边界诚实化，需失败 run 复现）、PR5 余项（自动标题/并排+行号 diff/真虚拟化，需真 friction）。

**我们有而主流普遍没有的强项（不稀释、留在 Inspector）**：promotion/merge-gate 谱系、验证矩阵、上下文预算快照（去重节省/压缩建议）、全链诚实闸（est. 标注/无定价不显 $/未发生不渲染）。

## 3. 差距矩阵

> 档位：**P0**=table stakes 缺失，直接决定"安静可信赖的多任务工具"体感；**P1**=有但弱/纯接线；**P2**=前沿或需 runtime 配套，证据/DecisionPoint 驱动；**不做**=定位裁决。

| # | 差距 | 主流证据 | 现状 | 档位 |
|---|---|---|---|---|
| G1 | ~~通知体系缺失~~ **✅ 已落地（1.2.83）**：OS 通知（settled/waiting 变迁×document.hidden×权限三重闸）+ favicon 三态点 + 设置开关 | T9 全家；规则收敛为"完成且未在看时才通知" | `useNotifications.ts` | ~~P0~~ 完成 |
| G2 | ~~用量不常驻~~ **✅ 已落地（1.2.83）**：header 常驻环（ratio 才渲染·est. 照标·压力分级变色）点击直达上下文标签 | T10；Claude Code usage ring 在模型选择器旁 | `ContextRing.tsx` | ~~P0~~ 完成 |
| G3 | ~~多会话监控无地基~~ **✅ 已落地（1.2.83）**：BFF 投影每会话 run_status+侧栏点泛化（运行绿/刚完成橙/刚失败红）+已归档 tab+逐行归档+7s 轻轮询；置顶未做 | T1 全家 | `decorateSessionRunStatus` + `SessionList` | ~~P0~~ 完成 |
| G4 | ~~diff 行级评论回喂缺失~~ **✅ 已落地（1.2.84）**：行 hover 评论→托盘批量提交→既有通道（运行中 steer/空闲新 turn·失败不清空）；真栈验模型逐条精确执行 | T5 四家 | `diffComments.ts` + `DiffCommentTray` | ~~P0~~ 完成 |
| G5 | ~~AI 自审未做成一等按钮~~ **✅ 已落地（1.2.84）**：改动面板一键自审（chat 只读 turn 带 diff·截断如实声明），结论按 `文件:行` 锚回 diff 视图（徽章+行级发现卡）；注：原文"runtime review 纯接线"部分过时——该命令是运行完成度评审无文件锚，按主流语义走消息通道 | T6 四家 | `aiReview.ts` + 改动面板 | ~~P1~~ 完成 |
| G6 | ~~计划只读~~ **刀一 ✅ 已落地（1.2.85）**：每步评论入口→托盘批量回喂（同 G4 通道）；刀二（真编辑式改计划）待 DecisionPoint | T8 五家 | `planComments.ts` + PlanChecklist | ~~P1~~ 刀一完成 |
| G7 | ~~rewind 无文件回滚~~ **✅ 已落地（1.2.89）**：turn 影子快照（临时索引真 commit·暂存区/HEAD 零扰动·.asteria 双向排除）+ diff 预览确认 + 恢复前自动安全快照可反悔；非 git 诚实降级 | T7 四家（全部回滚文件） | `git.mjs` 快照三函数 + TurnRewindButton | ~~P1~~ 完成 |
| G8 | ~~选择器只读~~ **G8-a 档位选择器 ✅ 1.2.104**；G8-b 具体模型覆盖仍缺 | Crush Ctrl+L 中途换模型；Cursor per-mode 模型 | `SettingsPanel.tsx` ModelSection·`lib/run-flags.mjs` | P1 |
| G9 | ~~排队消息不可编辑/重排~~ **✅ 已落地（1.2.83）**：点击放回输入框编辑+上移/下移（逐条删除本就存在——审计时"只有整队清除"系误判，已诚实对账） | Cursor Queued Messages 可重排；Claude Code 用户抱怨同款缺陷(#71726)——已超越参照物 | `Composer.tsx` 队列 chips | ~~P1~~ 完成 |
| G10 | ~~预览缺控制台~~ **✅ 已落地（1.2.88）**：注入客户端→postMessage→底部错误条（head 注入抓解析期错误）+ 375/768/1280 宽度预设 + 外开浏览器；proxy 模式不注入（dev server 自有浮层） | Claude Code Browser 窗格、Cursor 内嵌浏览器 | `previewConsole.ts` + PreviewPane | ~~P1~~ 完成 |
| G11 | ~~证据可见性~~ **✅ 已落地（1.2.86）**：组级折叠+计数、"显示全部 N 条"取代静默 slice、过滤搜全量、run 文件内联预览（大文件中文降级） | LangSmith 式分组/展开是 power-user 标配 | `EvidenceBlock`（模块级） | ~~P1~~ 完成 |
| G20 | ~~代码块无语法高亮~~ **✅ 已落地（1.2.83）**：rehype-highlight+主题 token 双色板（`--code-*`·GitHub 校准色·detect 关闭只染显式标注） | ChatGPT/Claude/Cursor 代码块全部着色 | `MarkdownBody.tsx` + tokens.css | ~~P1~~ 完成 |
| G12 | 定时任务/Automations→review queue | T14 五家 | runtime 无 scheduler | P2 |
| G13 | best-of-N 多路尝试+自动评审 | Codex/Cursor/Agent HQ | runtime candidate/merge 是现成底座，无 N 路语义 | P2 |
| G14 | ~~记忆管理 UI~~ **只读起步 ✅ 已落地（1.2.91）**：侧面板"记忆"标签渲染 active_goal 记忆（结构化卡+原文折叠+溯源）；编辑语义随记忆分叉 DecisionPoint 再议 | T15 三家 | `MemoryPanel.tsx` | ~~P2~~ 只读完成 |
| G15 | ~~会话分享/回放导出~~ **✅ 已落地（1.2.90）**：自包含 HTML 回放页（零外部请求·真攻击串验证转义·主线程范围诚实标注）+ 侧栏导出链接 | OpenCode share、Manus replay | `session-replay.mjs` + /export.html | ~~P2~~ 完成 |
| G16 | LAN/移动控制面（PWA+局域网） | T13 全家 | 仅 localhost | P2 |
| G17 | 图片/附件粘贴 — **⚠️ 2026-07-17 原「通道未就绪·诚实搁置」记录经活体探针证伪，已撤回**：①「`content: str` 不支持 parts 数组／改它须动 openai_compatible 序列化」**是错的**——该标注是裸标注（frozen dataclass 无 validator，出站路径零 isinstance），实测 `ChatMessage(content=[parts])` 零改动穿 `to_payload`→`_payload`→`json.dumps` 直达 provider，glm-4v-flash @ `/api/paas/v4` 正确读出图中数字；②「零视觉路由」属实且**是唯一真架构缺口**——实测配置中的 coding 端点（glm-5 @ `/api/coding/paas/v4`）拒图：`HTTP 400 code 1210 messages.content.type 不在合法取值范围 ['text']`，故必须有视觉档路由，不能往默认档发图；③前端 onPaste 属实未做。**刀一（CLI）✅ 已落地（1.2.96）**：`asteria chat --image` 端到端真跑读出图中数字；视觉档 `create_vision_client`（不进 `MODEL_TIERS` 成本轴·照 `create_recap_client` 形状）+ 图片消息插在信封前 + `context_budget` 多模态分流 + 未配档诚实拒答；顺修 chat 在有 current run 时必炸的既有 bug（`context_mode` 枚举漏 `chat`·c7c16a9）。**刀二（Studio 粘贴）✅ 已落地（1.2.98）**：onPaste→独立上传端点（不经 redactText 漏斗）→`.asteria/attachments/<session>/<hash>`→消息只带路径→主线程回显缩略图（走主服务器 GET，不依赖 preview-server 端口）；带图消息强制走 chat 并说明理由（run/plan 无图片通道，送去 run=丢图让模型半盲干活）；内容嗅探不信文件名+路径往返重校验。**G17 全案收官**。真跑得到的约束：视觉档窗口小（glm-4v-flash 仅 16384，chat 信封 ≈20k 装不下 → 须 glm-4.5v）且推理慢（需加长超时） | Codex CLI 贴图、Claude Code、Cursor | 刀二：前端 onPaste/上传/引用渲染 | ~~P2·待通道~~ **完成** |
| G18 | 成本金额：只报 token 无货币 | T10（credits/ACU/$） | 有意留白（无定价表·诚实闸）；**2026-07-17 判定「不做·等证据」**——设计/实现都不难，卡在「无真实 friction 证据的 Studio 新功能」冻结项 + P2 的证据门；零证据下动手=拿猜测撞规矩。拉起条件见 §4 | P2·等证据 |
| G19 | 每会话 worktree 隔离未暴露为产品语义 | T12 三家 | runtime 有 candidate 隔离，Studio 无"同仓并行多会话"表达 | P2 |
| — | GitHub/云 PR 集成（draft PR 流、@agent 评论触发） | Copilot/Codex/Jules | 本地优先内部引擎定位；git 分支产物已有 | 不做 |
| — | i18n 英文界面 | 主流英文优先 | 中文优先是定位不是差距 | 不做 |
| — | 语音输入、云 VM 桌面可视化、Slack/IM 集成 | 个别产品 | 无云 VM；IM 内网可后议 | 不做 |

## 4. 实施思路（observed_pattern → 落点 → 思路 → 验收 → 不抄什么）

### G1 通知体系（S）
- **模式**：Claude Code"会话完成任务且你不在看该会话"才发 OS 通知；Crush 仅终端失焦时提醒；Devin favicon 绿/橙点；OpenCode attention（通知+音效，问题/权限/完成三类）。
- **落点**：新 `hooks/useNotifications.ts` + `SettingsPanel` 开关分区；favicon 用 canvas 重绘。
- **思路**：触发源=已有 settled 信号（completed/failed）与 waiting/permission_request 事件（F1·1.2.54 已诚实化）；规则=`document.hidden || 非当前会话` 才发 `Notification`（localhost 是 secure context，API 直接可用）；favicon 点三态（运行=蓝、待决策/waiting=橙、失败=红）；声音默认关。首次开启走浏览器权限请求，拒绝则诚实降级为 favicon 点。
- **验收**：后台标签页跑 run→完成时收到 OS 通知+favicon 变点；前台盯着看时**不**发；权限请求事件发"需要你决策"；设置可全关。
- **不抄**：移动推送（无云中继，不伪造"离线可达"）。

### G2 常驻 usage ring（S）
- **模式**：Claude Code 模型选择器旁 usage ring，点开看会话上下文占用；VS Code 提示框内建 context indicator。
- **落点**：`Composer` 或 `MissionPaneHeader` 加小环；复用 `readContextUsage` helper 与 ContextPanel 数据。
- **思路**：环显 context 占用百分比（数据已在 cost_report/context 快照事件里）；点击展开既有 ContextPanel；无数据不渲染（派生而非杜撰）；`est.` 照标。
- **验收**：运行中环实时推进；无 cost_report 的会话不显示；点击直达 Inspector 上下文标签。
- **不抄**：订阅额度环（无订阅体系）；美元（无定价，G18 另议）。

### G3 会话状态徽章+归档+过滤（M）——mission control 地基
- **模式**：Claude Code 侧栏按状态/项目过滤+hover 归档；Devin 侧栏持久状态标签+橙点未读；Copilot Agents tab 状态一览。
- **落点**：`SessionList.tsx`/`sessionListUtils.ts`/`server.mjs` sessions 元数据。
- **思路**：①逐会话状态点——BFF 已有 liveJobs 与 session 事件，把"每个会话最新 run 状态"（running/waiting/failed/idle）投影进 `/api/sessions` 列表响应，侧栏渲染彩点（活动会话现有 `sessionLiveDot` 泛化）；②未读=会话在后台期间新增 settled/waiting 事件→橙点，进入即清；③归档=软删机制旁加 `archived` 状态+过滤 tab（沿用 soft-delete 的还原路径）；④过滤 tab 加"运行中"。**先做侧栏点+过滤，不做独立 Kanban 总览页**（Devin Desktop 级别属 P2，等真并行量证据）。
- **验收**：两个会话并行跑，侧栏同时显示两个运行点；后台会话完成→橙色未读点；归档后默认列表不显示、过滤可找回。
- **不抄**：PR 合并自动归档（无 PR 流）。

### G4 diff 行级评论回喂（M）——评论即指令 ✅ 已落地（1.2.84·3ec1895）
> 验收全达成：两条行评提交→glm 真跑逐条精确执行→新 diff 可见；运行中走 steer 且按钮如实标"提交·下一轮生效"；投递失败不清空评论。
- **模式**：Claude Code desktop 点任意行开评论框、多行评论 Cmd+Enter 一次提交、Claude 读评论产新 diff；Copilot"在 Files changed 里评论即 steer"。
- **落点**：`DiffReviewPane.tsx`/`InlineFileDiff.tsx` 行 hover 加评论入口；新 `diffComments.ts` 聚集评论。
- **思路**：评论=结构化引用（`file:line` + hunk 摘录 + 用户文字），聚集后一键提交→组装成一条用户消息走**既有通道**（运行中=mid-run steer/排队，空闲=新 turn），零新后端协议；模型天然读到"针对 X 文件第 N 行的意见"。回包后该文件 diff 刷新即闭环。
- **验收**：对运行产物 diff 两行各写评论→提交→模型下一轮按评论改动→新 diff 可见；运行中提交走 steer 队列并在 UI 如实标注生效时机（turn 边界）。
- **不抄**：GitHub 式评论 thread/resolve 状态机（第一刀=单向喂，不做评论生命周期管理）。

### G5 AI 自审按钮（S·纯接线）✅ 已落地（1.2.84·e272f4a）
> 落地时纠偏：原判"runtime `review` 已有=纯接线"部分过时——`asteria review` 是运行完成度评审（verdict/score/blockers，无文件锚），主流 "Review code" 语义=模型评审 diff 本体，故走消息通道（chat 只读 turn 带 diff，复用 G4 思路）。真栈验：埋除零 bug→"calc.py:19 div 未防护 b==0" 精确挂行。clean 路径（"未发现高信号问题"）单测锁死未真跑。
- **模式**：Claude Code 工具栏"Review code"让 Claude 评审当前 diff、评论直接留在 diff 视图（只报高信号问题）；@codex review。
- **落点**：`DiffReviewPane` 工具栏加按钮 → BFF 已有 runtime `review` 通道。
- **思路**：按钮触发 review run（KEEP_CORE 能力早在），结论按文件锚内联到 diff 视图（复用 G4 的行锚渲染）；review 是只读动作，无权限风险。
- **验收**：点按钮→review 真跑→发现的问题挂在对应文件区块；无问题时如实显示"未发现高信号问题"。
- **不抄**：Auto-fix/Auto-merge 开关（触自主环语义，须走 DecisionPoint）。

### G6 计划审批/编辑（M·部分触 runtime）——刀一 ✅ 已落地（1.2.85·e9acd91）
> 刀一（零后端）已落：每步评论入口+共享托盘分节合成一条消息回喂；真栈验"禁止 **/循环乘法/负指数 ValueError"三约束执行阶段全遵守。刀二（真编辑式改计划）仍待 DecisionPoint。
- **模式**：Jules 计划可审批/编辑/拒绝；VS Code 计划=Markdown 可行内评论后批准；Devin Agency+30 秒无反馈自动执行；Cursor 计划前先弹澄清问题。
- **思路**：分两刀。**刀一（零后端）**：PlanChecklist 各步加评论入口，聚合为一条结构化消息回喂（同 G4 通道）——"对计划第 2 步：不要动 schema"这类意见在 plan→execute 间隙天然生效。**刀二（触 runtime）**：真编辑式改计划（增删步骤后 planner 重排产物）需要 plan-revision 接口，**先立 DecisionPoint**——与 ADR-0016 相容的形态是"用户意见作为输入喂给模型重出计划"，而非 harness 直改 task_plan。
- **验收**：刀一——plan 模式产出后对某步写意见→执行阶段模型可见并遵守；刀二以 DecisionPoint 结论为准。
- **不抄**：30 秒自动继续默认值（我们的权限档已有更明确的语义，不引入第二套计时器）。
- **两份计划的真实语义（2026-07-17 代码核实·1.2.101）**——刀二若要动手，先认清编哪份意味着什么：
  - UI 的"第 N 步"**优先渲染 `model_todos.json`**，`task_plan.json` 仅兜底（`planModel.ts:51-63`，与 runtime
    `todo_read` 同一优先级 `todo_tools.py:82-102`——UI 没有自创第二套语义）。
  - `model_todos.json` = **模型的工作记忆**：唯一写入者 `todo_tools.py:56-58`（TodoWriteTool）；`todo_read` 每次
    **从磁盘重读**（无缓存）且是 `allow` 权限的注册工具（`defaults.py:22`·`agent_tool_surface.py:144-151`）。
    ⇒ 编辑它**不是空动作**，是真写进工作记忆；但 `context_loader.py` **不读它**（无每轮自动注入），
    所以只在模型主动 `todo_read` 时才被看见——**建议性、可能不被读回**。
  - `task_plan.json` = **调度原语**：`TaskBoard`（`execute_command.py:308`）驱动执行，`ready_tasks()`
    （`task_board.py:122`）决定跑哪个任务。改它=改 agent 接下来真去做什么。
  - 两者关系：**单向、读时、不落盘**派生（model_todos 缺失时 `todo_read` 用 task_plan 合成·`todo_tools.py:105-145`）；
    `todo_write` 从不回写 task_plan。会漂移，无同步机制。
  - ⇒ 结论：**刀二的卡点不是"不知该编哪份"**（语义清楚），而是"要不要一个与 ADR-0016 张力的直编辑面"——
    相容形态已由刀一实现。这是**产品决定**，交用户，不由本文档单方判。

### G7 checkpoint 文件回滚（L·依 git）✅ 已落地（1.2.89·106baba）
> 全部验收达成：改两轮→rewind 选"回滚文件"→工作区真回去且 diff 预览准确（真栈 UI E2E）；非 git 显示降级说明。落地附加：恢复前自动安全快照=回滚可反悔；快照锚按轮时间窗从原始事件解析（narrative 剔除 pointer-only final 的坑）；顺手修合并排序时区错序真 bug。
- **模式**：Claude Code 每 prompt 自动 checkpoint，rewind 四动作（对话+代码/只对话/只代码/摘要压缩）；Cline 影子 git 三恢复；OpenCode `/undo` 用 git 实现。
- **落点**：BFF `git.mjs`（影子 ref 管理）+ `turnRewind.ts`（选项扩展）。
- **思路**：turn 结束时用 `git stash create` 式影子 commit（不动索引/HEAD、不产生用户可见分支）把工作区快照挂到 session 事件；rewind 菜单从"从这里接着做/重做"扩为主流三选（含"同时回滚文件"）；回滚前显示与当前工作区的 diff 并确认（防吃掉用户手改）。非 git 工作区诚实降级为只回对话（如实文案）。
- **验收**：改两轮→rewind 到第一轮选"回滚文件"→工作区文件真回去且 diff 预览准确；非 git 工作区显示降级说明。
- **不抄**：30 天自动清理策略（本地磁盘，先不做生命周期管理）；不声称能回滚 shell 副作用（主流同样不追踪，界面如实注明——诚实闸）。

### G8 模型/提供方选择器（M·两份真源风险）
**用户 2026-07-17 拍板：两者都要**（档位默认 + 可覆盖具体模型）⇒ 分两刀。
**G8-a 档位选择器 ✅ 已落地**（1.2.104·brief `S83-model-strategy-selector.md`）：走上面结论里的**路径 (a)
= 活通道**。设置面板 ModelSection 从只读升为可选（四档 auto/quality/economy/local），BFF 冷路
`--model-strategy` flag / 暖路 `model_strategy` JSON 字段**同源同输入**（`lib/run-flags.mjs`）。
验收全中：真实用户路径探针证 `run_config.json` 落 `model_strategy:"economy"`（冷/暖两路各验一次，
暖路另经插桩直取 worker 收到的请求——**结果相同 ≠ 走了暖路**，回落冷路会给出一样的 run_config）；
`resume 不丢` 由构造保证（`execute_command.py:282` 从磁盘读 run_config，不经 CLI 默认值）。
**G8-b 具体 provider/model 覆盖 = 仍开着**，就是下面 (a)/(b) 里没做的那部分——见本节末尾。
- **模式**：Crush Ctrl+L 中途换模型保留上下文；Cursor per-mode 绑定模型。
- **思路**：SettingsPanel 模型分区从只读升为可选（写入 run_config 已有配置通道，经 BFF 传 runtime）；先做 per-run 模型档选择（strong/cheap 路由映射），中途换模型待 runtime 路由证据。⚠️ 改前先确认读写同一份配置（[[permission-mode-two-sources-of-truth]] 教训：resume 读错份就会吃默认值）。
- **验收**：设置里选 provider/档位→新 run 的 model_calls 证据里真用了所选模型；resume 不丢所选。
- **配置通道的真实形状（2026-07-17 代码核实·1.2.101）**——**"缺 run_config 字段"是假的**，别照那个方向做：
  - `model_routing_overrides` 是 run_config schema 的 **required** 字段，`build_run_config` 真写
    （`run_config.py:41`），`apply_run_config` 真 merge 进 `policy["model_routing"]`（`:99-100`）。通道**已通**。
  - `_model_routing_overrides()` 的 `return {}`（`:116-122`）是**有注释的刻意默认**，不是待填的坑：
    "别在 run 创建时把所有模型路由一把钉死，让 RuntimeProfileBuilder 综合任务形状/风险/能力反馈/预算决定"。
  - **真缺口在下一层**：`policy["model_routing"]` **零生产消费者**——全 `src/` 对 `model_routing` 只有
    `run_config.py:99-100` 写它的那两行，没有任何人读。填了它，什么都不会发生（**空动作**）。
    （测试夹具里的 `"model_routing": {...}` 是 policy 形状装饰，不是消费者。）
  - **活的路由通道**是另一条：`model_strategy` → `model_strategy_profile` → `RuntimeProfileBuilder`
    （真消费：`runtime_profile_builder.py:162`/`:401`/`:434`）。
  - ⇒ 做 G8 先定路径：**(a)** 接进活通道（扩 `model_strategy_profile`，改动小、与既有 tier 决策相容），
    还是 **(b)** 给 `model_routing` 补消费者（真 per-purpose 路由表，能力更强但要处理它与 tier 决策的优先级）。
- **G8-b 的选路（尚未定·下一刀先定它再动手）**：G8-a 已把 (a) 的**档位**部分吃掉了，但"覆盖**具体模型**"
  是 (a) 覆盖不到的——`model_strategy_profile` 表达的是**偏好**，不是"就用这个模型"。所以 G8-b 要么
  **(b) 给 `model_routing` 补消费者**（并推翻 `_model_routing_overrides:116-122` 的设计注释 ⇒ **须记 ADR**），
  要么**窄扩 (a)**（只让"钉死某个 purpose 的模型"这一种覆盖穿过去，保留 RuntimeProfileBuilder 对其余的
  决策权 ⇒ 与那条注释不冲突）。**真设计活，不是接线活**——别当成 G8-a 的续接线。

### G9 队列逐条编辑/重排（S）
Cursor 可重排、Claude Code 用户抱怨不可编辑（做了即超越参照物）。`composerQueue.ts` 加逐条删除/编辑/上下移，纯前端+已有持久化。验收：排 3 条→删 1 改 1 换序→turn 边界按新序 flush。

### G10 预览控制台+设备切换（S–M·原 PREVIEW-4）✅ 已落地（1.2.88·6f37123）
注入脚本转发 `console.error/window.onerror` 到已有 `/__livereload` SSE 同通道→PreviewPane 底部错误条；容器宽度预设（375/768/1280）做响应式切换；"在浏览器打开"按钮直开预览 URL。验收：页面 JS 报错→预览内可见错误条；切设备宽度渲染变化。
> 落地偏差（更直）：错误通道走 iframe→宿主 postMessage 而非 SSE 回环（零服务器往返·VS Code Live Preview 同款）；注入点提到 `<head>` 开头抓解析期错误；真栈验双错误恰 2 条挂条。顺手修 file_changed artifact_refs 形状漏提取 + 会话切换预览不重选两个真 bug。

### G11 证据分组/批量展开（S·原 INS-4）✅ 已落地（1.2.86·a69934f）
证据列表按类分组（validations/runs/routes/artifacts）+组级展开；`slice(0,6)`→"显示全部 N 条"；小 JSON/diff artifact 内联预览、大文件保持链接。已有 filter（`EvidenceExplorer.tsx:705`）之上补齐。
> 落地附加：过滤改搜组内全量（旧代码只搜预切尾巴=诚实性 bug）；BFF 英文拒绝串本地化。

### P2 各项（一句话思路，证据/DecisionPoint 驱动）
- **G12 定时任务**：需 runtime scheduler+触发器语义，先 ADR（对齐 Codex Automations"结果落 review queue 待人审"，不越过审批门）。
- **G13 best-of-N**：runtime candidate/merge 是现成底座；N 路尝试+评审 agent 选优是 ADR-0022 专家集群的自然延伸，触编排冻结项，先 DecisionPoint。
- **G14 记忆管理 UI**：Inspector 加"记忆"标签读 `.asteria/memory`（active_goal 双写+recovery journal 刚审计完·1.2.77/78），只读起步；编辑/批准语义与"harness 确定性抽取"现状有张力，随记忆分叉一起议。
- **G15 会话回放**：导出已有 JSON→自包含 HTML 回放页（零服务器依赖，适合内网转发复盘）。✅ 已落地（1.2.90·见上表）。
- **G16 LAN/PWA**：server.mjs 绑定 0.0.0.0+manifest.json+移动断点；内网团队"手机看进度/批权限"契合定位，安全边界（无鉴权）先议。
- **G17 图片粘贴**：Composer onPaste→BFF 存 workspace→消息带引用；依 provider 多模态能力（glm-4v）。**2026-07-17 探针已核实完毕**（见 §3 表 G17 行）：通道零改动即通、glm 视觉档真能读图、但配置中的 coding 端点拒图 → 真缺口只剩「视觉路由档」+ 前端。存图/回显不必新建通道：`preview-server.mjs` 已是现成图片静态服务（无扩展名白名单、二进制读、完整图片 MIME 表），`.asteria/attachments/` 可直接落盘（`isSafeWorkspacePath` 不拦 `.asteria/`，`MemoryPanel` 已有直读先例，且被 `fileChanges.ts` 有意排除出 Keep/Revert）。施工顺序：先 CLI 端到端一刀（`asteria chat --image` 真跑）证纵深，再前端。**未验证**：minimax 视觉能力、连续同角色消息 vs 单条 parts 数组的兼容性。
- **G18 成本金额**：可配置定价表（内部各 provider 结算价），无配置保持只报 token（诚实闸不破）。
  **2026-07-17 自驱循环排到它时判定「不做·等证据」**（留痕免得下轮重新论证）：设计上没问题——opt-in 配置 +
  `est.` 标注不破诚实闸，实现也不难（token 用量已在 `model_calls.jsonl` + 成本归属 1.2.44）。**卡的是门不是活**：
  AGENTS 冻结项明写「无真实 friction 证据的 Studio 新功能」，本文 §5 也给 P2 设了「按真实 friction 证据拉起」。
  **当前零证据**——没有任何人提过想看金额。在这种状态下动手 = 拿自驱循环的猜测撞项目自己的规矩，
  正是 §0 硬约束第 5 条「派生而非杜撰」要防的。**拉起条件**：有人真的问「这次跑花了多少钱」/
  需要内部结算归因。届时按上面的设计直接做即可。
- **G19 并行会话 worktree**：把 runtime 的 candidate 隔离暴露为"同仓并行会话"产品语义；依真实并行量证据。

## 5. 排序与依赖

**Wave N1「安静可信赖」= G1+G2+G3+G9 ✅ 已全部落地（2026-07-17 · 1.2.83，附 G20）**——多任务并行监控是内部引擎日用体感的第一短板，也是本次对标最一致的 table stakes。遗留小项：置顶（G3 未做）、并行双绿点真跑端到端（投影+轮询已分层验证）、OS 弹窗本体（需真隐藏页+授权，判定逻辑已单测锁死）。
**Wave N2「评论即指令」= G4+G5 ✅ 已全部落地（2026-07-17 · 1.2.84）**——两者共用"结构化引用→既有消息通道"，零新后端协议兑现。遗留小项：G5 clean 路径未真跑（单测锁死）、自审轮次定位在 side-ask 极端交错下未覆盖（V1 注明）、评论提交在 steer 关闭+运行中=诚实禁用（未做后台排队）。
**Wave N3「计划与回滚」= G6刀一+G7+G10+G11 ✅ 已全部落地（2026-07-17 · 1.2.85–1.2.89，自驱循环四轮完成，外加 session.json 竞态修复 1.2.87）**。遗留：G6 刀二（真编辑式改计划）与 G8 各自待 DecisionPoint；重启后首选会话事件态空怪癖另案；快照 ref 无清理策略（有意，本地磁盘）。
**P2 波次不排期**：G12/G13 触 runtime/编排冻结项，须先 ADR/DecisionPoint；其余按真实 friction 证据拉起。

执行规范沿用原路线 §7：每迭代 brief→精确 Edit→`tsc`+`vite build`+相关 smoke+preview 冒烟→一迭代一 commit→回写本表状态；不推送直到用户明示。遵守 [[keep-docs-aligned-no-drift]]、[[frontend-must-mirror-mainstream]]、[[convergence-direction]]。

## 6. 与总计划的挂接

- 本文=研发总计划 §5（行业对标映射）的前端细化层；ACTIVE_SLICE 推进时逐波在 §16 changelog 记账。
- Wave N1–N3 属 Part B 前端轨的自然延续；不新开编排 Wave、不触任务批 disjoint 调度冻结。
- P0 沙箱、B 线主观视觉（需用户目视）与本路线并行不冲突。

## 7. 信源与时效

四路调研（2026-07-16，官方文档优先）主入口：Claude Code `code.claude.com/docs`（desktop/vs-code/checkpointing/ultraplan/remote-control/agent-teams）；OpenAI Codex `developers.openai.com/codex` + `learn.chatgpt.com/docs`（cloud/ide/agent-approvals-security/whats-new）；GitHub `github.blog` changelog（agents-panel/mission-control/agents-tab/AI-credits/Agent-HQ）；Cursor `cursor.com/changelog`（1.2–3.11）+ `docs.cursor.com`（checkpoints/run-modes）；Devin `docs.devin.ai/release-notes` + `cognition.com` 博客（devin-2/devin-2.2/manages-devins/devin-desktop）；Jules `jules.google/docs`；Manus `manus.im/blog`；OpenCode `opencode.ai/docs`；Crush `github.com/charmbracelet/crush`；Cline `docs.cline.bot`；Goose `block-goose.mintlify.app`。

**已知未核实/口径冲突**（引用前自查）：Claude Code desktop steer 的官方口径（"当前 action 完成即读取"）与用户 issue（排队到整 turn 结束且不可编辑）冲突，以真机为准；Claude Code desktop 是否有 rewind GUI 文档未载（倾向无）；Codex Web 云任务运行中能否 steer 未见官方明说；Manus 并发上限、Devin 定价数字来自第三方；Roo Code 停运消息未直接核实仓库状态。
