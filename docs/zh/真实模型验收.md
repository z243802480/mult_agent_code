# 真实模型验收

本文档记录真实模型 provider 的手动验收和定期验收流程。它和离线验证分开维护，因为真实验收会消耗 API 调用额度。

## 最新验证记录

2026-05-16 的真实模型灰度验证和 Runtime OS core gate 已经通过；2026-05-17 在 planned verification 兜底修复后完成完整 gray suite 复跑。Asteria 改名合入 `main` 后，同日再次用 `asteria` 入口复跑真实 gate 与完整 gray suite；Coordinator/WorkerInvocation 边界继续下沉后又完成真实 gate、完整 gray suite 和 core gate 复验，当前可作为小范围产品灰度前的最新 release-ready 基线。

- `asteria real-model-gate`：通过，耗时 111.258 秒；strong route 配置为 GLM coding 套餐，端点应使用 `https://open.bigmodel.cn/api/coding/paas/v4`，实际调用记录为 `zai/glm-5.1`；medium route 为 `minimax/MiniMax-M2.7`；最小 `/run` 完成，记录 4 次模型调用、2 次工具调用、1 条 WorkerResult 和 1 条 TaskExecutionEvidence。
- `asteria real-model-acceptance --suite gray`：2026-05-17 复跑通过，`gray_ready=true`；4 个场景全部通过：`gray_file_artifact`、`gray_multi_file_scope`、`gray_debug_repair`、`runtime_request_resume`。
- gray suite 最新汇总：耗时约 486.210 秒，18 次模型调用，37 次工具调用，4 次 repair attempt，3 次 context compaction；报告位置为 `.asteria/verification/real_model_acceptance_gray_after_planned_verification.json`。
- 安装包命令复验：2026-05-17 使用本地 wheel 安装后的 `asteria real-model-gate` 和 `asteria real-model-acceptance --suite gray` 通过；gray suite 4/4 通过、`gray_ready=true`，耗时约 419.173 秒，20 次模型调用、30 次工具调用，strong=`zai`、medium=`minimax` 均有 route evidence；证据目录为 `.asteria/verification/installed-real-gray-20260517-075310`。
- 安装包 gray/core 复验：2026-05-17 新 wheel 在干净 venv 和新 workspace 中执行 installed CLI：`version`、`init`、`doctor`、`real-model-gate`、完整 `real-model-acceptance --suite gray --python <venv-python>`、`acceptance --suite core --allow-fake`、`acceptance-gate --suite core --min-scenarios 10 --min-capabilities 10` 和 `gate-status --json` 全部通过；最终 `rollout_state=release_ready`、`release_ready=true`。证据目录为 `%TEMP%/asteria-installed-gray-core-20260517-190034`。
- 小真实任务灰度批次：同一 installed workspace 首轮运行暴露 `docs/gray_batch_note.md` 文档任务会在 runtime request 后被 repair 阶段的坏 Python 验证命令卡住；已 targeted 修复为 `.md/.txt/.rst` 文本产物按需稳定存在且非空验证，并重建 wheel。复测位置包括 `%TEMP%/asteria-doc-gray-20260517-225535`、`%TEMP%/asteria-doc-final-20260517-232958` 和 `%TEMP%/asteria-doc-final2-20260517-233934`：文档/文本只写任务可完成，文档 verification 老问题未复现；小 CLI 与 pytest 测试生成任务仍可能 paused，主要风险从文档 verification 转移到 artifact 创建不稳定、跨 run 依赖、测试验收范围漂移和 DebugAgent 非 JSON 输出。
- 小真实任务批次复验：2026-05-18 在源码路径用真实 GLM/MiniMax route 连续运行 3 个 `real-model-smoke` 小任务，覆盖既有文档更新、小 CLI 文件和 pytest 测试文件；证据目录为 `%TEMP%/asteria-small-gray-20260518-071935`。既有文档更新和 pytest 文件任务均 0 repair / 0 failure；小 CLI 初跑暴露单引号包裹 planned verification 未被抽取，导致 2 repair / 3 failure。修复 planned verification 抽取后复跑小 CLI，通过且 0 repair / 0 failure，证据目录为 `%TEMP%/asteria-small-cli-rerun-20260518-072501`。
- Asteria main 复验：2026-05-17 将改名迁移快进合入 `main` 后，`asteria real-model-gate` 再次通过，证据目录为 `.asteria/verification/main-real-gray-20260517-083102`；随后 `asteria real-model-acceptance --suite gray` 通过，4 个场景全部通过：`gray_file_artifact`、`gray_multi_file_scope`、`gray_debug_repair`、`runtime_request_resume`，证据目录为 `.asteria/verification/main-real-gray-suite-20260517-083223`。
- Runtime boundary 复验：2026-05-17 Coordinator/WorkerInvocation 边界继续下沉后，`python -m asteria_runtime real-model-gate` 通过，耗时约 233.089 秒，记录 8 次模型调用、6 次工具调用、3 条 WorkerResult 和 5 条 TaskExecutionEvidence，strong=`zai`、medium=`minimax` 均被调用，证据目录为 `.asteria/verification/runtime-boundary-real-gate-20260517-125203`。
- Runtime boundary gray suite：同日完整 `gray` suite 初跑暴露 `gray_file_artifact` 中 Planner 漏放安全只读 `list_files`，导致 DebugAgent 能创建候选文件但无法完成验证/晋升；修复 Planner allowed tools 后复跑 4/4 通过，`gray_ready=true`，耗时约 412.855 秒，15 次模型调用、32 次工具调用、3 次 repair attempt、3 次 context compaction，证据目录为 `.asteria/verification/runtime-boundary-gray-rerun-20260517-130738`。
- `asteria acceptance-gate --suite core --min-scenarios 10 --min-capabilities 10`：通过，`release status=ready`；core report 显示 10/10 场景通过，Runtime OS gate 覆盖 `runtime_parallel_readonly`、`runtime_disjoint_writes`、`runtime_worker_failure`、`runtime_merge_gate_block`、`runtime_request_resume`、`context_package_slice`、`sandbox_backend_selection`、`planner_scope_quality`、`capability_feedback`、`runtime_evidence_consumption`。
- Runtime boundary core gate：2026-05-17 边界下沉后运行 `python -m asteria_runtime /acceptance --suite core --allow-fake --root . --scenario-timeout-seconds 300`，10/10 通过；随后 `python -m asteria_runtime acceptance-gate --suite core --min-scenarios 10 --min-capabilities 10 --root .` 通过，`release status=ready`，报告位置为 `.asteria/acceptance/acceptance_report.json`。
- `asteria gate-status --json`：当前输出 `rollout_state=release_ready`、`release_ready=true`、`stage=ready_for_small_real_task_gray`，表示可以进入受控的小范围真实任务灰度；这不是长期无人值守运行许可。
- `asteria package-check --json`：通过，`asteria-runtime` 与 runtime 版本均为 `0.1.0`，console script 为 `asteria=asteria_runtime.cli:main`，并检测到 `dist/asteria_runtime-0.1.0-py3-none-any.whl`。
- 安装包 core 修复：installed `/acceptance --suite core` 曾暴露 wheel 环境缺少仓库脚本 `scripts/real_model_acceptance.py`，现已 fallback 到 `python -m asteria_runtime.real_model_acceptance`，避免源码路径偶然通过。
- 路由证据：`aggregate.route_evidence.strong_used=true` 且 `medium_used=true`；GLM coding route 覆盖 `goal_spec`、`run_review`，MiniMax 覆盖 `task_execution`、`task_repair`。
- 成本状态：历史 gray suite 中多文件 scope 场景曾出现 `near_limit`；planned verification 兜底修复后，2026-05-16 单场景复跑和 2026-05-17 完整 gray suite 复跑中的 `gray_multi_file_scope` 均为 `within_budget`。这说明修复方向有效，但进入产品灰度后仍要继续观察多轮真实任务波动，不能直接放大到长时间无人值守运行。
- 运行态证据位置：`.asteria/model/real_model_gate_report.json`、`.asteria/verification/real_model_acceptance_gray.json`、`.asteria/verification/real_model_acceptance_gray_after_planned_verification.json`、`.asteria/verification/runtime-boundary-real-gate-20260517-125203`、`.asteria/verification/runtime-boundary-gray-rerun-20260517-130738`、`.asteria/acceptance/acceptance_report.json`、`.asteria/model/capability_profile.json`。这些目录属于运行产物，默认不进入 git；文档只记录摘要和结论，不提交完整 transcript 或 API 相关环境信息。

## 套件

- `smoke`：最小文件创建闭环。
- `gray`：灰度发布候选验证，覆盖文件产出、多文件 scope、debug 修复和 runtime request resume。
- `core`：`file_smoke`、`password_cli`、`markdown_kb`、`safe_file_renamer`、`multi_file_todo_cli`、`config_driven_report`，以及 Runtime OS 发布门槛场景。
- Runtime OS 场景：`runtime_parallel_readonly`、`runtime_disjoint_writes`、`runtime_worker_failure`、`runtime_merge_gate_block`、`runtime_request_resume`、`runtime_context_package_slice`、`runtime_sandbox_backend_selection`、`runtime_planner_scope_quality`、`runtime_capability_feedback`、`runtime_evidence_consumption`。
- `advanced`：失败测试修复和决策点处理。
- `nightly`：所有不依赖用户数据、适合定期运行的真实验收场景。
- `offline`：只用于 fake provider 的脚本自身覆盖。

## 推荐命令

先跑 provider 健康检查：

```powershell
python -m asteria_runtime /model-check --root . --tier strong
python -m asteria_runtime /model-check --root . --tier medium
```

如果 `doctor --json` 显示 medium 通过全局 `AGENT_MODEL_*` fallback 生效，而 `real-model-gate` 要求显式 tier route，可在当前 shell 中把全局 MiniMax 路由映射到 medium tier；不要把 key 写入文档或提交到仓库：

```powershell
$env:AGENT_MODEL_MEDIUM_PROVIDER = $env:AGENT_MODEL_PROVIDER
$env:AGENT_MODEL_MEDIUM_NAME = $env:AGENT_MODEL_NAME
$env:AGENT_MODEL_MEDIUM_API_KEY = $env:AGENT_MODEL_API_KEY
$env:AGENT_MODEL_CHEAP_PROVIDER = "fake"
```

灰度发布验证前必须先跑真实模型 gate：

```powershell
asteria real-model-gate --summary-json .asteria/model/real_model_gate_report.json
```

该 gate 不等同于完整 acceptance。它只证明当前 key、endpoint、strong/medium 路由、最小 `/run`、Worker evidence、TaskExecutionEvidence 和成本日志可用。只有 gate 通过后，才进入 core/nightly acceptance 或小范围真实任务灰度。

运行灰度候选场景集：

```powershell
asteria real-model-acceptance --suite gray --summary-json .asteria/verification/real_model_acceptance_gray.json
```

`gray` suite 会在 summary 中写入 `gray_ready`。只有 `gray_ready=true` 且 `aggregate.route_evidence` 显示 strong/medium route 都被实际使用，才继续进入 core acceptance。
安装包隔离 venv 中运行 `gray` suite 时，所选 `--python` 解释器需要可用 `pytest`；`gray_debug_repair` 会执行 `python -m pytest test_buggy_math.py -v`。若使用临时 venv 验证 wheel，请先在该 venv 内安装 pytest，或通过 `--python` 指向已有 pytest 的解释器。

运行最小真实模型闭环：

```powershell
asteria real-model-smoke --summary-json .asteria/verification/real_model_smoke.json
```

运行当前真实任务验收集：

```powershell
asteria acceptance --suite core --allow-fake
asteria acceptance-gate --suite core --min-scenarios 10 --min-capabilities 10
```

额度允许时运行更完整的 nightly 验收：

```powershell
asteria real-model-acceptance --suite nightly --summary-json .asteria/verification/real_model_acceptance_nightly.json
```

持久化可对比历史：

```powershell
asteria real-model-acceptance --suite core --summary-json .asteria/verification/real_model_acceptance_core.json --history-jsonl .asteria/verification/real_model_acceptance_history.jsonl
```

`scripts/real_model_smoke.py`、`scripts/real_model_gate.py` 和 `scripts/real_model_acceptance.py` 仍保留为仓库开发兼容 wrapper；安装包和灰度用户路径应优先使用 `asteria real-model-*`。

## 摘要指标

`asteria real-model-smoke` 会写入：

- `duration_seconds`
- `diagnostics.run_status`
- `diagnostics.review_status`
- `diagnostics.review_score`
- `diagnostics.task_status_counts`
- `diagnostics.model_calls`
- `diagnostics.tool_calls`
- `diagnostics.estimated_input_tokens`
- `diagnostics.estimated_output_tokens`
- `diagnostics.repair_attempts`
- `diagnostics.context_compactions`

`asteria real-model-acceptance` 会把这些成本和稳定性指标按场景聚合。
提供 `--history-jsonl` 时，脚本会把每次 summary 追加到 JSONL 历史，并在当前 summary 中写入
`trend.previous` 和数值 delta，覆盖通过/失败数量、耗时、模型/工具调用、token 估算、修复次数和
context compaction 次数。runtime 的 `asteria /acceptance` 默认会把历史写入
`.asteria/acceptance/history.jsonl`。

查看 runtime 工作区历史：

```powershell
python -m asteria_runtime /acceptance-history --root . --limit 5
```

查看脚本级自定义历史文件：

```powershell
python -m asteria_runtime /acceptance-history --root . --history-jsonl .asteria/verification/real_model_acceptance_history.jsonl
```

把趋势告警作为本地门禁：

```powershell
python -m asteria_runtime /acceptance-history --root . --suite nightly --fail-on-warning
```

也可以让 `/acceptance` 在同一次验收中直接因为趋势告警失败：

```powershell
python -m asteria_runtime /acceptance --root . --suite core --fail-on-trend-warning
```

告警阈值可以配置：

```powershell
python -m asteria_runtime /acceptance-history --root . --warn-model-call-delta 5 --warn-duration-delta 120 --warn-repair-delta 1 --fail-on-warning
python -m asteria_runtime /acceptance --root . --suite core --warn-model-call-delta 5 --warn-duration-delta 120 --warn-repair-delta 1 --fail-on-trend-warning
```

## 通过标准

一个场景只有满足以下条件才算通过：

- `/model-check` 成功。
- 期望产物存在，并包含期望文本。
- 必要运行产物存在且非空。
- `run.json` 状态为 `completed`。
- `eval_report.json` 的 overall status 为 `pass`。
- 当前有效任务均为 done；`discarded` 只允许作为被 replan 替代的历史修复记录。
- 成本计数和 JSONL 日志数量一致。

真实验收失败不应被忽略，应通过 `asteria /acceptance --promote-failures` 纳入修复闭环。

## 能力画像

验收报告应写入 `scenario_metadata`，并在每个场景上保留 `capability` 和 `tier`。runtime
读取旧历史时会根据已知场景名回填这些字段，避免 `asteria capability-report` 和
`asteria acceptance-gate` 把历史能力覆盖统计成 `unknown`。

最新发布状态以 `.asteria/acceptance/acceptance_report.json` 为准，因为它包含 runtime
计算出的趋势告警；脚本级 `history.jsonl` 可能只包含原始 summary。

常用门禁：

```powershell
python -m asteria_runtime /acceptance-gate --root . --suite core --min-scenarios 3 --min-capabilities 3
python -m asteria_runtime /acceptance-gate --root . --suite core --min-scenarios 3 --min-capabilities 3 --allow-trend-warnings
```

`core` / `nightly` release gate 默认要求 Runtime OS 证据：worker invocation/result、runtime profile、context mount、validation result、失败 evidence、candidate isolation、merge gate block、resume recovery、ContextPackage 按 `read_scope` 切片、sandbox backend 选择原因、Planner scope quality、capability feedback，以及 debug/review 对 Runtime OS evidence 的消费。只有在兼容旧报告时才使用 `--no-require-runtime-os`。

第一条会因为成本或稳定性趋势告警阻断。第二条允许趋势告警，把状态标记为
`conditional`，适合在更宽的真实基线已经通过、但仍需要继续优化成本时使用。
## Runtime OS core gate 组合命令

发布前的完整 core gate 应把普通真实模型场景和 Runtime OS 场景放在同一次 core suite 里运行，避免后一次报告覆盖前一次报告导致 gate 只看到部分场景：

```powershell
python -m asteria_runtime /acceptance --root . --suite core --fail-on-trend-warning
python -m asteria_runtime /acceptance-gate --root . --suite core --min-scenarios 10 --min-capabilities 10
```

如果只想快速验证 Runtime OS release gate 的结构化证据，可以单独跑确定性的 Runtime OS 子集；这适合开发期回归，不等价于完整真实模型 core gate：

```powershell
python -m asteria_runtime /acceptance --root . --suite core --allow-fake --scenario runtime_parallel_readonly --scenario runtime_disjoint_writes --scenario runtime_worker_failure --scenario runtime_merge_gate_block --scenario runtime_request_resume
python -m asteria_runtime /acceptance-gate --root . --suite core --min-scenarios 5 --min-capabilities 5
```

Runtime OS gate 的能力清单和 required evidence 由 `src/asteria_runtime/acceptance/runtime_os_catalog.py` 维护，检查逻辑由 `RuntimeOSGateEvaluator` 统一执行。后续 `capability-report`、`weekly-report` 或 dashboard 如果需要复用发布门槛，应消费 evaluator 结果，而不是重新硬编码 capability 名称。
