{
  "schema_version": "1.0.0",
  "purpose": "Phase 5 unified harness scenarios: execute parallel_disjoint vs subagent swarm planning.",
  "phase": "5",
  "paths": {
    "execute_parallel_disjoint": {
      "description": "Task graph parallel disjoint writes with candidate promotion.",
      "required_events": ["parallel_safe_batch_selection"],
      "required_files": ["experiments.jsonl", "worker_results.jsonl", "agent_run_graph.json"],
      "min_workers": 2,
      "min_promoted_files": 2
    },
    "subagent_swarm_planning": {
      "description": "Subagent decomposition with orchestrator plan evidence.",
      "required_files": [
        "subagent_child_plans.jsonl",
        "swarm_execution_plans.jsonl"
      ],
      "min_swarm_plans": 1
    }
  },
  "notes": "At least one path must pass audit per run. Both may coexist in complex runs."
}
