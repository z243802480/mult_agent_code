{
  "schema_version": "0.1.0",
  "purpose": "Wave 4 workflows gray — isolated real_disjoint probe + route regression",
  "brief": "benchmarks/reference_briefs/S64-orchestration-wave4-workflows-probe.md",
  "depends_on": [
    "benchmarks/orchestration_wave3_catalog_gate.json",
    "benchmarks/phase5b_swarm_rollout_gate.json"
  ],
  "thresholds": {
    "route_regression_min_hit_rate": 1.0
  },
  "route_regression_cases": [
    {
      "id": "w4_small_edit_no_parallel",
      "message": "给 greet_cli 增加 --version 参数并补测试",
      "expect_capability": "cold_goal_execute",
      "accept_capabilities": ["cold_goal_execute", "session_continue_execute"],
      "forbidden_capabilities": ["spawn_parallel_workers"]
    },
    {
      "id": "w4_read_only_no_parallel",
      "message": "先只读分析 orchestration_router，不要改文件",
      "expect_capability": "read_only_plan",
      "forbidden_capabilities": ["spawn_parallel_workers"]
    }
  ],
  "real_disjoint_probe": {
    "required_spawn": {
      "scheduling_mode": "parallel",
      "fake_path": false
    },
    "required_evidence": ["worker_spawn_plan.json", "events.jsonl"]
  }
}
